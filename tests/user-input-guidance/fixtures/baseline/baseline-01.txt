Create user authentication API endpoint. The system should allow users to register with email and password. The endpoint will validate the email format and password strength. Upon successful registration, the system stores the user data in the database and returns a success response with the user ID.
