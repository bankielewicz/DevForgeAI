Implement job queue system (RabbitMQ or AWS SQS) processing long-running tasks asynchronously at 10,000 jobs/minute. Handle email batches (<5 min), report generation (<30 min), bulk imports (<15 min), with 3 retries (5s, 10s, 30s exponential backoff) and dead-letter queue for failures. Given: User initiates bulk user import (10,000 records). When: Job queued for background processing. Then: User receives confirmation immediately, import completes within 10 minutes. Given: Job encounters error (database unavailable, API timeout). When: Error occurs. Then: System retries 3 times with exponential backoff (5s, 10s, 30s) before failing. Given: Job processing encounters fatal error. When: Max retries exhausted. Then: