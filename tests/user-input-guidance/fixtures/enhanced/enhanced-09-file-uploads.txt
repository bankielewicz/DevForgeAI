File upload service for PDF, CSV, JPEG, PNG, DOCX.
Max 500MB per file, 2GB per session.
Store customer documents and transaction records.
Resume on connection drop.

Given: End user selects 200MB file for upload.
When: Upload starts.
Then: Progress every 500ms, complete in 60s.

Given: Connection drops at 75%.
When: Reconnect.
Then: Resume from checkpoint, no data loss.

Checkpoints saved every 10MB.

Given: End user uploads PDF.
When: File arrives.
Then: ClamAV scans in 10 seconds.

Malware: reject, alert end user.
Clean: S3 AES-256 encryption.

Progress every 500ms. Resume from checkpoint.

Performance: support 200MB+ transfers.
Reliability: resume, scan.
