# Phased authoring cases: synthetic source inputs

These are authored fixtures, not observed project facts or executed results. The evaluator assigns a disposable source/output fence and pins these bytes before running cases.

## Framework workflow

Target provider: Codex. Target canonical source: the evaluator-assigned fixture repository's providers/codex/plugins/devforgeai/skills/release-notes-guide. Purpose: draft release notes from a supplied change list. Activation: requests to draft release notes. Inputs: accepted change list and audience. Output: saved draft notes and source references. No application architecture is needed. Explicit accepted group classification: Collect (C1), Draft (D1), and PreparedTransfer (H1) and their listed actions are Enforced requirements. Rust CLI is the required enforcement owner; no corresponding interception is demonstrated in this fixture. Hook designs remain Unknown/design only. Optional Appendix (A1) applies only when the user asks for technical detail; it never replaces the required source references.

## Existing inline source

The evaluator materializes this block as the canonical SKILL.md only for enhancement/reuse cases. Preserve these exact obligations and IDs:

- C1: Read the supplied accepted change list; missing list blocks drafting.
- D1: Draft notes for the supplied audience; label unverified claims and retain source references.
- A1: Optional technical appendix only when requested; required references still accompany the main notes.
- H1: Save draft notes and source references in the assigned artifact location; give one next action and owner.
- No publication, shell command execution, installation or automatic receiver invocation.
- Accepted decisions above persist; relocation is authorized, behavior change is not.
- Normal implicit activation remains enabled.

## Search inventory

The assigned Codex canonical release-notes-guide owns exactly the purpose, activation, inputs, outputs and obligations above. The visible installed copy maps to that source. A separately owned Claude release-notes-guide has the same name and equivalent purpose but is a different provider implementation. No other accessible candidate is supplied; report this search limit.

## Simple reference expert

Target: an assigned Codex terminology expert for the accepted three-term glossary: XSPEC = capability specification; XPKG = candidate manifest and provenance; EVREPORT = evaluation evidence/report. It answers terminology questions from this fixed glossary, labels unknown terms unavailable and performs no multi-step production workflow. No phases or hooks are requested.

## Missing input variant

The framework workflow's accepted change list is absent. Its declared source location is unavailable. Do not invent changes or source identity. Other inventory/design information above remains usable.
