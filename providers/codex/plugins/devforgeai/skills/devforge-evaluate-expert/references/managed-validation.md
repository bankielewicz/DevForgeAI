> Legacy managed v1 reference. Manual promotion does not admit this adapter under the new package name. Use manual-operation.md for the promoted flow.

# Managed validation evidence

Use this reference only for an actual admitted utility assignment. Exact selected DevForge mechanical interface identities are in derivation.json. Package presence and model-generated session fields do not create authority. The operator owns devforge.utility-session/v1, its devforge.utility-delivery/v1 contract, protected journal, accepted gate producers and devforge.utility-receipt/v1 publication/readback.

## Useful work by phase

| Phase and tasks | Substantive evidence | Protected prerequisite |
| --- | --- | --- |
| P1 / T01-T02 | Exact target/provider/authority, specification, source/installed/baseline identities, cases and scope; bounded workspace allocation and missing native inputs | Actual assigned source/write fence and selected input bytes |
| P2 / T03 | Raw deterministic inspection records and interpreted structural limits | External deterministic-inspection producer gate |
| P3 / T04 | Independent R01-R10 review evidence, findings, reviewer identity and independence limits | External independent-review producer gate |
| P4 / T05-T08 | Preparation/readiness records and separate C/B/A native outcomes, raw observations and missing causes | External native-prerequisites, native-C, native-B and native-A gates |
| P5 / T09 | Evidence-based disposition, exact coverage/freshness and separate failure/unavailable states | All applicable current evidence; no worker-issued PASS |
| P6 / T10-T12 | Saved verification results, bounded enhancement specification, prepared continuation and custody references | Runtime checks final saved bytes and publishes/readbacks its receipt |

All phases and tasks remain Enforced. Missing observations do not become passes and do not prevent honest failure reporting. Report COULD_NOT_RUN with the cause where required evidence is unavailable; leave unattempted executions NOT_RUN. Runtime accepts only its selected external producer inputs. A local helper result or supplied grade string is not authority to fabricate or replace a gate record. Semantic review quality remains an independently assessed claim.

## Evidence submission and questions

Use actual supplied task, phase, sequence, challenge, selected-input digest, paths and required artifact fields. Save useful outputs and submit the allocated devforge.utility-checkpoint/v1 shape with concrete saved artifact references. A ready checkpoint requests inspection; it cannot advance its own phase. Retain runtime feedback and the remaining correction count. Never invent a callable schema or controller command from this overview.

For a consequential missing answer, provide the assigned stable question identity and affected dependency. The runtime preserves WAITING_USER. A finite-choice response must match its selected rule; a free-text consequential response needs the operator's interpretation bound to the exact pending question and observed prompt. Unrelated messages, synthetic continuation text and elapsed time do not resolve it. Continue only independent permitted work. Original deadlines and bounded correction never reset on pause/resume.

Do not call advance, resume, complete, check, verify or a receipt helper as a fallback for missing protected context. Evaluator helpers may create their documented scoped inspection/adjudication outputs; they do not confer runtime completion, native authenticity or acceptance.

## Preparation and native admission

Retain the selected Git-worktree option. Freeze its bounded repository/common-directory/base/destinations/write allocation before creating workspaces. Preparation can proceed while model/authentication/repetitions/native budgets are pending; record actual setup and missing readiness separately. Workspace templates neither admit a native worker nor allocate unlimited additional workspaces.

Before native execution freeze every required case/arm/repetition and every control, probe, grader, receiving execution and failed launch charge. Confirm the complete allocation fits the approved cap. Report a shortfall without dropping cases. Use distinct unused workspaces and client states, the selected subscription arrangement, no credential copying, and observed source/history/tool/process boundaries. Preserve the original campaign deadline through pauses and allow no unallocated retry or continuation.

Run C, then B, then A only when their applicable prerequisite observations exist. Keep process custody, semantic grading, callback origin, selection, actual resource consultation, task completion and rendered delivery separate. An authenticated collector receipt cannot prove the other dimensions. The reviewed mechanical interface is not observed native readiness. Interactive cases need a separately selected owned answer transport; a single-turn collector or PTY existence is insufficient. Missing authentication, boundary, hooks, allocation or transport prevents dependent native work and remains an explicit prerequisite.

## Creation-time delivery

Populate the handoff only at completion, transfer or recovery. Use saved outputs and only observations existing when it is authored. Runtime owns managed final-byte checks, immutable snapshots and receipt publication/readback; retain null with a cause if the final identity has not yet been supplied. Ordinary unmanaged source/write identities may be labelled as author observations. A containing document never hashes itself, creates a mutual digest dependency, or claims a later receipt.

The handoff is substantive continuation. Prepared transfer, an admitted transition and an actually invoked receiving skill are distinct. Preserve all earlier failures and unavailable outcomes; later installed refreshes or component tests do not change them. No native callback authentication, rendered delivery, receiving execution or external acceptance is established by this package or by mechanical receipt custody.
