# Framework and project context

This package implements SKILL-008, devforge-evaluate-expert for Codex only. DevForgeAI remains an adaptive, spec-driven framework in local POC development. Claude support is unchanged and not established by this promotion.

DevForgeAI owns conversational methodology, skills and project expertise. DevForge owns protected runtime, deterministic gates and installation. The creator authors; the evaluator independently evaluates and returns a bounded repair specification. The user initiates receiving skills and commands. Package identity, structural binding, behavioral evidence, human acceptance and operational installation remain separate.

Derive all resource paths from the loaded package directory; do not require the framework checkout. Project experts use the consuming project's declared source/output map. Ground expertise in the actual goal/story, accepted architecture, code and pinned dependencies. Preserve decisions and source versions; unverified API claims remain missing inputs.

Read [manual operation](manual-operation.md), [authoring contract](contracts/skill-authoring-contract.md), [artifact contract](contracts/artifact-contract.md) and [execution contract](contracts/execution-contract.md) as relevant. references/derivation.json preserves historical sources and the new derivative identities. Old prototype names in provenance and managed v1 references are historical identities, not discoverable aliases.
