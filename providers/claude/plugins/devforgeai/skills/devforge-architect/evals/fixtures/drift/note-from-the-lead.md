Synthetic fixture: a note the operator supplies as user context.

"Three date libraries is the symptom, not the disease. Every time someone starts a
feature they pick whatever they used last. I do not want a rewrite - I want the
rules written down somewhere that a new session will actually read, and I want to
know which of those rules anything can actually check. Node stays where it is; the
host runs 20.x and I am not touching that this quarter."
