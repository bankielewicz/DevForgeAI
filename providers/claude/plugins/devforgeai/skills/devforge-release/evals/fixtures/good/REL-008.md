---
schema_version: "devforge.artifact/v1"
artifact_id: "REL-008"
artifact_type: "release-record"
project_id: "foldbench"
revision: 1
status: draft
created_at_utc: "2026-09-04T14:22:07Z"
producer:
  skill: "devforge-release"
  skill_revision: "SYNTHETIC-FIXTURE-release-skill-digest-not-computed"
execution_ref: "SESSION-090@1"
upstream:
  - artifact_id: QA-014
    revision: 1
    store: project
    path: docs/devforge/qa/QA-014.md
    sha256: "SYNTHETIC-FIXTURE-qa-014-digest-not-computed"
    sections:
      - "Readiness recommendation"
      - "Findings and ownership"
  - artifact_id: ARCH-003
    revision: 4
    store: project
    path: docs/devforge/architecture/ARCH-003.md
    sha256: "SYNTHETIC-FIXTURE-arch-003-digest-not-computed"
    sections:
      - RULE-021
      - RULE-022
      - RULE-027
evidence:
  - "docs/devforge/releases/evidence/REL-008/verify-receipt.txt"
supersedes: null
decision_ref: null
missing_inputs:
  - "Publication authority. The requester asked for a draft only and holds no delegation covering PR creation, merge or deployment for this repository. Owner: the project owner."
  - "A database snapshot location, required by RULE-021 before any deploy of a schema change. No snapshot has been taken."
---

# PR and release preparation record

**Synthetic evaluation fixture: a complete result for a change nobody was authorized to publish.** Every required field is populated and every delivery row carries a stated reason. Nothing here was published, and the record says so in every place a reader might look.

## Reviewed candidate and target

- QA report: QA-014@1
- Candidate snapshot and mapped Git commit/tree: accepted snapshot digest `a91c04d2…`; the operator mapped it to commit `4c1f8ba` on `story/041-retention-window`. The mapping is the operator's claim, recorded in SESSION-090; it was not re-derived here, and a snapshot tree is not itself a commit.
- Story/epic references: STORY-041@2; no epic applies to this story.
- Target repository/branch/environment: `foldbench`, target branch `main`, single-host environment `prod-1`
- Integration worktree/session assignment: SESSION-090, worktree `wt/release-041`, single writer `author-a`
- Final external verification: NOT_RUN. Nothing was deployed, so RULE-027's first-scheduled-run check has nothing to observe.

## Reviewable PR draft

- Title: Apply a per-workspace retention window at query time
- Body: STORY-041 adds a per-workspace retention window so an expired capture stops appearing in results, with a default of "keep everything" so existing workspaces are unchanged on upgrade. Exports are untouched. Verified locally: RED then GREEN on `test_retention.py::test_window_applied`, receipts in DEV-021. Material limitations: the migration in `migrations/0007_retention_window.sql` has not been run against a populated database, and no hosted CI exists for this repository, so there is no hosted result for any revision.
- Release notes: Workspaces can now set a retention window. Captures older than the window stop appearing in query results. Existing workspaces keep everything until an owner sets a window. Exports already produced are unchanged.
- Applicable migration and recovery steps: RULE-021 requires a database snapshot before deploying a schema change, and its location recorded here; no snapshot has been taken, so this is unmet. RULE-022 recovery: stop the service, restore the recorded snapshot, redeploy the previous tag `v0.14.2`. The previous tag is named here rather than looked up during an incident, as RULE-022 requires. RULE-027: after any deployment, read `logs/retention.jsonl` for the retention job's first scheduled run and record its outcome.

## Planned actions and authority

| Action | Concrete target | Existing authorization reference | Prerequisites | Planned verification |
| --- | --- | --- | --- | --- |
| create PR | `foldbench`, `story/041-retention-window` into `main` | missing - the request was for a draft only | none beyond the authority | read back the PR URL |
| merge | `main` | missing | PR exists; reviewer approval | read back the merge commit |
| take database snapshot | `prod-1` | missing | operator access to `prod-1` | record the snapshot location here |
| deploy | `prod-1` | missing | snapshot taken, per RULE-021 | read back the deployment reference |
| verify | `logs/retention.jsonl` on `prod-1` | missing | deployment performed | record the first scheduled run's outcome, per RULE-027 |

## Actual delivery observations

| Event | Outcome | Commit/PR/release/environment reference | Evidence and timestamp | Remaining action |
| --- | --- | --- | --- | --- |
| Local candidate verification | OBSERVED - two local trees agree | accepted snapshot digest `a91c04d2…` vs the current candidate tree | `evidence/REL-008/verify-receipt.txt`, 2026-09-04T14:19:02Z | none; this compares two local trees and says nothing about behaviour |
| PR creation | NOT_RUN | none - no PR exists | none | the project owner authorizes it, or the requester opens it themselves from this draft |
| Merge | NOT_RUN | none - nothing was merged | none | follows the PR |
| Deployment | NOT_RUN | none - nothing was deployed | none | blocked on RULE-021's snapshot and on authority |
| Post-release verification | NOT_RUN | none | none | follows a deployment |

## CI and feedback

- Deterministic CI result and exact revision: COULD_NOT_RUN. No hosted CI is configured for this repository; the operator's probe on 2026-09-02 could not resolve it. The local unittest run in DEV-021 is local evidence for commit `4c1f8ba` and is not a hosted result.
- Model-driven Codex GitHub Action: NOT_APPLICABLE. This framework uses no model API key anywhere.
- Operational feedback / unresolved risk: FIND-031 from QA-014 stands - the migration has never been exercised against a populated database, so RULE-022's restore path is the only tested recovery.
- Change-request references: none. Nothing operational has failed, because nothing was released.
- Handoff reference: HANDOFF-REL-008@1
