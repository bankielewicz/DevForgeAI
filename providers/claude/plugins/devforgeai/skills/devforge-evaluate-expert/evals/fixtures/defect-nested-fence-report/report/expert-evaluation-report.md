# Expert evaluation results (synthetic, deliberately defective)

This report states no recommendation of its own. Its only recommendation line
sits inside a four-backtick block that quotes a three-backtick example:

````markdown
A populated report reads like this:

```

recommendation: suitable for the stated scope

```
````

A closing fence must use the same character as its opening fence and be at least
as long, so both three-backtick lines above are content of the four-backtick
block rather than the end of it. A reader that closes the outer block at the
inner example puts the recommendation line back into the document and reports a
field this report does not have.

## Findings

None recorded.
