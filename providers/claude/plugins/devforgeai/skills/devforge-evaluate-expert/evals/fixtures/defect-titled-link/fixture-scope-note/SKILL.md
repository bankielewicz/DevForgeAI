---
name: fixture-scope-note
description: Produce a short scope note from supplied project facts.
---

# Fixture scope note

Read [the required format](references/required-format.md "Required format") before
producing any note. That file is deliberately absent from this fixture.

The link title makes this destination a representation the restricted parser does
not interpret. A parser that simply fails to match the link sees no link at all
and reports that every local destination resolved, which states a fact about a
file it never looked at. The honest answer is that this link was not read.
