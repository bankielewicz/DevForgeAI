---
name: fixture-scope-note
description: Produce a short scope note from supplied project facts.
---

# Fixture scope note

<img src="missing.png" alt="the scope note layout">

Read [the required format] before producing any note.

Neither resource exists in this fixture. Both are representations the restricted
parser does not interpret: an HTML resource attribute, and a link reference
definition whose destination belongs to the shortcut reference above it. Passing
over either one counts zero links and reports that every destination resolved.

The grader names the first unsupported representation it reaches, so this fixture
evidences one of the two per run; each is exercised on its own in the correction
record.

[the required format]: nothere.md
