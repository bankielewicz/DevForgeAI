# Expert evaluation results (synthetic, deliberately defective)

This report has no Disposition or recommendation field of its own. The only
lines that look like fields sit inside an illustrative fence:

```text
recommendation: suitable for the stated scope
behavior_status: NOT_EVALUATED
```

A field named only inside an example is an illustration. A grader that reads it
as the document's own value reports a populated field the document does not have.

## Findings

None recorded.
