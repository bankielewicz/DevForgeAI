# Template: Development delivery

**Producer:** current `dev`  
**Consumers:** `qa` (primary); human; `release` only after QA and authorization  
**Operational template:** `src/agents/skills/dev/assets/delivery.md`  
**Does not:** grant protected acceptance, QA verdict, or deploy permission.

## Envelope

| Field | Value |
| --- | --- |
| Delivery identity / UTC | |
| Overall status | COMPLETE / PARTIAL / BLOCKED |
| Producer | dev |
| Downstream consumer | qa |
| Failure behavior | Missing required output path prevents VERIFIED/COMPLETE even if tests pass |
| Non-claims | Developer QA is not independent QA |

## Upstream inputs

| Role | Locator | SHA-256 |
| --- | --- | --- |
| Selected specs / stories | | |
| Context / slice plan / traceability | | |
| qa-fix (if repair) | | |

## Candidate

- Source / test / config manifest locators and hashes:
- Tools / platforms:
- Git branch / HEAD / dirty state if present:
- Changed paths and purpose:

## Requirement accounting

- Selected count / verified / pending / blocked / excluded: [from traceability]
- Plan-only or subset limitation: [what this delivery does not cover]

## Required output readback

Original `selected_evidence_value` / `selection_source` / `resolved_evidence_root`: [reread from original input].

| Required output | Required path | Observed path | Digest | Result |
| --- | --- | --- | --- | --- |
| | | | | verified / mismatch / missing |

## Checks and metrics

| Check / platform | Attempt / report | Result | Counts or unperformed reason |
| --- | --- | --- | --- |
| behavior / integration / regression / negative / recovery / format / static / coverage / native | | PASS / FAIL / ERROR / NOT_RUN / NOT_APPLICABLE | |

- Required-case set and pass-rate: [passing / all required; do not round below the policy floor]
- Coverage: [definition, denominator, exclusions, tool, executed-line vs branch]
- Per-platform: [unavailable required cases are NOT_RUN]
- Earlier attempts / flakes: [retained; no denominator inflation]

## Gaps and next action

- Remaining defects: [requirement IDs]
- External framework acceptance: actual authority locator or `NOT_EVALUATED`
- Checkpoint if work remains: [path]
- Next safe action: [qa invocation text or missing input]

## Downstream handoff

**To qa:** this delivery + candidate identity + specs/stories. QA binds current bytes itself.

**Return path:** qa-fix on FAIL comes back to `dev` as a new selected repair, producing a **new** candidate and delivery. Do not edit this file to hide the earlier attempt.
