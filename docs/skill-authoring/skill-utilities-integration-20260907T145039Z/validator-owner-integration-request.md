# Prepared validator-owner integration request

Status: prepared coordination artifact; no receiving invocation, source edit or installation is performed by this document. The user confirmed a separate session is actively editing skill-validator. That owner retains the canonical source, design, history and installation.

After that owner releases an exact source identity, use skill-builder only for a bounded authoring enhancement. Preserve W1, P1-P6 and T01-T12 as Enforced and preserve the Git-worktree preparation option, including the distinction between workspace allocation and complete native plan admission. Source remains providers/codex/plugins/devforgeai/skills/skill-validator; installed copies are generated resources.

The runtime integration branch is impl/skill-utilities-runtime-20260907 in the companion repository. Its current interface is under review, not a deployment contract. Wait for its reviewed committed identity before authoring against it. It introduces separate devforge.utility-session/v1, devforge.utility-delivery/v1 and devforge.utility-checkpoint/v1 schemas. The worker writes selected output files and checkpoint evidence using runtime-supplied task, phase, sequence, challenge and input binding. It does not invoke advance/resume/complete/native-admission or a receipt helper. The external supervisor transports SessionStart, UserPromptSubmit, Stop and SessionEnd. Native launching remains unavailable; reservation alone cannot establish execution, isolation or C/B/A results.

Refresh selected contract copies and record their original and new hashes. Reconcile H1-H5 as requirement mappings to the actual managed interface; do not activate legacy callbacks alongside managed callbacks. Required external evidence must preserve independent static/review producers and each C/B/A outcome, including explicit missing-observation causes. Reporting completion must remain separate from passing validation and release acceptance. Prepared output and authoritative receipt are separate artifacts; no document embeds its own digest or is rewritten to claim subsequent delivery.

Next authoring outputs: bounded specification amendment, before/after source manifests, change dispositions, revised package resources, selected derivation record and prepared handoff. The integration owner then generates the installation and a separate evaluator freezes new candidate/baseline/cases/configuration/budget for deterministic checks, independent AI review and authorized C/B/A attempts. Existing reports remain unchanged.

Pending user decisions: actual native client/model, subscription authentication arrangement without credential copying, native repetition/attempt/time budget, and the builder phase-group classification left unresolved by its governing specification. These pending decisions do not authorize edits to another session's files or imply unavailable native success.
