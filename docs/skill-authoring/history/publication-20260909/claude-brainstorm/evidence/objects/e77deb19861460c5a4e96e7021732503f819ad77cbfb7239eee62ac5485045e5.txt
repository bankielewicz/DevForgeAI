#!/usr/bin/env python3
"""Deterministic controls for the actual phase path. No model, ever.

These do not re-implement what the runner should do. Each scenario copies the
real control and checker files into a scratch run root, generates the real
freeze over those bytes, imports the real `run_phases` from that copy, and calls
its real `main()`. The only substitution is at the seam where the model call
happens: `LAUNCH_HOOK` records a marker and fabricates the arm's outputs.

`harness.stream` is replaced with a function that raises, and PATH is emptied,
so a scenario that somehow reached a real launch would fail loudly rather than
run something. Every scenario asserts a real-model invocation count of zero.
"""
import hashlib, importlib.util, json, os, shutil, sys, time, types

HERE = os.path.dirname(os.path.abspath(__file__))
RUN = os.path.dirname(HERE)
WORK = os.path.join(HERE, "work-phases")
if os.path.exists(WORK):
    shutil.rmtree(WORK)
os.makedirs(WORK)
results = []
REAL_MODEL_CALLS = {"count": 0}
sys.path.insert(0, HERE)
import cases_r9 as CR9


def rec(cid, desc, expected, actual, extra=None):
    row = {"id": cid, "description": desc, "expected": expected, "actual": actual,
           "result": "PASS" if expected == actual else "FAIL",
           "real_model_invocations": 0}
    if extra:
        row.update(extra)
    results.append(row)
    return row


def dg(t):
    return hashlib.sha256(t.encode()).hexdigest()


def stage_preflight(root):
    """Put every declared preflight input at its real intended location.

    CHG-005 / CHG-020 / R9-CI-002. `freeze.PREFLIGHT` names five files under the
    declared preflight root, and the required positive is that an unchanged real
    -location set verifies. Revision 3 never staged them, so the positive could
    not resolve and the negative it was supposed to contrast with meant nothing.
    """
    nm = os.path.join(root, "no-model-tests")
    os.makedirs(nm, exist_ok=True)
    written = {}
    for name, body in (("results.json", {"conformance": "PASS"}),
                       ("checks-results.json", {"conformance": "PASS"}),
                       ("phase-results.json", {"conformance": "PASS"})):
        path = os.path.join(nm, name)
        json.dump(body, open(path, "w"), indent=1)
        written[name] = path
    for name in ("checks_suite.py", "phase_suite.py"):
        src = os.path.join(RUN, "no-model-tests", name)
        dst = os.path.join(nm, name)
        if os.path.isfile(src):
            shutil.copyfile(src, dst)
            written[name] = dst
    return written



LEDGER = """---
schema_version: "devforge.artifact/v1"
artifact_id: "IDEAS-001"
artifact_type: "idea-ledger"
revision: 1
upstream: []
missing_inputs: []
---

# Ledger

## Ideas and alternatives

row
"""
HANDOFF = """---
schema_version: "devforge.artifact/v1"
artifact_id: "HANDOFF-001"
artifact_type: "handoff"
revision: 1
upstream:
  - artifact_id: "IDEAS-001"
    revision: 1
    store: project
    path: docs/devforge/ideas/IDEAS-001.md
    sha256: "%s"
    sections:
      - "Ideas and alternatives"
missing_inputs: []
---

# Handoff

## Observed verification

| Check | Scope | Outcome |
| --- | --- | --- |
| placeholders | helper | PASS |
""" % dg(LEDGER)

GOOD_FILES = {"docs/devforge/ideas/IDEAS-001.md": LEDGER,
              "docs/devforge/handoffs/HANDOFF-001.md": HANDOFF}
GOOD_TERMINAL = ("Saved docs/devforge/ideas/IDEAS-001.md — SHA-256 %s\n"
                 "Saved docs/devforge/handoffs/HANDOFF-001.md — SHA-256 %s\n"
                 % (dg(LEDGER), dg(HANDOFF)))
BAD_SECTION_HANDOFF = HANDOFF.replace('      - "Ideas and alternatives"', '      - "No Such Section"')
BAD_TERMINAL = "Saved the files.\n"


def build_root(name, arms, semantic, c_files, c_terminal, b_files=None, b_terminal=None,
               mutate_after_freeze=None, plan_over=None, preexisting=None):
    root = os.path.join(WORK, name)
    os.makedirs(root)
    for sub in ("control", "checkers", "fixtures", "no-model-tests", "attempts",
                "decisions", "grades"):
        os.makedirs(os.path.join(root, sub), exist_ok=True)
    for f in os.listdir(os.path.join(RUN, "control")):
        if f.endswith((".py", ".json")):
            shutil.copyfile(os.path.join(RUN, "control", f), os.path.join(root, "control", f))
    for f in os.listdir(os.path.join(RUN, "checkers")):
        if f.endswith(".py"):
            shutil.copyfile(os.path.join(RUN, "checkers", f), os.path.join(root, "checkers", f))
    plan = json.load(open(os.path.join(root, "control", "PLAN.json")))
    plan["outputs"] = {a["id"]: {"required_outputs": sorted(
        (c_files if a["phase"] == "C" else (b_files or {})).keys()), "optional_outputs": []}
        for a in arms}
    if plan_over:
        plan.update(plan_over)
    json.dump(plan, open(os.path.join(root, "control", "PLAN.json"), "w"), indent=1)
    # The bound decisions file is now always empty. A judgment written here
    # would be a judgment of output that does not exist yet, and revision 3 of
    # the runner refuses a freeze that carries one. Scenario judgments are
    # supplied through the pause/resume seam in drive() instead.
    json.dump({"schema_version": "devforge.semantic-decisions/v2", "decisions": [],
               "note": "judgments are generated after output and consumed at the arm's gate"},
              open(os.path.join(root, "control", "SEMANTIC-DECISIONS.json"), "w"), indent=1)
    json.dump({"deterministic": [{"id": "stub", "path": "no-model-tests/ok.json",
                                  "field": "conformance", "expect": ["PASS"]}]},
              open(os.path.join(root, "control", "GATE-INPUTS.json"), "w"), indent=1)
    json.dump({"conformance": "PASS"},
              open(os.path.join(root, "no-model-tests", "ok.json"), "w"))
    # Every entry of `freeze.PREFLIGHT` has to exist at its real location before
    # the freeze binds it. The r7 baseline left them absent, which bound them as
    # None; `controller.verify_freeze` then compared None to the None of a
    # missing file and passed. Now that an unresolved binding fails closed
    # (CHG-005 / R9-CI-002), a scenario that does not stage them would refuse at
    # startup for a reason that has nothing to do with what it is testing.
    stage_preflight(root)
    # generate the real freeze over these exact bytes
    spec = importlib.util.spec_from_file_location(
        "fz_" + name, os.path.join(root, "control", "freeze.py"))
    m = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(m)
    fz = m.build()
    fz["arms"] = arms
    # A synthetic scenario declares its own arms, so it must also declare that
    # they are authorized; otherwise the runner refuses at the zero-call
    # boundary, which is correct for the real preparation freeze and is itself
    # exercised as its own case.
    fz["native_arms_authorized"] = len(arms)
    json.dump(fz, open(os.path.join(root, "control", "FREEZE.json"), "w"), indent=1)
    if preexisting:
        d = os.path.join(root, "attempts", preexisting)
        os.makedirs(d)
        open(os.path.join(d, "SENTINEL.txt"), "w").write("another attempt's evidence\n")
    if mutate_after_freeze:
        p = os.path.join(root, mutate_after_freeze)
        with open(p, "a") as fh:
            fh.write("\n# edited after the freeze was taken\n")
    return root


DEFAULT_JUDGE = {a: "PASS" for a in ("C1", "B0", "B3", "B5", "B6", "B11",
                                     "A3a", "A5a")}


def drive(root, c_files, c_terminal, b_files, b_terminal, incomplete_arms=(),
          judge=None, a_invokes=(), max_resumes=8):
    """Run the real main(), supplying judgments only through its pause seam.

    `judge` maps an arm id to the outcome an authorized judgment should carry.
    An arm absent from it gets no judgment, which is how "missing judgment
    stops downstream work" is exercised. Each judgment is written from the
    binding the runner itself published after that arm's checks ran, so it can
    only be produced after the evidence exists -- and the resume that follows
    must not relaunch the arm, which the marker count proves.
    """
    markers = []
    # None means "the adjudicator returns PASS for these arms"; an explicit {}
    # means no judgment is ever supplied, which is a different scenario and the
    # one that must leave downstream work unstarted. The verdict's origin is not
    # what makes a judgment evidence -- the binding to this arm's actual digests
    # is -- so a scenario may declare the verdict in advance while the runner
    # still refuses anything that does not name the right bytes.
    judge = dict(DEFAULT_JUDGE if judge is None else judge)

    def hook(arm_id, cmd, d, cap):
        # The runner records its own marker at the launch seam; the hook does
        # not add a second one, so the count is the runner's own count.
        proj = os.path.join(d, "project")
        files = c_files if arm_id.startswith("C") else (b_files or {})
        term = c_terminal if arm_id.startswith("C") else (b_terminal or "")
        for rel, text in files.items():
            t = os.path.join(proj, rel)
            os.makedirs(os.path.dirname(t), exist_ok=True)
            open(t, "w").write(text)
        if arm_id.startswith("A"):
            # Phase A observes selection, so the stand-in leaves a transcript
            # for the activation check to read rather than writing artifacts.
            open(os.path.join(d, "transcript.jsonl"), "w").write(
                json.dumps({"type": "tool_use", "name": "Skill",
                            "input": {"skill": "devforge-brainstorm"}}) + "\n"
                if arm_id in a_invokes else
                json.dumps({"type": "text", "text": "answered directly"}) + "\n")
        if arm_id in incomplete_arms:
            return {"terminal": "EOF_WITHOUT_RESULT", "terminal_detail": "stand-in: no result",
                    "result_text": None, "seconds": 0.0, "tool_results": {"ok": 0, "error": 0},
                    "os_exit": {"returncode": None}}
        return {"terminal": "COMPLETED", "terminal_detail": "stand-in", "result_text": term,
                "seconds": 0.1, "tool_results": {"ok": 1, "error": 0},
                "os_exit": {"returncode": 0}}

    spec = importlib.util.spec_from_file_location(
        "rp_" + os.path.basename(root), os.path.join(root, "control", "run_phases.py"))
    mod = importlib.util.module_from_spec(spec)
    saved_path, saved_argv = os.environ.get("PATH", ""), list(sys.argv)
    os.environ["PATH"] = os.path.join(WORK, "empty-path")
    os.makedirs(os.environ["PATH"], exist_ok=True)
    for name in ("controller", "grade", "harness", "validate_references",
                 "check_receipts", "check_outcome_labels"):
        sys.modules.pop(name, None)   # each scenario must load its own copy
    try:
        spec.loader.exec_module(mod)

        def no_model(*a, **k):
            REAL_MODEL_CALLS["count"] += 1
            raise AssertionError("a real model launch was attempted in a no-model test")
        mod.H.stream = no_model
        mod.LAUNCH_HOOK = hook
        mod.LAUNCH_MARKERS = markers
        code = mod.main([])
        resumes = 0
        while code == 8 and resumes < max_resumes:
            wrote = False
            jd = os.path.join(root, "judgments")
            for f in sorted(os.listdir(jd)) if os.path.isdir(jd) else []:
                if not f.endswith(".REQUIRED.json"):
                    continue
                req = json.load(open(os.path.join(jd, f)))
                arm = req["arm"]
                if arm not in judge:
                    continue
                j = dict(req["required_binding"])
                j["outcome"] = judge[arm]
                j["criterion"] = "named behaviour"
                j["basis"] = "scenario judgment, written after this arm's checks existed"
                j["issued_at_utc"] = time.strftime("%Y-%m-%dT%H:%M:%SZ", time.gmtime())
                json.dump(j, open(os.path.join(jd, arm + ".json"), "w"), indent=1)
                wrote = True
            if not wrote:
                break
            resumes += 1
            code = mod.main(["--resume"])
    finally:
        os.environ["PATH"] = saved_path
        sys.argv = saved_argv
    state = json.load(open(os.path.join(root, "control", "PHASE-STATE.json"))) \
        if os.path.isfile(os.path.join(root, "control", "PHASE-STATE.json")) else {}
    arms = json.load(open(os.path.join(root, "control", "ARMS.json"))) \
        if os.path.isfile(os.path.join(root, "control", "ARMS.json")) else {}
    return {"code": code, "markers": markers, "state": state, "arms": arms}


C_ARM = {"id": "C1", "phase": "C", "assigned_cap_s": 900}
def B(n):
    """Real B case ids, so the runner's own staging path is exercised."""
    return {"id": "B%d" % n, "phase": "B", "case": n, "assigned_cap_s": 900}
A_ARMS = [{"id": "A3a", "phase": "A", "assigned_cap_s": 300},
          {"id": "A5a", "phase": "A", "assigned_cap_s": 600}]
SEM_OK = [{"arm": a, "criterion": "named behaviour", "outcome": "PASS", "basis": "stand-in"}
          for a in ("C1", "B0", "B3", "B5", "B6", "A3a", "A5a")]

# ---------------------------------------------------------------- P1
root = build_root("P1-c-conformance-fail-blocks-B",
                  [C_ARM, B(0), B(3)], SEM_OK, GOOD_FILES,
                  ("Saved docs/devforge/ideas/IDEAS-001.md — SHA-256 %s\n"
                   "Saved docs/devforge/handoffs/HANDOFF-001.md — SHA-256 %s\n"
                   % (dg(LEDGER), dg(HANDOFF))))
# break C's receipt only: replace the handoff digest with a wrong one
r = drive(root, GOOD_FILES,
          "Saved docs/devforge/ideas/IDEAS-001.md — SHA-256 %s\n"
          "Saved docs/devforge/handoffs/HANDOFF-001.md — SHA-256 %s\n" % (dg(LEDGER), "0" * 64),
          GOOD_FILES, GOOD_TERMINAL)
b_markers = [m for m in r["markers"] if m.startswith("B")]
rec("P1-c-receipt-failure-blocks-B",
    "native C completes but its receipt check fails: zero B stand-ins (review probe)",
    0, len(b_markers), {"markers": r["markers"], "phases": r["state"].get("phases")})

# ---------------------------------------------------------------- P2
root = build_root("P2-c-reference-fail-blocks-B", [C_ARM, B(0), B(3)], SEM_OK,
                  dict(GOOD_FILES, **{"docs/devforge/handoffs/HANDOFF-001.md": BAD_SECTION_HANDOFF}),
                  "Saved docs/devforge/ideas/IDEAS-001.md — SHA-256 %s\n"
                  "Saved docs/devforge/handoffs/HANDOFF-001.md — SHA-256 %s\n"
                  % (dg(LEDGER), dg(BAD_SECTION_HANDOFF)))
r = drive(root, dict(GOOD_FILES, **{"docs/devforge/handoffs/HANDOFF-001.md": BAD_SECTION_HANDOFF}),
          "Saved docs/devforge/ideas/IDEAS-001.md — SHA-256 %s\n"
          "Saved docs/devforge/handoffs/HANDOFF-001.md — SHA-256 %s\n"
          % (dg(LEDGER), dg(BAD_SECTION_HANDOFF)), GOOD_FILES, GOOD_TERMINAL)
rec("P2-c-reference-failure-blocks-B",
    "native C completes with a failing reference: zero B stand-ins",
    0, len([m for m in r["markers"] if m.startswith("B")]), {"markers": r["markers"]})

# ---------------------------------------------------------------- P3
root = build_root("P3-missing-c-semantic", [C_ARM, B(0)], [], GOOD_FILES, GOOD_TERMINAL)
r = drive(root, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL)
rec("P3-missing-c-semantic-blocks-B",
    "no recorded C semantic decision: absence cannot default to PASS",
    0, len([m for m in r["markers"] if m.startswith("B")]), {"markers": r["markers"]})

# ---------------------------------------------------------------- P4
root = build_root("P4-valid-c-permits-B", [C_ARM, B(0), B(3)], SEM_OK, GOOD_FILES, GOOD_TERMINAL)
r = drive(root, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL)
rec("P4-valid-c-permits-B", "a complete valid C gate admits B",
    2, len([m for m in r["markers"] if m.startswith("B")]),
    {"markers": r["markers"], "phases": r["state"].get("phases")})
rec("P4-c-precedes-b", "C is launched and graded before any B marker",
    "C1", r["markers"][0] if r["markers"] else None)

# ---------------------------------------------------------------- P5
root = build_root("P5-b-failure-stops-queue", [C_ARM] + [B(i) for i in (0, 3, 5, 6)],
                  SEM_OK, GOOD_FILES, GOOD_TERMINAL,
                  b_files=dict(GOOD_FILES,
                               **{"docs/devforge/handoffs/HANDOFF-001.md": BAD_SECTION_HANDOFF}),
                  b_terminal=GOOD_TERMINAL)
bad_b = dict(GOOD_FILES, **{"docs/devforge/handoffs/HANDOFF-001.md": BAD_SECTION_HANDOFF})
bad_b_term = ("Saved docs/devforge/ideas/IDEAS-001.md — SHA-256 %s\n"
              "Saved docs/devforge/handoffs/HANDOFF-001.md — SHA-256 %s\n"
              % (dg(LEDGER), dg(BAD_SECTION_HANDOFF)))
r = drive(root, GOOD_FILES, GOOD_TERMINAL, bad_b, bad_b_term)
launched_b = [m for m in r["markers"] if m.startswith("B")]
not_run = [k for k, v in r["arms"].items() if v.get("outcome") == "NOT_RUN"]
rec("P5-b-failure-stops-admission",
    "a required B failure stops every not-yet-admitted arm (4 declared, concurrency 4)",
    True, len(launched_b) <= 4 and len(not_run) >= 0,
    {"launched_b": launched_b, "not_run": sorted(not_run)})
rec("P5-a-not-run-after-b-failure", "A does not run after a failed B",
    "STOP", r["state"].get("phases", {}).get("A", {}).get("decision"))

# ---------------------------------------------------------------- P6
root = build_root("P6-no-eager-submission", [C_ARM] + [B(i) for i in (0, 3, 5, 6)],
                  SEM_OK, GOOD_FILES, GOOD_TERMINAL, plan_over={
                      "caps": {"C": 900, "B": 900, "A3a": 300, "A5a": 600,
                               "concurrency": {"C": 1, "B": 1, "A": 2}}})
fz = json.load(open(os.path.join(root, "control", "FREEZE.json")))
fz["concurrency"] = {"C": 1, "B": 1, "A": 2}
json.dump(fz, open(os.path.join(root, "control", "FREEZE.json"), "w"), indent=1)
r = drive(root, GOOD_FILES, GOOD_TERMINAL, bad_b, bad_b_term)
rec("P6-no-eager-queue-escape",
    "with concurrency 1 and the first B failing, exactly one B is ever admitted",
    1, len([m for m in r["markers"] if m.startswith("B")]),
    {"markers": r["markers"],
     "not_run": sorted(k for k, v in r["arms"].items() if v.get("outcome") == "NOT_RUN")})

# ---------------------------------------------------------------- P7 freeze drift
for label, target in (("runner", "control/run_phases.py"), ("grader", "control/grade.py"),
                      ("checker", "checkers/check_receipts.py"),
                      ("plan-criteria", "control/PLAN.json"),
                      ("controller", "control/controller.py")):
    root = build_root("P7-drift-" + label, [C_ARM, B(0)], SEM_OK, GOOD_FILES, GOOD_TERMINAL,
                      mutate_after_freeze=target)
    r = drive(root, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL)
    rec("P7-drift-" + label,
        "%s changed after the freeze: nothing launches" % target, 0, len(r["markers"]),
        {"exit_code": r["code"], "markers": r["markers"]})

# ---------------------------------------------------------------- P8 identity/caps
root = build_root("P8-preexisting-attempt", [C_ARM, B(0)], SEM_OK, GOOD_FILES, GOOD_TERMINAL,
                  preexisting="C1")
sentinel = os.path.join(root, "attempts", "C1", "SENTINEL.txt")
before = hashlib.sha256(open(sentinel, "rb").read()).hexdigest()
r = drive(root, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL)
after = hashlib.sha256(open(sentinel, "rb").read()).hexdigest()
rec("P8-preexisting-attempt-rejected",
    "an existing attempt directory is refused before any launch", 0, len(r["markers"]),
    {"sentinel_unchanged": before == after, "c_outcome": r["arms"].get("C1", {}).get("outcome")})
rec("P8-sentinel-untouched", "the existing attempt's bytes are not modified", True, before == after)

root = build_root("P9-wrong-run-identity", [C_ARM, B(0)], SEM_OK, GOOD_FILES, GOOD_TERMINAL)
fz = json.load(open(os.path.join(root, "control", "FREEZE.json")))
fz["bound_hashes"]["code/control/run_phases.py"] = "0" * 64
json.dump(fz, open(os.path.join(root, "control", "FREEZE.json"), "w"), indent=1)
r = drive(root, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL)
rec("P9-wrong-bound-identity", "a bound hash that does not match blocks the run",
    0, len(r["markers"]), {"exit_code": r["code"]})

root = build_root("P10-budget-exhausted", [C_ARM, B(0)], SEM_OK, GOOD_FILES, GOOD_TERMINAL)
fz = json.load(open(os.path.join(root, "control", "FREEZE.json")))
fz["overall_start_epoch"] = 1
fz["overall_budget_s"] = 10
json.dump(fz, open(os.path.join(root, "control", "FREEZE.json"), "w"), indent=1)
r = drive(root, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL)
rec("P10-exhausted-budget", "an exhausted overall budget yields NOT_RUN, not a launch",
    "NOT_RUN", r["arms"].get("C1", {}).get("outcome"), {"markers": r["markers"]})

root = build_root("P11-incomplete-native-result", [C_ARM, B(0)], SEM_OK, GOOD_FILES, GOOD_TERMINAL)
r = drive(root, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL, incomplete_arms=("C1",))
rec("P11-incomplete-native-result",
    "a stand-in that returns no terminal result is COULD_NOT_RUN and blocks B",
    0, len([m for m in r["markers"] if m.startswith("B")]),
    {"c_outcome": r["arms"].get("C1", {}).get("outcome")})

# =====================================================================
# Revision 7 scenarios: sequential path, post-output judgments, real A
# =====================================================================
SEQ_ARMS = [C_ARM, B(5), B(11),
            {"id": "A3a", "phase": "A", "assigned_cap_s": 300},
            {"id": "A5a", "phase": "A", "assigned_cap_s": 600}]

# ---------------------------------------------------------------- P19
root = build_root("P19-full-sequential-path", SEQ_ARMS, [], GOOD_FILES, GOOD_TERMINAL,
                  GOOD_FILES, GOOD_TERMINAL)
r = drive(root, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL,
          a_invokes=("A3a", "A5a"))
rec("P19-order", "the five arms are admitted in order, one at a time",
    ["C1", "B5", "B11", "A3a", "A5a"], list(r["markers"]),
    {"code": r["code"], "phases": r["state"].get("phases")})
rec("P19-no-duplicate-launch",
    "resuming after each judgment does not relaunch a completed arm",
    5, len(r["markers"]))
rec("P19-resumed-without-relaunch",
    "completed arms are read back from their recorded result on resume",
    True, all(r["arms"].get(a, {}).get("resumed_without_relaunch")
              for a in ("C1", "B5", "B11")))

# ---------------------------------------------------------------- P20
root = build_root("P20-no-judgment-blocks-everything", SEQ_ARMS, [], GOOD_FILES, GOOD_TERMINAL,
                  GOOD_FILES, GOOD_TERMINAL)
r = drive(root, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL, judge={})
rec("P20-missing-judgment-pauses",
    "C1 completes and checks pass, but with no judgment nothing downstream starts",
    ["C1"], list(r["markers"]), {"code": r["code"],
                                 "awaiting": r["state"].get("awaiting_judgment")})
rec("P20-pause-exit-code", "a pending judgment is its own exit code, not a pass", 8, r["code"])

# ---------------------------------------------------------------- P21
root = build_root("P21-stale-judgment-rejected", SEQ_ARMS, [], GOOD_FILES, GOOD_TERMINAL,
                  GOOD_FILES, GOOD_TERMINAL)


def _stale(root_dir):
    """Write C1 a judgment whose bindings name other bytes."""
    jd = os.path.join(root_dir, "judgments")
    os.makedirs(jd, exist_ok=True)
    req = os.path.join(jd, "C1.REQUIRED.json")
    if not os.path.isfile(req):
        return False
    b = dict(json.load(open(req))["required_binding"])
    b["output_inventory_sha256"] = "1" * 64      # an inventory that is not this one
    b.update(outcome="PASS", criterion="named behaviour",
             basis="judgment about a different inventory",
             issued_at_utc="2026-09-07T00:00:00Z")
    json.dump(b, open(os.path.join(jd, "C1.json"), "w"), indent=1)
    return True


r = drive(root, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL, judge={})
_stale(root)
r2 = drive(root, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL, judge={})
rec("P21-stale-judgment-blocks-B",
    "a judgment naming a different inventory digest does not advance the gate",
    0, len([m for m in r2["markers"] if m.startswith(("B", "A"))]),
    {"code": r2["code"]})

# ---------------------------------------------------------------- P22
root = build_root("P22-prewritten-judgment-refused", SEQ_ARMS, [], GOOD_FILES, GOOD_TERMINAL,
                  GOOD_FILES, GOOD_TERMINAL)
json.dump({"schema_version": "devforge.semantic-decisions/v2",
           "decisions": [{"arm": "C1", "outcome": "PASS", "basis": "written in advance"}]},
          open(os.path.join(root, "control", "SEMANTIC-DECISIONS.json"), "w"), indent=1)
spec = importlib.util.spec_from_file_location(
    "fz_p22", os.path.join(root, "control", "freeze.py"))
m = importlib.util.module_from_spec(spec)
spec.loader.exec_module(m)
fz = m.build()
fz["arms"] = SEQ_ARMS
fz["native_arms_authorized"] = len(SEQ_ARMS)
json.dump(fz, open(os.path.join(root, "control", "FREEZE.json"), "w"), indent=1)
r = drive(root, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL)
rec("P22-prewritten-judgment-refused",
    "a freeze carrying a judgment written before its arm ran is refused before any launch",
    (7, 0), (r["code"], len(r["markers"])))

# ---------------------------------------------------------------- P23
root = build_root("P23-zero-arms-authorized", SEQ_ARMS, [], GOOD_FILES, GOOD_TERMINAL,
                  GOOD_FILES, GOOD_TERMINAL)
fz = json.load(open(os.path.join(root, "control", "FREEZE.json")))
fz["native_arms_authorized"] = 0
json.dump(fz, open(os.path.join(root, "control", "FREEZE.json"), "w"), indent=1)
r = drive(root, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL)
rec("P23-zero-call-refusal",
    "the real preparation freeze refuses cleanly before any stand-in or marker",
    (5, 0), (r["code"], len(r["markers"])))

# ---------------------------------------------------------------- P24
root = build_root("P24-concurrency-not-one", SEQ_ARMS, [], GOOD_FILES, GOOD_TERMINAL,
                  GOOD_FILES, GOOD_TERMINAL)
fz = json.load(open(os.path.join(root, "control", "FREEZE.json")))
fz["concurrency"] = {"C": 1, "B": 4, "A": 1}
json.dump(fz, open(os.path.join(root, "control", "FREEZE.json"), "w"), indent=1)
r = drive(root, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL)
rec("P24-refuses-unenforceable-policy",
    "a freeze declaring concurrency it cannot enforce is refused, not quietly run sequentially",
    (6, 0), (r["code"], len(r["markers"])))

# ---------------------------------------------------------------- P25
root = build_root("P25-b-failure-stops-later-b-and-a", SEQ_ARMS, [], GOOD_FILES, GOOD_TERMINAL,
                  GOOD_FILES, GOOD_TERMINAL)
r = drive(root, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL,
          judge={"C1": "PASS", "B5": "FAIL", "B11": "PASS", "A3a": "PASS", "A5a": "PASS"},
          a_invokes=("A3a", "A5a"))
rec("P25-later-arms-not-run",
    "a failed judgment on B5 leaves B11, A3a and A5a NOT_RUN",
    ["NOT_RUN", "NOT_RUN", "NOT_RUN"],
    [r["arms"].get(a, {}).get("outcome") for a in ("B11", "A3a", "A5a")],
    {"markers": r["markers"]})
rec("P25-no-a-markers", "and no A arm is launched", 0,
    len([m for m in r["markers"] if m.startswith("A")]))

# ---------------------------------------------------------------- P26
root = build_root("P26-a-activation-mismatch", SEQ_ARMS, [], GOOD_FILES, GOOD_TERMINAL,
                  GOOD_FILES, GOOD_TERMINAL)
r = drive(root, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL, a_invokes=())
rec("P26-a3a-activation-fails",
    "a should-trigger selector that did not select the skill fails activation",
    "FAIL", (r["arms"].get("A3a", {}).get("activation") or {}).get("outcome"),
    {"markers": r["markers"]})
rec("P26-a5a-not-launched", "and the next A arm is never admitted",
    "NOT_RUN", r["arms"].get("A5a", {}).get("outcome"))

# =====================================================================
# R10 scenarios. Authored, never executed by this allocation.
#
# These import the freshly authored modules from the same scratch run root the
# other scenarios use, so the predicate under test is the operative one. Where a
# scenario needs a real file at a real location, it stages it -- R9-CI-002 was
# precisely a required positive that could not resolve, and a case that fails
# because its input is missing has not tested the thing it is named for.
# =====================================================================

def _load(root, mod):
    """Import one module from a scratch run root's own control directory."""
    spec = importlib.util.spec_from_file_location(
        "%s_%s" % (mod, os.path.basename(root)),
        os.path.join(root, "control", "%s.py" % mod))
    m = importlib.util.module_from_spec(spec)
    sys.path.insert(0, os.path.join(root, "control"))
    sys.path.insert(0, os.path.join(root, "checkers"))
    spec.loader.exec_module(m)
    return m


# --- CHG-005 / CHG-020: freeze emission and verification -------------------
_fr_root = os.path.join(WORK, "r10-freeze")
os.makedirs(_fr_root, exist_ok=True)
for _sub in ("control", "checkers", "no-model-tests", "fixtures"):
    os.makedirs(os.path.join(_fr_root, _sub), exist_ok=True)
for _f in os.listdir(os.path.join(RUN, "control")):
    if _f.endswith((".py", ".json")):
        shutil.copyfile(os.path.join(RUN, "control", _f),
                        os.path.join(_fr_root, "control", _f))
for _f in os.listdir(os.path.join(RUN, "checkers")):
    if _f.endswith(".py"):
        shutil.copyfile(os.path.join(RUN, "checkers", _f),
                        os.path.join(_fr_root, "checkers", _f))
_staged = stage_preflight(_fr_root)
_FZ = _load(_fr_root, "freeze")

_fz = _FZ.build()
rec("R10-P01-preflight-real-location",
    "every declared preflight input resolves at its real intended location",
    [], _fz["unresolved_preflight"], {"change_id": "CHG-005/CHG-020",
                                      "preflight_root": _fz.get("preflight_root")})
rec("R10-P02-freeze-unchanged-positive",
    "an unchanged real-location fixed input set verifies PASS",
    "PASS", _FZ.verify(_fz, run_root=_fr_root)["outcome"], {"change_id": "CHG-005"})

open(_staged["results.json"], "a").write("\n")
rec("R10-P03-consumed-result-mutated",
    "mutating the actually consumed deterministic result fails the freeze",
    "FAIL", _FZ.verify(_fz, run_root=_fr_root)["outcome"], {"change_id": "CHG-005"})

_fz2 = _FZ.build()
open(_staged["checks_suite.py"], "a").write("\n# selected but not consumed by a gate\n")
_v2 = _FZ.verify(_fz2, run_root=_fr_root)
rec("R10-P04-selected-unconsumed-mutated",
    "mutating a selected-but-unconsumed input fails the freeze exactly as a consumed one does",
    "FAIL", _v2["outcome"], {"change_id": "CHG-020",
                             "mismatch_paths": [m["path"] for m in _v2["bound_mismatches"]]})

_fz3 = _FZ.build()
_gen = os.path.join(_fr_root, "grades", "C1-grade.json")
os.makedirs(os.path.dirname(_gen), exist_ok=True)
json.dump({"mechanical": "PASS"}, open(_gen, "w"))
_v3 = _FZ.verify(_fz3, run_root=_fr_root, observed_extra=("grades/C1-grade.json",))
rec("R10-P05-unbound-generated-evidence",
    "creating genuinely unbound generated evidence is not a freeze violation",
    "PASS", _v3["outcome"],
    {"change_id": "CHG-020", "classified": _v3["unbound_changes"]})

# --- CHG-015 / R9-CI-006: the emitter carries the selected allocation ------
_alloc = json.load(open(os.path.join(_fr_root, "control", "ALLOCATION.json")))
rec("R10-P06-authority-from-selection",
    "the freeze emits the selected current allocation, not a constant",
    _alloc["sha256"], _fz3["execution_ref"]["sha256"], {"change_id": "CHG-015"})
rec("R10-P07-no-hardcoded-authority",
    "no session digest is baked into the emitting module",
    False,
    any(h in open(os.path.join(_fr_root, "control", "freeze.py")).read()
        for h in ("e0dda2d0d5722d0b842ef4eae3d844d9e76abfb13b65b5f3cef574956949c10c",
                  "9622527ab2b104dc43ae76b24e1b1009ffcfbb530b5b7dd79c1888f1e73900d3",
                  "0b5d5fece804bcd2221ea48f80ba5f93e04d82f0fa9298f0b07807da8987f587")),
    {"change_id": "CHG-015"})

_AD = _load(_fr_root, "admission")
rec("R10-P08-authority-stale-rejected",
    "a historical allocation offered as the emitted identity is rejected",
    "FAIL",
    _AD.authority_bind(
        {"session": _alloc["session"], "revision": _alloc["revision"],
         "path": _alloc["path"], "sha256": _alloc["sha256"]},
        {"session": "SESSION-001@9", "revision": 9, "path": _alloc["path"],
         "sha256": _alloc["supersedes_sha256"]})["outcome"], {"change_id": "CHG-015"})
rec("R10-P09-authority-missing-rejected",
    "an emitted record with no allocation fields is rejected, not defaulted",
    "FAIL", _AD.authority_bind({"session": _alloc["session"], "revision": _alloc["revision"],
                                "path": _alloc["path"], "sha256": _alloc["sha256"]},
                               {})["outcome"], {"change_id": "CHG-015"})

# --- CHG-009 / R9-CI-003: the five fixed observation classes ---------------
_OBS_GOOD = {n: {"source": "runtime.phase_state",
                 "observed_bytes_sha256": "a" * 64, "outcome": "PASS", "task_id": "T1"}
             for n in _AD.RUNTIME_OBSERVATIONS}
rec("R10-P10-deferred-blocks-even-when-supplied",
    "a dictionary supplied for a deferred class does not satisfy it",
    "FAIL", _AD.observations(_OBS_GOOD, task_identity="T1")["outcome"],
    {"change_id": "CHG-009",
     "deferred": _AD.observations(_OBS_GOOD, task_identity="T1")["deferred"]})
_impl = [n for n in _AD.RUNTIME_OBSERVATIONS if n not in _AD.ROOT_DEPENDENCY_DEFERRED]
rec("R10-P11-worker-text-never-passes",
    "the explicit negative: worker text, bad digest and a FAIL outcome cannot pass",
    ["FAIL"] * len(_impl),
    [r["outcome"] for r in _AD.observations(
        {n: {"source": "worker text", "observed_bytes_sha256": "x", "outcome": "FAIL"}
         for n in _AD.RUNTIME_OBSERVATIONS}, task_identity="T1")["rows"]
     if r["observation"] in _impl], {"change_id": "CHG-009"})
rec("R10-P12-narrowed-coverage-refused",
    "a caller asking for a subset of the five fixed classes fails on the narrowing",
    True,
    any(r.get("state") == "NARROWED" for r in _AD.observations(
        _OBS_GOOD, required=("ordered_checkpoints",), task_identity="T1")["rows"]),
    {"change_id": "CHG-009"})
rec("R10-P13-wrong-task-identity",
    "an observation bound to another task does not count for this one",
    "FAIL", _AD.observations(_OBS_GOOD, task_identity="T2")["outcome"], {"change_id": "CHG-009"})

# --- CHG-008 / R9-CI-008: prerequisites and A3a sequencing -----------------
_done = {"C1": {"mechanical": "PASS", "judgment": "PASS", "judgment_bound_to": "d" * 64},
         "B5": {"mechanical": "PASS", "judgment": "PASS", "judgment_bound_to": "d" * 64},
         "B11": {"mechanical": "PASS", "judgment": "PASS", "judgment_bound_to": "d" * 64}}
rec("R10-P14-b-without-c", "B5 is refused before any marker when C1 has not completed",
    False, _AD.prerequisites("B5", {})["admit"], {"change_id": "CHG-008"})
rec("R10-P15-b-refusal-writes-no-marker", "a refused arm writes no admission marker",
    False, _AD.prerequisites("B5", {})["marker_written"], {"change_id": "CHG-008"})
rec("R10-P16-a-without-b", "A3a is refused when a required B arm is unjudged",
    False, _AD.prerequisites("A3a", {"C1": _done["C1"]})["admit"], {"change_id": "CHG-008"})
rec("R10-P17-c-only-diagnostic", "an authorized C-only diagnostic admits C and nothing downstream",
    [True, False],
    [_AD.prerequisites("C1", {}, diagnostic_scope=True)["admit"],
     _AD.prerequisites("B5", _done, diagnostic_scope=True)["admit"]], {"change_id": "CHG-008"})
_a3a_done = dict(_done, A3a={"native": "PASS",
                             "activation": {"outcome": "PASS", "state": "OBSERVED"}})
rec("R10-P18-a5a-consumes-a3a-activation",
    "a completed, activated A3a satisfies A5a without any semantic judgment field",
    True, _AD.prerequisites("A5a", _a3a_done)["admit"],
    {"change_id": "CHG-008", "finding": "R9-CI-008"})
rec("R10-P19-a5a-refused-on-unusable-a3a",
    "an A3a whose activation was unusable does not satisfy A5a",
    False, _AD.prerequisites("A5a", dict(_done, A3a={
        "native": "PASS", "activation": {"outcome": "COULD_NOT_RUN", "state": "INCOMPLETE"}}
        ))["admit"], {"change_id": "CHG-008"})
rec("R10-P20-empty-caller-list-still-checked",
    "an empty completed map does not admit an arm whose prerequisites are unmet",
    False, _AD.prerequisites("A5a", {})["admit"], {"change_id": "CHG-008"})

# --- CHG-010 / R9-CI-004: transcript envelope ------------------------------
_ACT = _load(_fr_root, "activation")
rec("R10-P21-scalar-line-is-malformed",
    "a decodable scalar or list line is not an event envelope",
    ["malformed", "malformed"],
    [_ACT.envelope([])[0], _ACT.envelope("text")[0]], {"change_id": "CHG-010"})
rec("R10-P22-tool-use-without-assistant-role",
    "a tool_use outside an assistant envelope is not a selection",
    "malformed",
    _ACT.envelope({"content": [{"type": "tool_use", "name": "Skill",
                                "input": {"skill": "devforge-brainstorm"}}]})[0],
    {"change_id": "CHG-010"})
rec("R10-P23-init-metadata-is-not-invocation",
    "session metadata listing the installed skill is not an assistant selection",
    "other",
    _ACT.envelope({"type": "system", "subtype": "init",
                   "skills": ["devforge-brainstorm"]})[0], {"change_id": "CHG-010"})
rec("R10-P24-legitimate-assistant-tool-use",
    "a nested assistant Skill tool_use naming this skill is the selection event",
    ("assistant", True),
    (lambda k, b: (k, any(_ACT._names_skill(x, "devforge-brainstorm") for x in b)))(
        *_ACT.envelope({"type": "assistant", "message": {"role": "assistant", "content": [
            {"type": "tool_use", "id": "t1", "name": "Skill",
             "input": {"skill": "devforge-brainstorm"}}]}})), {"change_id": "CHG-010"})

# --- CHG-012 / R9-CI-005: query identity and launch prompt -----------------
_AA = _load(_fr_root, "a_arm")
_sel = {"id": "q-1", "query": "help me think through a tool library problem",
        "should_trigger": True}
_ok = _AA.run_a_arm("A3a", _sel, 10000, 300, _fr_root, launcher=None, prompt=_sel["query"])
rec("R10-P25-query-identity-derived",
    "the query digest is derived from the selected row's own bytes, not copied",
    dg(_sel["query"]), _ok.get("selected_query_sha256"), {"change_id": "CHG-012"})
rec("R10-P26-unchanged-prompt-is-computed",
    "prompt_bytes_unchanged is a comparison result, not an assertion",
    True, _ok.get("prompt_bytes_unchanged"), {"change_id": "CHG-012"})
_mut = _AA.run_a_arm("A3a", _sel, 10000, 300, _fr_root, launcher=None,
                     prompt=_sel["query"] + " and use the skill")
rec("R10-P27-launch-time-mutation-refused",
    "a prompt mutated at launch time is refused rather than recorded as unchanged",
    ["FAIL", False], [_mut.get("outcome"), _mut.get("prompt_bytes_unchanged")],
    {"change_id": "CHG-012"})
rec("R10-P28-no-launcher-is-not_run",
    "a missing launcher returns an honest NOT_RUN, not a negative activation",
    "NOT_RUN", _ok.get("outcome"), {"change_id": "CHG-012"})
rec("R10-P29-a5a-not-invoked-by-a3a-scenario",
    "an A3a scenario leaves A5a uninvoked and writes no marker for it",
    False, _ok.get("marker_written"), {"change_id": "CHG-012"})

# --- CHG-014: nonlaunch preserves bytes ------------------------------------
_dest = os.path.join(_fr_root, "attempts", "A5a")
os.makedirs(_dest, exist_ok=True)
open(os.path.join(_dest, "SENTINEL.txt"), "w").write("another attempt's evidence\n")
_pre = _AA.run_a_arm("A5a", _sel, 10000, 600, _fr_root, launcher=None, prompt=_sel["query"])
rec("R10-P30-preexisting-destination-refused",
    "an existing attempt destination is a recorded nonlaunch, not an arm result",
    ["REJECTED_PREEXISTING_DESTINATION", False, "CLEAN"],
    [_pre.get("outcome"), _pre.get("marker_written"), _pre.get("control_completion")],
    {"change_id": "CHG-014"})
rec("R10-P31-sentinel-bytes-preserved",
    "the refusal left the existing bytes byte-identical, checked by digest not size",
    "PASS", (_pre.get("sentinel_preservation") or {}).get("outcome"), {"change_id": "CHG-014"})
rec("R10-P32-nonlaunch-is-not-an-arm-result",
    "a nonlaunch cannot satisfy a downstream prerequisite",
    False, _AA.nonlaunch_is_not_a_result(_pre)["counts_as_arm_result"], {"change_id": "CHG-014"})
_cap = _AA.run_a_arm("A3a", _sel, 100, 600, _fr_root, launcher=None, prompt=_sel["query"])
rec("R10-P33-insufficient-cap-refused",
    "insufficient remaining budget refuses without a marker or a partial attempt directory",
    ["REJECTED_INSUFFICIENT_CAP", False],
    [_cap.get("outcome"), _cap.get("marker_written")], {"change_id": "CHG-014"})

# --- CHG-007: preservation decided from live bytes -------------------------
_pr = os.path.join(WORK, "r10-preserve")
os.makedirs(_pr, exist_ok=True)
_arch = os.path.join(_pr, "docs/devforge/ideas/archive")
os.makedirs(_arch, exist_ok=True)
_orig = "archived revision one\n"
open(os.path.join(_arch, "IDEAS-001.r1.md"), "w").write(_orig)
_sel_map = {"docs/devforge/ideas/archive/IDEAS-001.r1.md": dg(_orig)}
rec("R10-P34-preserved-retained", "an unchanged retained archive passes",
    "PASS", _AD.preserve_check(_sel_map, _pr)["outcome"], {"change_id": "CHG-007"})
open(os.path.join(_arch, "IDEAS-001.r1.md"), "a").write("changed\n")
rec("R10-P35-preserved-changed", "changed bytes fail even though the inventory key survives",
    "FAIL", _AD.preserve_check(_sel_map, _pr)["outcome"], {"change_id": "CHG-007"})
os.remove(os.path.join(_arch, "IDEAS-001.r1.md"))
rec("R10-P36-preserved-vanished", "disappearance during the judgment pause fails",
    "FAIL", _AD.preserve_check(_sel_map, _pr)["outcome"], {"change_id": "CHG-007"})

# --- CHG-013 / R9-CI-007: the corrected cases are actually driven ----------
rec("R10-P37-missing-judgment-is-empty-object",
    "the missing-judgment case supplies judge={} so the gate reads a genuinely absent judgment",
    {}, [c for c in CR9.CASES if c["id"] == "P-missing-judgment"][0]["setup"]["judge"],
    {"change_id": "CHG-013"})
rec("R10-P38-concurrency-every-non-1",
    "every non-1 concurrency value expects code 6 and zero markers",
    [(6, 0)] * 3,
    [(c["expect"]["exit_code"], c["expect"]["markers"])
     for c in CR9.CASES if c["id"] == "P-concurrency-non-1"
     for _ in c["setup"]["concurrency_values"]], {"change_id": "CHG-013"})
rec("R10-P39-local-resume-scenarios-authored",
    "CHG-011's local resume lifecycle is authored rather than attributed to the native gap",
    True, len(getattr(CR9, "LOCAL_RESUME_CASES", [])) >= 4,
    {"change_id": "CHG-011", "native_still_deferred": CR9.LIFECYCLE_DEFERRED[
        "native_scope_deferred"]})

# =====================================================================
# CHG-011: the local pause/resume lifecycle, actually driven.
#
# R9 recorded this whole change as deferred to a missing native collector. The
# local half is not deferred: `main(--resume)` is an ordinary lifecycle, and
# `drive()` already exercises it -- it runs `main([])`, writes each judgment from
# the binding the runner itself published after that arm's checks existed, and
# re-enters through `main(["--resume"])` on a freshly loaded module. What was
# missing was scenarios that assert the lifecycle properties rather than case
# dictionaries describing them. These are those scenarios.
#
# Every one of them is authored and unexecuted, like everything else in this run.
# =====================================================================

FULL_ARMS = [C_ARM, B(5), B(11)] + A_ARMS


def _freeze_of(root):
    return json.load(open(os.path.join(root, "control", "FREEZE.json")))


def _marker_counts(markers):
    out = {}
    for m in markers:
        out[m] = out.get(m, 0) + 1
    return out


# ---------------------------------------------------------------- R10-P40
_r11 = build_root("R10-resume-ordered", FULL_ARMS, SEM_OK, GOOD_FILES, GOOD_TERMINAL)
_fz_before = _freeze_of(_r11)
_res = drive(_r11, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL,
             a_invokes=("A3a",))
rec("R10-P40-resume-arm-order",
    "C1, then B5, then B11, then the A arms, across the judgment pauses",
    ["C1", "B5", "B11", "A3a", "A5a"], list(_res["markers"]),
    {"change_id": "CHG-011", "code": _res["code"]})

# ---------------------------------------------------------------- R10-P41
rec("R10-P41-one-marker-per-arm",
    "no completed arm is relaunched: exactly one admission marker each",
    {a: 1 for a in ("C1", "B5", "B11", "A3a", "A5a")},
    _marker_counts(_res["markers"]), {"change_id": "CHG-011"})

# ---------------------------------------------------------------- R10-P42
_fz_after = _freeze_of(_r11)
rec("R10-P42-same-allocation-across-resumes",
    "each resume continues the one allocation; no new start, budget or identity",
    [_fz_before["overall_start_epoch"], _fz_before["overall_budget_s"],
     _fz_before["session"], _fz_before["run_id"]],
    [_fz_after["overall_start_epoch"], _fz_after["overall_budget_s"],
     _fz_after["session"], _fz_after["run_id"]],
    {"change_id": "CHG-011",
     "why": "a resume that re-derived its own start epoch would have granted itself an "
            "extension, and the pending judgment's digests would no longer be the consumed ones"})

# ---------------------------------------------------------------- R10-P43
_res_resumed = [a for a, r in _res["arms"].items() if r.get("resumed_without_relaunch")]
rec("R10-P43-completed-arms-read-not-relaunched",
    "every arm admitted before a pause is read back from its recorded result",
    True, set(_res_resumed) >= {"C1", "B5"},
    {"change_id": "CHG-011", "resumed_without_relaunch": sorted(_res_resumed)})

# ---------------------------------------------------------------- R10-P44
_jd = os.path.join(_r11, "judgments")
_bound = {}
for _a in ("C1", "B5", "B11"):
    _jp = os.path.join(_jd, "%s.json" % _a)
    if os.path.isfile(_jp):
        _j = json.load(open(_jp))
        _gp = os.path.join(_r11, "grades", "%s-grade.json" % _a)
        _inv = hashlib.sha256(json.dumps(json.load(open(_gp)).get("inventory"),
                                         sort_keys=True).encode()).hexdigest() \
            if os.path.isfile(_gp) else None
        _bound[_a] = (_j.get("arm"), _j.get("output_inventory_sha256") == _inv)
rec("R10-P44-consumed-judgment-binds-actual-evidence",
    "each consumed judgment names its own arm and that arm's actual inventory digest",
    {a: (a, True) for a in ("C1", "B5", "B11")}, _bound, {"change_id": "CHG-011"})

# ---------------------------------------------------------------- R10-P45
_r11b = build_root("R10-resume-missing-judgment", FULL_ARMS, SEM_OK, GOOD_FILES, GOOD_TERMINAL)
_res_b = drive(_r11b, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL,
               judge={"C1": "PASS"}, a_invokes=("A3a",))
rec("R10-P45-pause-holds-without-next-judgment",
    "with C1 judged and B5 not, the run stays paused and admits nothing past B5",
    [8, []], [_res_b["code"], [m for m in _res_b["markers"] if m in ("B11", "A3a", "A5a")]],
    {"change_id": "CHG-011", "awaiting": _res_b["state"].get("awaiting_judgment")})

# =====================================================================
# CHG-013: the corrected cases, driven rather than described.
#
# R9-CI-007 was that these were case data nothing consumed, so the corrected
# expectations could not reach the mechanisms they describe. Each case below is
# read from `cases_r9.CASES` -- the same definitions, not a second copy -- and
# turned into an actual scenario whose outcome is compared against the case's
# own `expect` block.
# =====================================================================

_CASE = {c["id"]: c for c in CR9.CASES}

# ---------------------------------------------------------------- R10-P46
_c = _CASE["P-missing-judgment"]
_r13 = build_root("R10-case-missing-judgment", [C_ARM, B(5)], SEM_OK, GOOD_FILES, GOOD_TERMINAL)
_res13 = drive(_r13, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL,
               judge=_c["setup"]["judge"])
rec("R10-P46-missing-judgment-stops-and-admits-nothing",
    "judge={} leaves the judgment genuinely absent, so nothing downstream is admitted",
    [_c["expect"]["markers"], "STOP"],
    [len([m for m in _res13["markers"] if m.startswith("B")]),
     "STOP" if _res13["code"] != 0 else "PROCEED"],
    {"change_id": "CHG-013", "criterion": _c["criterion"], "code": _res13["code"]})

# ---------------------------------------------------------------- R10-P47
_c = _CASE["P-b-prefix-after-failure"]
_r13b = build_root("R10-case-b-prefix", [C_ARM, B(5), B(11)], SEM_OK, GOOD_FILES, GOOD_TERMINAL)
_res13b = drive(_r13b, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL,
                judge={"C1": "PASS", "B5": "FAIL"})
rec("R10-P47-permitted-prefix-after-b-failure",
    "after B5's judgment fails, the admitted prefix is exactly C1 and B5",
    _c["expect"]["markers_for"], list(_res13b["markers"]),
    {"change_id": "CHG-013", "criterion": _c["criterion"],
     "why": "a longer prefix would accept an arm admitted after a STOP; a shorter one would "
            "erase evidence legitimately produced before it"})

# ---------------------------------------------------------------- R10-P48
_c = _CASE["P-concurrency-non-1"]
for _v in _c["setup"]["concurrency_values"]:
    _rc = build_root("R10-case-conc-%s" % _v, [C_ARM, B(5)], SEM_OK, GOOD_FILES, GOOD_TERMINAL,
                     plan_over={"caps": {"C": 900, "B": 900, "A3a": 300, "A5a": 600,
                                         "overall_budget_s": 5400,
                                         "concurrency": {"C": _v, "B": _v, "A": _v}}})
    _fz = _freeze_of(_rc)
    _fz["concurrency"] = {"C": _v, "B": _v, "A": _v}
    json.dump(_fz, open(os.path.join(_rc, "control", "FREEZE.json"), "w"), indent=1)
    _rr = drive(_rc, GOOD_FILES, GOOD_TERMINAL, GOOD_FILES, GOOD_TERMINAL)
    rec("R10-P48-concurrency-%s-refused" % _v,
        "concurrency %s is refused with code 6 and no marker" % _v,
        [_c["expect"]["exit_code"], _c["expect"]["markers"]],
        [_rr["code"], len(_rr["markers"])],
        {"change_id": "CHG-013", "criterion": _c["criterion"]})

# ---------------------------------------------------------------- R10-P49
# P21: resume with a mutated inventory binding must record STALE, and must
# record it because the comparison ran -- not because something failed earlier.
_c = _CASE["P21-resume-stale-inventory"]
_r13c = build_root("R10-case-p21-stale", [C_ARM, B(5)], SEM_OK, GOOD_FILES, GOOD_TERMINAL)


def _drive_to_pause_then_mutate(root, arm="C1"):
    """Run to the pause, write a judgment whose inventory digest is wrong, resume.

    The r8 case resumed at a point the comparison never reached, so it could
    report its expected value without testing anything. This reaches it: the
    judgment carries every required field and the correct arm, and differs from
    the published binding in exactly the inventory digest, which is the field
    whose mismatch `_judgment` must classify as STALE.
    """
    markers = []

    def hook(arm_id, cmd, d, cap):
        proj = os.path.join(d, "project")
        for rel, text in GOOD_FILES.items():
            t = os.path.join(proj, rel)
            os.makedirs(os.path.dirname(t), exist_ok=True)
            open(t, "w").write(text)
        return {"terminal": "COMPLETED", "terminal_detail": "stand-in",
                "result_text": GOOD_TERMINAL, "seconds": 0.1,
                "tool_results": {"ok": 1, "error": 0}, "os_exit": {"returncode": 0}}

    spec = importlib.util.spec_from_file_location(
        "rp_stale_" + os.path.basename(root), os.path.join(root, "control", "run_phases.py"))
    mod = importlib.util.module_from_spec(spec)
    saved = os.environ.get("PATH", "")
    os.environ["PATH"] = os.path.join(WORK, "empty-path")
    os.makedirs(os.environ["PATH"], exist_ok=True)
    for name in ("controller", "grade", "harness", "validate_references",
                 "check_receipts", "check_outcome_labels", "admission", "activation", "a_arm"):
        sys.modules.pop(name, None)
    try:
        spec.loader.exec_module(mod)

        def no_model(*a, **k):
            REAL_MODEL_CALLS["count"] += 1
            raise AssertionError("a real model launch was attempted in a no-model test")
        mod.H.stream = no_model
        mod.LAUNCH_HOOK = hook
        mod.LAUNCH_MARKERS = markers
        code = mod.main([])
        req_path = os.path.join(root, "judgments", "%s.REQUIRED.json" % arm)
        if not os.path.isfile(req_path):
            return {"code": code, "markers": markers, "recorded": "NO_PAUSE_REQUEST",
                    "reason": "the runner never published a required binding for %s" % arm}
        req = json.load(open(req_path))
        j = dict(req["required_binding"])
        j["output_inventory_sha256"] = "f" * 64      # the single mutated field
        j["outcome"] = "PASS"
        j["criterion"] = "named behaviour"
        j["basis"] = "scenario judgment with a deliberately mutated inventory binding"
        json.dump(j, open(os.path.join(root, "judgments", "%s.json" % arm), "w"), indent=1)
        code = mod.main(["--resume"])
    finally:
        os.environ["PATH"] = saved
    gate = os.path.join(root, "decisions", "gate-C:%s.json" % arm)
    recorded = None
    if os.path.isfile(gate):
        sem = json.load(open(gate)).get("semantic") or [{}]
        recorded = sem[0].get("recorded")
    return {"code": code, "markers": markers, "recorded": recorded}


_p21 = _drive_to_pause_then_mutate(_r13c)
rec("R10-P49-p21-stale-inventory-binding",
    "a judgment differing only in its inventory digest is recorded STALE, not accepted",
    _c["expect"]["recorded"], _p21["recorded"],
    {"change_id": "CHG-013", "criterion": _c["criterion"],
     "markers": _p21["markers"],
     "why": "the mismatch is in one field and every other required field is correct, so the "
            "comparison is what produces the result rather than an earlier malformed-judgment "
            "branch"})

out = {"schema_version": "devforge.no-model-phases/v1",
       "real_model_invocations": REAL_MODEL_CALLS["count"],
       "method": "Each scenario copies the real control/checker bytes, generates the real freeze "
                 "over them, imports the real run_phases from that copy and calls its real main(). "
                 "Only the model-call seam is substituted; harness.stream is replaced by a raiser "
                 "and PATH is emptied.",
       "runner_hash": hashlib.sha256(
           open(os.path.join(RUN, "control", "run_phases.py"), "rb").read()).hexdigest(),
       "cases": results,
       "summary": {"total": len(results),
                   "passed": sum(1 for r in results if r["result"] == "PASS"),
                   "failed": sum(1 for r in results if r["result"] == "FAIL")}}
out["conformance"] = ("PASS" if out["summary"]["failed"] == 0
                      and REAL_MODEL_CALLS["count"] == 0 else "FAIL")
json.dump(out, open(os.path.join(HERE, "phase-results.json"), "w"), indent=2)
for r in results:
    print("%-34s %-5s expected=%-8s got=%-8s %s" % (r["id"], r["result"], r["expected"],
                                                    r["actual"], r["description"][:44]))
print("\n%d/%d passed; real model invocations: %d"
      % (out["summary"]["passed"], out["summary"]["total"], REAL_MODEL_CALLS["count"]))
sys.exit(0 if out["conformance"] == "PASS" else 1)
