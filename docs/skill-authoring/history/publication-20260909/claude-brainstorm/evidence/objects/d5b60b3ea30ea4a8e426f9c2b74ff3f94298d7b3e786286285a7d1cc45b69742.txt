# Revision-8 remediation specification and bridge selection

This is the working revision of the existing design for a bounded repair. Preserve the complete prior design in SKILL-DESIGN-SPECIFICATION-r7.md and managed interface in AUTHORING-RUNTIME-BRIDGE-r7.md. Their historical @7 allocation/status prose is historical. This addendum, REPAIR-MAP.json and each issued @8 authority govern this pass. No new workflow, phase, child task or Optional/Enforced classification is introduced: W1 and P1-P4 keep the user's settled classifications. Existing skill reuse/enhancement, activation, provider ownership, installed mapping and search limits remain selected.

## Exact scope changes from revision 7

Codex: preserve the entire reviewed 30-file candidate byte-for-byte, including derivation/execution provenance from @7. Author only a new run-side measurement-plan.proposed.json and grade-rubric.proposed.json plus records/copies. Do not patch executable controls. Address F-001/CHG-001 by mandatory explicit causal-provenance judgment and discriminating future cases. Review found no material Codex source defect; missing native evidence alone is not a reason to edit source. A preparation receipt uses @8 while unchanged source keeps its true @7 provenance.

Claude: source changes are limited to SKILL.md, references/recording-rules.md, references/derivation.json and references/managed-runtime.md. The managed reference already exists; do not require absence or add another path. Preserve all 16 other candidate files and the exact discovery description. Address F-002-F-004. Copy the selected entire prior run into your one new run as immutable baseline input before making fresh run-side control/checker/suite proposals for F-005-F-016. No copy of an old allocation or judgment is current authority. Retain all original finding IDs and original severity labels alongside builder aliases.

Source repair is the first Claude priority. Then repair receipt/reference/preservation and admission predicates, then their discriminating suite definitions and remaining controls. Work remains a single bounded pass: if the rest cannot be authored inside the technical window, freeze a precise partial and mark each remaining CHG deferred. Do not extend the clock to finish this list. Do not claim CP5/CC7 closure from inspection or JSON/AST parsing.

## Selected contract and utility guidance

Use the six provider-specific shared documents and frozen v1 runtime files copied in this packet. The same v1 managed bridge stays selected. The later reference-coverage v2 implementation and grammar are not selected or issued. Do not retrofit them. Root-owned missing metadata/native admission, effective hook provenance, full runtime validation and installed observation remain integration dependencies.

The current captured skill-builder and skill-validator packages are now available. Apply the builder's bounded-remediation intake and change receipt; retain the exact @7 selected shared contract/template revisions if packaged guidance points to a newer/different source. Do not silently change shared authority. The validator package is a read-only review resource, not execution permission. This repair maps alternative-format reports into the working specification as the builder guide permits. It does not invent SEVAL/SENH completion or CLI acceptance. No new hook event research or executable hook authoring is required; H1 remains Design only with prior feasibility Partial.

Codex applies captured Codex creator and builder authoring guidance. Claude actually invokes its selected native Anthropic skill-creator in the retained author session, using the selected package identity and no-execution fence; a read/copy alone is not invocation. If unavailable, record COULD_NOT_RUN for that action without launching a replacement client/provider or using the Codex native protocol. The builder is authoring guidance, not an instruction to install/invoke a Codex-only utility in Claude. The selected native creator invocation is an in-session authoring action, not a new native evaluation worker.

## Measurement semantics retained

Fixed criteria are selected before measured output; semantic judgments are separate later records bound to actual inspected output/checkpoint/receipt/transcript/check bytes and reviewer identity, selected before dependent admission. A hash or timestamp alone cannot prove review chronology. Future controllers must reject missing/stale observations; authoring cannot synthesize an observation or launch a model. A missing native event yields the appropriate nonpass/unavailable status, never an inferred positive or verified negative.

Runtime measurement requires actual phase/challenge ordering, Stop and applicable correction/wait/resume observations, receipt final checks/publication/readback/current verification and rendering, exact installed SKILL/reference/template consultation, native completion, EOF and owned process cleanup as distinct observations. Static hashes/resource advertisement/worker final hashes do not satisfy them. Author a fail-closed interface or mark the unsupported integration deferred; no requirement to fake working runtime wiring inside this allocation.

Codex keeps C-before-B6-before-both-B6E semantics and separately allocated A. Claude keeps fixed C/B prerequisites before prospective A, A3a positive and A5a negative unchanged query bytes, concurrency 1, native/nonlaunch outcomes and old failures. C-only diagnostic scope cannot admit B/A without prerequisites. Do not run any of these cases. Unsectioned reference exceptions require a corresponding absence disclosure; retained outputs require exact original live bytes and cannot be excluded using a stale saved inventory.

## Handback and future review

Every CHG receives applied/deferred/declined, evidence, old/new path/hash identities, preserved requirement IDs and reason in a builder change receipt. Applied means authored, not validated. Keep one exclusive new run, complete baseline/final candidate copies, complete proposal inventory, actual CLOCK, AUTHORING-RECORD.md, named report, candidate manifest and separate receipt. Report all scopes independently: validation NOT_PERFORMED; hooks DESIGN_ONLY; native C/B/A NOT_RUN; root admission and human acceptance NOT_RUN. Preserve old C COULD_NOT_RUN, all historical NOT_RUN arms, prior Claude budget FAILs and late write. Read back complete final bytes/report and write the external receipt without a self digest. Return absolute paths/full SHA-256 values and stop for independent root review.
