# R10 bounded Claude source and executable-control authoring specification — DRAFT

This is a preparation packet for independent root review, then a separately recorded SESSION-001@9 to @10 issuance. It grants no work by its existence. Keep all original finding identities, severities, requirements and historical outcomes. The R9 reviews provide overlapping observations about existing CHGs; their record counts are not a count of independent unique defects. REPAIR-MAP.json preserves original rows, current per-subpart states and all R9 findings without treating any as closed.

## Selected scope and frozen inputs

Retain the complete SKILL-DESIGN-SPECIFICATION-r7.md and AUTHORING-RUNTIME-BRIDGE-r7.md semantics, six selected Claude shared contract/specification/template files, and the selected managed v1 runtime. W1/P1-P4 and the prior explicit authoring decisions remain settled. No interview restart, new feature, new phase, hook implementation or v2 grammar is authorized. Frozen builder/creator/validator guidance is subordinate to these selected revisions and the issued allocation; mutable utility work elsewhere is not selected.

Use one fresh run in the retained assigned Claude author session. The only current package source path that may change is references/derivation.json. The other nineteen files, including all operative instructions, description, assets, scripts and evals, must remain byte-exact. Before writing, bind the twenty frozen current candidate files in selected/claude/candidate/devforge-brainstorm to live source under the assigned worktree; drift or collision stops affected work without overwrite or rollback. Preserve prior CHG-002/003/004/017/018 source behavior and the owner-selected handoff-only exception.

The only additional write scope is the single newly created run. Preserve every historical run, baseline, report, authority and reviewer record. Source authoring is actual executable code preparation, but no target/helper/suite/fixture import or execution, compilation, validator/linter, install/export, model worker/grader, native evaluation or hook activation is permitted. Standard-library custody and source/JSON text inspection are allowed. Copying executable source does not authorize running it.

## First priority: current derivation metadata and additive custody correction

R9-CLAUDE-STATIC-001 (MAJOR, R07/R09) identifies three current destination bindings. Refresh the current entries using final raw bytes, reconcile the associated after-last-write verification claims, and preserve former values as explicitly historical. The selected unchanged values are:

| Destination | Current SHA-256 |
| --- | --- |
| SKILL.md | a5316781cafbe0a68cf0a434a27bbd35ba8761de6205a10caebabed6f16eff8c |
| references/recording-rules.md | 5b4f47c018d8de7c8976fff62619107e6b25a0fc8fb88b1611e7d044758aa6a3 |
| references/managed-runtime.md | 6949fc670a5c5e1888eb8dc79532b5187c334178f6d84fae7a855f0b0663ef30 |

The selected pre-edit references/derivation.json digest is 8f4ad26e13fab252d56cc078463f89b911f089fb7d767642d359a8cb3accc31b. Do not put the derivation file's own complete-byte digest inside itself. New external candidate custody will bind its resulting digest. Metadata correction changes no behavioral/native result.

R9-CUSTODY-001 (MAJOR) requires a new additive correction in the new run; never rewrite the R9 report, CLOCK, handoff or receipts. Explicitly acknowledge the impossible reported chronology: the report claims a 17:53:30Z technical stop; its receipt was issued at 16:29:04.452179Z; the handoff asserts 18:06Z creation; root already observed complete bytes at 16:39:42Z, before the relayed 18:12Z closure. These cannot all be accepted as actual events. Retract unobserved future-event claims, distinguish scheduled cutoffs from actual observations, cite evidence for known times, and leave unobserved times unknown. Do not infer a new overrun, invent replacement timestamps, audit an entire private history, reset any clock or replace prior results. Also acknowledge that the prior clock note allowed assignment reading before first-work recording contrary to the issued rule.

R9-CUSTODY-002 (MINOR) requires an additive coverage correction. The historical manifest has 78 entries: 77 valid nonself entries, including the report, and one invalid self entry; it therefore does not verify as its complete listed set. The historical run has 80 files, with report/READBACK.json and report/RECEIPT.json outside the manifest. The external receipt correctly binds report/manifest/readback; all 73 receipt/readback targets and 40 builder before/after targets match. Preserve those distinct facts. Do not repeat the incorrect statement that the old manifest excludes the report. Preserve the old manifest with its defect; the new manifest/receipt scheme below prevents recurrence.

## Full effective source and actual integration

EFFECTIVE-SOURCE-SELECTION.json materializes the complete fourteen-file r7 control/checker/suite baseline plus the selected R9 overlay. The selected PLAN is 132 lines with SHA-256 002099d3872587b6fee487528bea30c31bde85fa953d0a5f6dd37efdec80c56e. The 78-line earlier PLAN is historical only. Preserve selected/claude/control-baseline-r7 and historical/claude-r9-run unchanged; copy selected/claude/effective-controls into the single new run for edits. Its twenty-five files are selected starting bytes, not an already integrated repair.

Author the actual controller.py, run_phases.py, grade.py, freeze.py, admission.py, activation.py, a_arm.py, harness/checker calls, checks_suite.py, phase_suite.py and any additional explicitly inventoried modules together. R9-CI-001 (high) requires actual calls before decisions/markers, adapters from real consumed records, and fresh suite imports that resolve to the newly authored checker/control tree. A helper dictionary, unused module, INDEX.json or setup/expect list is not a repaired caller or executable scenario. Do not execute any of these files to establish this connection; author and inspect it statically.

Make run root, exact final candidate root, selected fixture roots, current allocation and preflight roots explicit required bindings. Do not silently read a mutable live source or an older historical absolute DELEG path when its bytes are absent from the selected fixed set. All effective code, entrypoints, actual suites, cases, fixture bytes, criteria and selected unconsumed files must enter the fresh fixed-input set before future consumption. Historical CLOCK/SEMANTIC-DECISIONS/GATE-INPUTS values are input history, never fresh allocation, runtime observations or judgments. Unavailable external selected fixture/observation bindings stay explicit and block dependent future operations.

## Required code and discriminating cases

All cases below are requested executable scenario authoring and remain NOT_RUN. Each must stage valid prerequisite target/config bytes and assert the intended reason/classification, not merely accept a failure caused by missing target/hash/schema. Preserve preexisting valid cases as well as new negatives; do not alter trigger query text or expected activation to get a result.

1. **CHG-005/020, CC7-001 high, CC8-PROP-002 medium; R9-CI-001/002 high.** Correct both freeze emission and actual controller verification. Map preflight keys to real RUN/no-model-tests/... files, matching GATE-INPUTS' consumed no-model-tests/results.json. Require an unchanged real-location positive; mutation of the consumed result negative; mutation of a selected but unconsumed input negative; and a truly unbound newly generated evidence mutation that does not alone invalidate fixed criteria. Missing bound input must fail closed, never None == None. Bind admission.py, activation.py, a_arm.py, cases and every additional effective module/suite, including formerly omitted files. Later generated grades/judgments have their separate evidence binding boundary.

2. **CHG-006/021, CC7-002 high, CC8-PROP-003 medium, CP5-CK-003 retained; CC9-RR-002/003 high.** Preserve validated occurrence spans and whole exact 64-hex token guards. Validate the complete absolute locator token, not an accepted substring. With selected sandbox /work/arm and actual H_a, /work/arm/a.md followed by H_a passes; /other//work/arm/a.md followed by H_a fails. Retain exact relative/sandbox-root positives; sha256sum-order, table and adjacent-line positives; bare valid locator without digest plus separate wrong-root digest decoy negative; simple wrong-root negative; 65-hex negative; and both long swapped-pair negatives. Stage actual frozen target bytes and promised/mapping manifests. Fresh checks_suite imports the revised checkers and consumes all fixtures. The old simple wrong-root-only case already failed baseline; preserve that corrected history.

3. **CHG-007, CC7-003 high; R9-CI-001 high.** Build the selected preservation map and call live existence/digest checks from actual grading/admission, including across the judgment pause. An unchanged retained archive passes; changed bytes fail; disappearance during the pause fails. Recorded inventory or size equality alone is insufficient. Selected exclusion identity must remain exact.

4. **CHG-008, CC7-004 high; R9-CI-001 high / R9-CI-008 medium.** Require the fixed checked C1/B5/B11 prerequisite set before relevant downstream markers, even when caller arm lists omit members or are empty; retain an explicit C-only diagnostic branch that cannot admit A. C/B requirements bind actual consumed mechanical checks and post-output semantic decisions, reviewer identity and exact output/checkpoint/receipt/transcript/check bytes. A5a sequencing uses A3a's selected native-completion/activation result; a valid A3a activation does not require invented C/B-style semantic PASS. Author missing-C, missing-B, shortened/empty caller-list, unrelated/stale judgment-binding negatives and a completed bound A3a sequencing positive without semantic-judgment fields; unsupported native observations still block actual admission.

5. **CHG-009, CC7-005 high; R9-CI-001/003 high.** All five independently required observation classes remain fixed: ordered checkpoints; actual Stop/correction; runtime receipt publication/readback/current verification; installed resource consultation; rendered delivery. A caller cannot narrow or replace that required set. Reject missing rows, unknown/subset coverage, bad digest syntax, unmatched actual bytes, wrong provenance/task identity, unsuccessful outcomes and any dictionary that attempts to bypass a deferred dependency. Explicit negative: {source: "worker text", observed_bytes_sha256: "x", outcome: "FAIL"} can never yield PASS. Consume only precisely selected evidence-bound formats that the frozen v1 actually exposes. If trustworthy selected evidence cannot be consumed, record unavailable/ROOT_DEPENDENCY_DEFERRED and refuse dependent native admission. No invented collector, API or synthetic PASS. Exposed protected correction/accepted/resume and verified receipt mechanical results exist in selected phase_state/delivery_core/hook_protocol and must be distinguished from unavailable native callback provenance, consultation and rendering collection. Neither worker hash prose nor socket-write completion proves independent publication/readback/current verification or rendered delivery.

6. **CHG-010, CC7-006 high; R9-CI-001/004 high.** Integrate activation parsing into the actual A caller. Decode and validate the selected transcript event envelope, not merely JSON syntax. Ordinary init metadata does not establish invocation; a legitimate nested assistant Skill tool_use selecting the skill does. JSON [] or scalar/object shapes without usable events, malformed/incomplete envelopes, and tool-use objects without assistant role/event identity must not yield verified A5a nonactivation or verified invocation. Preserve unavailable/unusable outcomes separately from a valid completed negative observation. Author reason-specific fixtures for all these cases.

7. **CHG-011, CC7-007 high.** The local evaluation-runner main(--resume) lifecycle is available and distinct from native/managed WAITING_USER callbacks. Author an actual fresh-process or fresh-module scenario for each supported pause/resume transition using the same original allocation, retained state and selected saved-evidence digests. Require C1 then B5 then B11 then A arms, exactly one ordered marker per arm and no rerun of completed work; assert each consumed judgment and unchanged prior result binding. This is locally implementable authoring. If unfinished, record the local code/scenario subparts NOT_AUTHORED/PARTIAL separately from legitimately unavailable native callbacks; do not attribute all resume work to a missing collector.

8. **CHG-012, CC7-008 high; R9-CI-005 high.** Bind case A3a/A5a to the actual selected row and fixed should_trigger value. Current selected rows have id/query/should_trigger/split/category and no query_sha256; derive the digest from exact selected query bytes. Compare the actual prompt handed to the launcher, including any launch-time mutation, rather than copying an optional digest or asserting unchanged bytes. Author matched-row/query positive, A3a paired with A5a row negative, modified query negative and modified launch prompt negative. Author prospective A3a invocation and A5a noninvocation/completion assertions without changing their query bytes. Missing launcher must return honest NOT_RUN. Native completion, EOF, owned cleanup and actual A outcomes remain separate unavailable observations, not synthetic proof.

9. **CHG-013, CC7-009 medium; R9-CI-007 medium.** Replace unused case dictionaries with callable constructors/drivers/assertions in the fresh selected suite. The missing-judgment case supplies judge={}; retain already-correct P20. Assert exactly the permitted B prefix after failure. Every non-1 concurrency, including the historical A=2 and B=4 cases, expects code 6 and zero markers. P21 must actually start the next call in resume mode, mutate the bound inventory and assert recorded STALE for that mismatch. Retain the corrected locators: PLAN reference_conformance line 23, semantic criterion line 27, prior r8 remediation live-preservation line 27 (the older design example was line 51), activation implementation run_phases.py _activation. Do not rewrite the historical scenarios as covering only zero.

10. **CHG-014, CC7-010 medium; R9-CI-001 high.** Connect real A caller/gate nonlaunch paths for valid selectors encountering existing destination or insufficient cap. Assert no launch marker, truthful recorded nonlaunch outcome, clean control completion, no unwanted attempt directory and exact preserved sentinel bytes or before/after digests; a size-only inventory does not prove preservation. Refusal is not arm PASS.

11. **CHG-015, CC7-011 medium; R9-CI-001 high / R9-CI-006 medium.** Fresh freeze/record emission requires the explicitly selected issued current allocation, including session/revision/path/digest and run identity; reject missing or stale values before output. Equality of two arbitrary caller dictionaries is not authority binding. No SESSION-001@5, @9 or @10 digest may be baked into generic emitting code as a substitute for the fresh selection. A future @10 issuance supplies the actual allocation through verified inputs. Author exact-selected-current emission positive and missing/stale/mismatched selection negatives; leave historical records untouched.

12. **CHG-016/019, CP5-CK-001 retained absence-disclosure limitation, CC8-PROP-001 high; CC9-RR-001/003 high.** Bind disclosure to the referenced artifact and its section fact. Actual inspected absence plus an explicit matching absence disclosure is required under selected v1. Use the retained real IDEAS-900 no-heading target with its actual digest (103bdc02105ace4c1eb5b6ddea626033336b445fc38c24cedab5b9e491aebc43), real matching upstream and fresh checker/suite binding. Preserve same-artifact absence positive, same-ID authoring-date-only negative and empty missing_inputs negative. Add: "IDEAS-900 has no authoring date; IDEAS-901 has no stable sections." => FAIL for IDEAS-900; "IDEAS-900 has no stable sections; its authoring date is unknown." => PASS with other checks satisfied; "IDEAS-900: unable to read the section list, so no sections are cited." => FAIL; a real heading-present target with false absence => FAIL; unreadable/unknown sections => no accepted absence. Supply actual target/config bytes so each reaches disclosure/heading classification. Replace the R9 zero-digest/missing-target placeholder only in the fresh run and assert its predicate-specific reason, not generic early failure. Do not distribute an entry-wide classification to every ID or let an unrelated unknown erase a valid absence fact.

## Partial states, observation limits and historical ownership

REPAIR-MAP.json carries all original CHGs and exact original severities alongside reviewer statements. R9 checker changes and helper source genuinely exist; they are not integrated control repairs or an executed suite. The previous broad APPLIED_EXECUTABLE labels are historical author claims, retained rather than silently rewritten. Current intake distinguishes specification, helper/checker source, actual caller integration, executable fixtures/suite, native collector/deferred subparts and execution. New work records before/after evidence and COMPLETE_AUTHORED_UNEXECUTED, PARTIAL, NOT_AUTHORED or ROOT_DEPENDENCY_DEFERRED for each applicable subpart. Do not label an entire CHG applied while required caller/fixture work remains absent. No future results may be claimed in advance.

Root owns runtime changes, authentic native observation availability, root admission and later tests. This packet's root status is supplied only through the separately finalized ROOT-INTEGRATION-ANNEX.json and referenced frozen review/status artifacts before issuance. Never import live concurrently edited utility/runtime policy files as selected author input. The prospective v2 interface is unissued. All historical Codex C COULD_NOT_RUN and B-arm NOT_RUN states, prior Claude budget FAILs and the 687-second late write remain unchanged. Other three authorities/source trees remain held; no new prompts or runs are allocated to them.

## Clock, creator and final custody

The first author tool/action is an actual clock observation before any assignment-file reading. Count all reading, preparation, waits, creator action and persistence in the one allocation: at most 120 minutes, final twenty reserved for freeze/report/readback/receipt, with explicit absolute cutoff/deadline. Stop technical edits at reserve; return precise partial by original deadline. Never reset/extend or replace first-work time. Record actual observations only; scheduled times are not observed events.

Use the selected native Anthropic skill-creator in the retained Claude session as already-authorized authoring guidance; record actual observable invocation/application or COULD_NOT_RUN. Reading/copying is not invocation. Creator evaluations, helper execution, native/model clients, replacement providers or delegates remain disabled. Frozen validator guidance is read-only review context, not a validator campaign.

Produce CLOCK, unchanged baseline copies, complete final twenty-file candidate copies, source mapping and executable/fixture inventory, additive R9-CORRECTION.md, AUTHORING-RECORD.md, per-CHG/subpart builder receipt, substantive handoff and one named report. Read every complete final source/control/checker/suite/fixture/specification/report byte set and record paths/full SHA-256. Finalize all intended manifest members before writing the manifest; exclude the manifest itself and explicitly list the later readback/receipt closure boundary. Then read back the finalized manifest and all listed members, publish READBACK.json with exact coverage, and publish a separate external RECEIPT.json binding report, manifest, READBACK and all later handoff/report artifacts. The receipt excludes itself; read it back afterward and return its full SHA-256 separately. A late artifact must have an explicit later binding, never implicit coverage or self-hash.

Report source authoring and every executable/specification/caller/suite subpart separately. Validation NOT_PERFORMED; tests, native C/B/A, root admission, hook activation and human acceptance NOT_RUN; hooks DESIGN_ONLY. Any authorized creator result is reported separately. No native finding closure, acceptance, release readiness or authority issuance follows from source presence, custody or this authoring handback. Return named report/manifest/readback/receipt paths and full hashes, then hold for independent root review.
