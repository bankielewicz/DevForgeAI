{
  "format": "devforge.web-response/v1",
  "text": "Optimizing skill descriptions - Agent Skills (https://agentskills.io/skill-creation/optimizing-descriptions)\nciteturn28view0 [wordlim: 200] Crawled: today; Content type: text/html; Source: open({\"ref_id\":\"https://agentskills.io/skill-creation/optimizing-descriptions\",\"lineno\":null}); Total lines: 209\nL0: > ## Documentation Index\nL1: >\nL2: > Fetch the complete documentation index at: cite0†/llms.txt L3: >\nL4: > Use this file to discover all available pages before exploring further.\nL5: \nL6: cite1†Skip to main content L7: \nL8: cite2†Agent Skills home page Agent Skills L9: \nL10: Search...\nL11: \nL12: Navigation\nL13: \nL14: For skill creators\nL15: \nL16: Optimizing skill descriptions\nL17: \nL18: For skill creators\nL19: # Optimizing skill descriptions\nL20: \nL21: How to improve your skill’s description so it triggers reliably on relevant prompts.\nL22: \nL23: Copy page Copy page\nL24: A skill only helps if it gets activated. The `description` field in your `SKILL.md` frontmatter is the primary mechanism agents use to decide whether to load a skill for a given task. An under-specified description means the skill won’t trigger when it should; an over-broad description means it triggers when it shouldn’t. This guide covers how to systematically test and improve your skill’s description for triggering accuracy.\nL25: ##\nL26: \nL27: cite3†​ L28: \nL29: How skill triggering works\nL30: Agents use cite4†progressive disclosure to manage context. At startup, they load only the `name` and `description` of each available skill — just enough to decide when a skill might be relevant. When a user’s task matches a description, the agent reads the full `SKILL.md` into context and follows its instructions. This means the description carries the entire burden of triggering. If the description doesn’t convey when the skill is useful, the agent won’t know to reach for it.\nL31: One important nuance: agents typically only consult skills for tasks that require knowledge or capabilities beyond what they can handle alone. A simple, one-step request like “read this PDF” may not trigger a PDF skill even if the description matches perfectly, because the agent can handle it with basic tools. Tasks that involve specialized knowledge — an unfamiliar API, a domain-specific workflow, or an uncommon format — are where a well-written description can make the difference.\nL32: ##\nL33: \nL34: cite5†​ L35: \nL36: Writing effective descriptions\nL37: \nL38: Before testing, it helps to know what a good description looks like. A few principles:\nL39:   * Use imperative phrasing. Frame the description as an instruction to the agent: “Use this skill when…” rather than “This skill does…” The agent is deciding whether to act, so tell it when to act.\nL40:   * Focus on user intent, not implementation. Describe what the user is trying to achieve, not the skill’s internal mechanics. The agent matches against what the user asked for.\nL41:   * Err on the side of being pushy. Explicitly list contexts where the skill applies, including cases where the user doesn’t name the domain directly: “even if they don’t explicitly mention ‘CSV’ or ‘analysis.’”\nL42:   * Keep it concise. A few sentences to a short paragraph is usually right — long enough to cover the skill’s scope, short enough that it doesn’t bloat the agent’s context across many skills. The cite6†specification enforces a hard limit of 1024 characters.\nL43: ##\nL44: \nL45: cite7†​ L46: \nL47: Designing trigger eval queries\nL48: \nL49: To test triggering, you need a set of eval queries — realistic user prompts labeled with whether they should or shouldn’t trigger your skill.\nL50: eval_queries.json\nL51: \nL52:     `[\nL53:       { \"query\": \"I've got a spreadsheet in ~/data/q4_results.xlsx with revenue in col C and expenses in col D — can you add a profit margin column and highlight anything under 10%?\", \"should_trigger\": true },\nL54:       { \"query\": \"whats the quickest way to convert this json file to yaml\", \"should_trigger\": false }\nL55:     ]\nL56:     `\nL57: \nL58: Aim for about 20 queries: 8-10 that should trigger and 8-10 that shouldn’t.\nL59: ###\nL60: \nL61: cite8†​ L62: \nL63: Should-trigger queries\nL64: \nL65: These test whether the description captures the skill’s scope. Vary them along several axes:\nL66:   * Phrasing: some formal, some casual, some with typos or abbreviations.\nL67:   * Explicitness: some name the skill’s domain directly (“analyze this CSV”), others describe the need without naming it (“my boss wants a chart from this data file”).\nL68:   * Detail: mix terse prompts with context-heavy ones — a short “analyze my sales CSV and make a chart” alongside a longer message with file paths, column names, and backstory.\nL69:   * Complexity: vary the number of steps and decision points. Include single-step tasks alongside multi-step workflows to test whether the agent can discern the skill is relevant when the task it addresses is buried in a larger chain.\nL70: The most useful should-trigger queries are ones where the skill would help but the connection isn’t obvious from the query alone. These are the cases where description wording makes the difference — if the query already asks for exactly what the skill does, any reasonable description would trigger.\nL71: ###\nL72: \nL73: cite9†​ L74: \nL75: Should-not-trigger queries\nL76: \nL77: The most valuable negative test cases are near-misses — queries that share keywords or concepts with your skill but actually need something different. These test whether the description is precise, not just broad. For a CSV analysis skill, weak negative examples would be:\nL78: \nL79:   * `\"Write a fibonacci function\"` — obviously irrelevant, tests nothing.\nL80:   * `\"What's the weather today?\"` — no keyword overlap, too easy.\nL81: Strong negative examples:\nL82:   * `\"I need to update the formulas in my Excel budget spreadsheet\"` — shares “spreadsheet” and “data” concepts, but needs Excel editing, not CSV analysis.\nL83:   * `\"can you write a python script that reads a csv and uploads each row to our postgres database\"` — involves CSV, but the task is database ETL, not analysis.\nL84: ###\nL85: \nL86: cite10†​ L87: \nL88: Tips for realism\nL89: \nL90: Real user prompts contain context that generic test queries lack. Include:\nL91: \nL92:   * File paths (`~/Downloads/report_final_v2.xlsx`)\nL93:   * Personal context (`\"my manager asked me to...\"`)\nL94:   * Specific details (column names, company names, data values)\nL95:   * Casual language, abbreviations, and occasional typos\nL96: ##\nL97: \nL98: cite11†​ L99: \nL100: Testing whether a description triggers\nL101: The basic approach: run each query through your agent with the skill installed and observe whether the agent invokes it. Make sure the skill is registered and discoverable by your agent — how this works varies by client (e.g., a skills directory, a configuration file, or a CLI flag). Most agent clients provide some form of observability — execution logs, tool call histories, or verbose output — that lets you see which skills were consulted during a run. Check your client’s documentation for details.\nL102: The skill triggered if the agent loaded your skill’s `SKILL.md`; it didn’t trigger if the agent proceeded without consulting it. A query “passes” if:\nL103:   * `should_trigger` is `true` and the skill was invoked, or\nL104:   * `should_trigger` is `false` and the skill was not invoked.\nL105: ###\nL106: \nL107: cite12†​ L108: \nL109: Running multiple times\nL110: Model behavior is nondeterministic — the same query might trigger the skill on one run but not the next. Run each query multiple times (3 is a reasonable starting point) and compute a trigger rate: the fraction of runs where the skill was invoked. A should-trigger query passes if its trigger rate is above a threshold (0.5 is a reasonable default). A should-not-trigger query passes if its trigger rate is below that threshold. With 20 queries at 3 runs each, that’s 60 invocations. You’ll want to script this.\nL111: Here’s the general structure — replace the `claude` invocation and detection logic in `check_triggered` with whatever your agent client provides:\nL112: \nL113:     `#!/bin/bash\nL114:     QUERIES_FILE=\"${1:?Usage: $0 <queries.json>}\"\nL115:     SKILL_NAME=\"my-skill\"\nL116:     RUNS=3\nL117: \nL118:     # This example uses Claude Code's JSON output to check for Skill tool calls.\nL119:     # Replace this function with detection logic for your agent client.\nL120:     # Should return 0 (success) if the skill was invoked, 1 otherwise.\nL121:     check_triggered() {\nL122:       local query=\"$1\"\nL123:       claude -p \"$query\" --output-format json 2>/dev/null \\\nL124:         | jq -e --arg skill \"$SKILL_NAME\" \\\nL125:           'any(.messages[].content[]; .type == \"tool_use\" and .name == \"Skill\" and .input.skill == $skill)' \\\nL126:           > /dev/null 2>&1\nL127:     }\nL128: \nL129:     count=$(jq length \"$QUERIES_FILE\")\nL130:     for i in $(seq 0 $((count - 1))); do\nL131:       query=$(jq -r \".[$i].query\" \"$QUERIES_FILE\")\nL132:       should_trigger=$(jq -r \".[$i].should_trigger\" \"$QUERIES_FILE\")\nL133:       triggers=0\nL134: \nL135:       for run in $(seq 1 $RUNS); do\nL136:         check_triggered \"$query\" && triggers=$((triggers + 1))\nL137:       done\nL138: \nL139:       jq -n \\\nL140:         --arg query \"$query\" \\\nL141:         --argjson should_trigger \"$should_trigger\" \\\nL142:         --argjson triggers \"$triggers\" \\\nL143:         --argjson runs \"$RUNS\" \\\nL144:         '{query: $query, should_trigger: $should_trigger, triggers: $triggers, runs: $runs, trigger_rate: ($triggers / $runs)}'\nL145:     done | jq -s '.'\nL146:     `\nL147: If your agent client supports it, you can stop a run early once the outcome is clear — the agent either consulted the skill or started working without it. This can significantly reduce the time and cost of running the full eval set.\nL148: ##\nL149: \nL150: cite13†​ L151: \nL152: Avoiding overfitting with train/validation splits\nL153: \nL154: If you optimize the description against all your queries, you risk overfitting — crafting a description that works for these specific phrasings but fails on new ones. The solution is to split your query set:\nL155:   * Train set (~60%): the queries you use to identify failures and guide improvements.\nL156:   * Validation set (~40%): queries you set aside and only use to check whether improvements generalize.\nL157: Make sure both sets contain a proportional mix of should-trigger and should-not-trigger queries — don’t accidentally put all the positives in one set. Shuffle randomly and keep the split fixed across iterations so you’re comparing apples to apples. If you’re using a script like the one cite12†above , you can split your queries into two files — `train_queries.json` and `validation_queries.json` — and run the script against each one separately.\nL158: ##\nL159: \nL160: cite14†​ L161: \nL162: The optimization loop\nL163:   1. Evaluate the current description on both train and validation sets. The train results guide your changes; the validation results tell you whether those changes are generalizing.\nL164:   2. Identify failures in the train set: which should-trigger queries didn’t trigger? Which should-not-trigger queries did?\nL165:      * Only use train set failures to guide your changes — whether you’re revising the description yourself or prompting an LLM, keep validation set results out of the process.\nL166:   3. Revise the description. Focus on generalizing:\nL167:      * If should-trigger queries are failing, the description may be too narrow. Broaden the scope or add context about when the skill is useful.\nL168:      * If should-not-trigger queries are false-triggering, the description may be too broad. Add specificity about what the skill does not do, or clarify the boundary between this skill and adjacent capabilities.\nL169:      * Avoid adding specific keywords from failed queries — that’s overfitting. Instead, find the general category or concept those queries represent and address that.\nL170:      * If you’re stuck after several iterations, try a structurally different approach to the description rather than incremental tweaks. A different framing or sentence structure may break through where refinement can’t.\nL171:      * Check that the description stays under the 1024-character limit — descriptions tend to grow during optimization.\nL172:   4. Repeat steps 1-3 until all train set queries pass or you stop seeing meaningful improvement.\nL173:   5. Select the best iteration by its validation pass rate — the fraction of queries in the validation set that passed. Note that the best description may not be the last one you produced; an earlier iteration might have a higher validation pass rate than later ones that overfit to the train set.\nL174: Five iterations is usually enough. If performance isn’t improving, the issue may be with the queries (too easy, too hard, or poorly labeled) rather than the description.\nL175: The cite15†`skill-creator`†github.com Skill automates this loop end-to-end: it splits the eval set, evaluates trigger rates in parallel, proposes description improvements using Claude, and generates a live HTML report you can watch as it runs.\nL176: ##\nL177: \nL178: cite16†​ L179: \nL180: Applying the result\nL181: \nL182: Once you’ve selected the best description:\nL183:   1. Update the `description` field in your `SKILL.md` frontmatter.\nL184:   2. Verify the description is under the cite6†1024-character limit .\nL185:   3. Verify the description triggers as expected. Try a few prompts manually as a quick sanity check. For a more rigorous test, write 5-10 fresh queries (a mix of should-trigger and should-not-trigger) and run them through the eval script — since these queries were never part of the optimization process, they give you an honest check on whether the description generalizes.\nL186: Before and after:\nL187: \nL188:     `# Before\nL189:     description: Process CSV files.\nL190: \nL191:     # After\nL192:     description: >\nL193:       Analyze CSV and tabular data files — compute summary statistics,\nL194:       add derived columns, generate charts, and clean messy data. Use this\nL195:       skill when the user has a CSV, TSV, or Excel file and wants to\nL196:       explore, transform, or visualize the data, even if they don't\nL197:       explicitly mention \"CSV\" or \"analysis.\"\nL198:     `\nL199: The improved description is more specific about what the skill does (summary stats, derived columns, charts, cleaning) and broader about when it applies (CSV, TSV, Excel; even without explicit keywords).\nL200: ##\nL201: \nL202: cite17†​ L203: \nL204: Next steps\nL205: \nL206: Once your skill triggers reliably, you’ll want to evaluate whether it produces good outputs. See cite18†Evaluating skill output quality for how to set up test cases, grade results, and iterate.\nL207: \nL208: cite19†Best practices cite18†Evaluating skills \n"
}
