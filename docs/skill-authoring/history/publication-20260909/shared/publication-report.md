# Shared DevForgeAI source publication

This draft preserves the exact pending shared contracts, guidance, contributor design records and publication plan from DevForgeAI main at `d29430beace67a141d4d35c2beb066981060cda4`. It retains newer manual-mode/local-baseline provisions; stale shared copies from author worktrees are excluded. Provider candidates and hooks belong to separate dependent draft PRs.

The current user assigned both repository coordinators and publication of all four released author handoffs to one app conversation. Original author worktrees remain intact. The saved planning prompts are historical instructions; no original session has to run Git inside its restricted authoring sandbox.

`ai-shared-source-selection.json` records the 37 selected files. `evidence-map.json` maps 92 original records to 78 unchanged content-addressed objects: contributor authority snapshots, the implementation handoff and selected concise-handoff evidence and dependencies. Objects use inert text filenames so historical skill entrypoints, fixtures and workflow snapshots are not treated as current framework source. Their bytes and historical locators are preserved; the map supplies their new location. This selected preservation does not claim full archival closure of all prior campaigns.

The baseline and five historical proposal/VPI tips are published as exact refs. `ai-public-suitability.json` binds the screened reachable objects; `ai-archive-results.json` records archive refs. These refs preserve history without merging deferred implementation.

The four source releases comprise 30 Codex brainstorm, 20 Claude brainstorm, 184 Codex contribution and 78 Claude contribution files, all verified against their inventories. Candidate PRs must preserve those exact bytes and state their source-only/static evidence limits. Native qualification, hook activation, operational installation and acceptance remain unclaimed. Hook configuration remains a separate unactivated draft.

CLI publication is https://github.com/bankielewicz/DevForge/pull/1. Its exact source passed local Rust checks and 621 Python tests. The demo stopped at the promoted-package manual-adoption evidence requirement. Hosted CI stopped at bubblewrap uid-map permission denial, before its Rust/Python steps. No gates, tests or CI policies were changed to complete source publication.

Private backup gap: workspace-root AGENTS.md/CLAUDE.md and research under docs/, installed .agents packages, ignored .poc/runtime outputs not explicitly selected, WSL and Windows client histories/settings/authentication, and unselected tmp files are outside these source pushes. No private off-machine destination was supplied. Originals remain local and no cleanup, credential copying or session shutdown occurred. Build caches and reproducible publication staging remain temporary.
