# Accepted project decisions
Owner: synthetic project owner under this user task. Python 3.11; pathlib and json standard library only. No third-party dependency, database or network. Public functions load(path) and save(path, notes) remain. Missing input raises FileNotFoundError and differs from a valid empty JSON list. Invalid JSON/read errors surface; never overwrite unreadable input. Notes are JSON objects with nonempty unique string id and string text. Unknown JSON fields must survive edits. Edit only the addressed note text when asked; do not drop metadata or change untouched notes. This task creates an expert instruction package, not application implementation. Existing code is authoritative evidence about current behavior, while these accepted decisions control desired behavior. Do not invent atomic-save guarantees or claim all constraints are currently tested. Source project/experts/notes-storage maps to proposed .agents/skills/notes-storage; only the source and allocated outbox may be authored, never install.
