# Bounded utility runtime alignment

This amendment extends the preserved validator design and builder SENH revision 3; their original sections, decisions and evaluation records remain historical inputs. The user requests protected runtime integration and bounded alignment. The validator edit reservation was released at a78b9bff83c090153952b31b84b0584ef1650f02; no newer reservation was observed. assignment.json binds the source fence and reviewed mechanical interface at DevForge 8a9702523569e3713863a3abc8f811e17cedb926. Selected exact runtime sources are preserved in selected-runtime.

Enhance the existing Codex skill-builder and skill-validator. Their purpose, activation, ownership and canonical-to-installed mapping remain suitable; the prior discovery/search decision is retained. Installation copies are generated by integration after authoring, and all evaluation occurs separately. No other skill, shared contract, hook configuration or executable helper is changed by this authoring task.

The selected governing authoring/execution contracts are the exact revision-3 files already preserved in skill-builder-modernization-20260907T125046Z/selected-inputs. The shared handoff and artifact source selections come from that same preserved set. New working-tree shared bytes do not replace them. The older validator selection remains explicitly historical in derivation.json. Source paths, exact hashes and transformations are retained there, with this amendment and selected runtime identities.

| Requirement | Bounded change and preserved behavior |
| --- | --- |
| AL-01 | Keep builder authoring-only and validator target-read-only. Mechanical admission, transitions, waiting, correction, artifact checks and receipt publication/readback remain with protected runtime. |
| AL-02 | Document the selected utility-session/delivery/checkpoint/receipt v1 interface conditionally. Consume actual admitted context and allocated artifact shapes; never manufacture a session, challenge, schema or controller command. |
| AL-03 | Retain validator W1, P1-P6, T01-T12 Enforced and all cases. Builder Intake, Selection, Design, Authoring, PreparedTransfer and required actions are Enforced for this user's integration assignment; other assignments retain their actual classifications. |
| AL-04 | Preserve Git worktree selection, frozen bounded preparation allocation, actual preparation and later complete experiment/readiness freeze. A model/auth/budget gap alone does not block authorized preparation. |
| AL-05 | Map H1-H5 requirements to protected utility mechanics and external producer gates. Keep earlier generic PreToolUse/Stop proposals historical; never combine them blindly with selected managed callbacks or claim hooks active. |
| AL-06 | Require complete native allocation including controls, probes, graders, receiving calls and failures. Preserve approved caps/deadlines through pauses; no silent dropped cases or uncounted retry. Planning, reservation and fixture tests do not establish execution. |
| AL-07 | Keep installed C, comparative B and discovery A separate, with exact raw evidence and independent semantic judgment. Process authenticity, callback origin, consultation and rendered delivery are separate observations. |
| AL-08 | In managed mode supply phase-specific useful evaluation/authoring artifacts. Never call advance/resume/complete/check/verify or a receipt helper to substitute for missing runtime admission. Existing evaluator helpers may produce scoped analysis; they cannot create authoritative runtime completion. |
| AL-09 | A pending consequential question remains unresolved until an actual matching answer and any required operator interpretation exist. Retain original deadline and correction budget. Continue permitted independent work and failure reporting. |
| AL-10 | Populate handoff at completion/transfer/recovery using only creation-time observations. No containing self-digest, circular references or later receipt claims. Runtime owns managed final-byte identities; unmanaged author observations are labelled. Prepared handoff, admitted transition and receiving invocation remain separate. |
| AL-11 | Preserve every prior source, installed copy and evaluation result. Refresh active derivation hashes and selected-source disposition consistently; retain older selection records as historical. New bytes need affected reevaluation. |
| AL-12 | Disclose actual limits: native campaign not admitted, interactive native answer transport unsupported, effective native boundaries/hooks and receiving invocation unobserved. This authoring is neither integration acceptance nor native success. |

Acceptance examples for the separate validator: confirm package-relative managed links when source docs are unavailable; review skipped-phase and stale/wrong-task evidence handling against the external interface; a mismatched answer cannot become approval; absent managed context cannot trigger helper-based completion; native failure still permits P5/P6 reporting; a handoff saved before receipt publication keeps that creation-time status; old derivation selections remain clearly historical. Reuse all original cases and their native variants. These examples add no covert case exclusions or execution authority.

The reviewed mechanical component is complete for its documented scope. The native launcher/importer revision is separately undergoing review and is not selected as an observed capability by this amendment. Unsupported native prerequisites remain with integration/operator. The current coverage analysis already proves that the approved 24 total attempts cannot hold all required executions. No campaign clock is bound.

Validation status: Not performed.
Hook status: Design only.
