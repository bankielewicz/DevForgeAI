Evaluate the new skill-validator candidate identified by source-after-manifest.json in this handoff directory against the exact design-after.md, including SV-WA-01–06. Preserve the former evaluation, both old/new canonical and installed snapshots and all previous evidence. Treat CHG-WA-01 as a new authorized usability enhancement, not a retroactive defect. Allocate a new evaluation output directory; do not resume by editing previous outputs.

The user already selected Create Git worktrees for validation. Reuse that selection without asking again. Resolve the consuming repository, bounded workspace count, destinations/write scope and authorization; infer routine base/detached details from the actual assignment. Freeze a concise workspace-allocation.json and prepare the assigned worktrees once that allocation is complete, even if native model/authentication/repetitions/attempt-time budgets remain pending. Record actual preparation and remaining setup in a separate environment-setup.json. No test worktrees were created by this authoring handoff.

Before measured native execution, freeze the complete cases/candidate/baseline/client/model/auth/repetitions/budgets/observation plan with demonstrated boundaries and unique attempt-to-unused-workspace/output/client-state bindings. Obtain actual protected runtime admission. Additional workspaces need bounded allocations; never silently reset or reuse prior writable state. Prepare subscription sign-in at concrete paths under an operator arrangement; never copy credentials or global stores. Missing native observations remain unavailable while prepared workspaces are retained and delivered.

Use the former candidate in source-before as old_skill. Independently evaluate the new package structurally and with a fresh static reviewer, then separate native C/B/A as prerequisites permit. Cover revised SV-019/SV-022, new SV-024–SV-028 and applicable existing cases. Cases are authored and NOT_RUN; do not expose expected answers to workers or treat authoring/installation hashes as executed case evidence. Missing native choices in a self-case are inputs to the inner validation fixture, never authorization to launch the outer test worker without its own complete confined subscribed-runtime plan.

Preserve all P1–P6/T01–T12 Enforced classifications and selected contract pins. Keep mechanical admission/transitions with the protected runtime; runtime-integration-requirements.md records separately owned unimplemented work. Do not change shared policy, helpers, hooks, skill-builder guidance or sibling skills. Report any unsupported runtime separately. Return new evidence-bound verification results, bounded builder specification and handoff with exact candidate/plan identities; no automatic target repairs or acceptance.
