# Separate protected-runtime integration requirements

Status: Requirements only; no implementation or validation in this authoring assignment. Owner: protected runtime/shared integration owner, not this skill-only author.

1. Distinguish bounded workspace preparation from measured native execution in the actual protected action admission. Preparation consumes actual authorization plus frozen repository/common-directory/base/count/destinations/write scope; unknown native model/auth/repetitions/budgets alone must not deny that action.
2. Native admission consumes the complete immutable experiment and authentic installation/authentication/observation/boundary evidence, mapping each attempt to its allocated unused workspace and independent client state. Allocation/PREPARED status cannot substitute.
3. Retain allocation/addition scope, consumed workspace history, immutable setup snapshots and plan/run bindings without cycles; reject unauthorized additions and silent reuse. Cover actual alternate write/launch routes under the selected runtime.
4. Preserve reporting on blocked native work. The existing evidence reducer's positive budget requirement belongs to experiment assessment; neither it nor a model-authored record grants preparation authority. Its current code does not mechanically enforce every new allocation field. Any future schema/reducer/runtime integration needs its own scoped implementation and independent tests.

H1/H2 remain Design only. No shared runtime policy, provider hooks, supervisor, helper code, skill-builder guidance or phase transition commands were modified. No active enforcement or native admission is claimed. The present request authorizes this record only, not integration changes.
