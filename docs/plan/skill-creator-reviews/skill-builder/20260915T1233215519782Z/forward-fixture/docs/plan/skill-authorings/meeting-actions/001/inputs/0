Create a portable skill named meeting-actions at the isolated project's src/agents/skills/meeting-actions. It converts supplied meeting notes into a Markdown action list with columns Action, Owner, Due date; use Unassigned and Not specified when absent, preserve explicit uncertainty, and do not invent decisions. Return the table in chat unless the user provides a file destination. No external services needed.
Author and deliver through the documented skill-builder workflow. Do not run tests or execute the generated skill. Do not modify source builder, operational skills, or existing evidence. Keep this single trial bounded to about 5 minutes; retain failures and do not retry failed publication.