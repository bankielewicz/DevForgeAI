# Frozen source-identity implementation handoff

The user's approval selects the bounded proposal at `docs/plan/framework-worker-native-continuation/20260916T013303Z/source-inventory-proposal.md`. Implementation, developer checks and readback are complete. The independent QA campaign owns the next qualification decision; no developer result authorizes installed-source collection before that PASS.

- Candidate: `C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe`.
- Frozen manifest: `candidate-manifest.json`, SHA-256 `3108f976f7d51b734ab995ebf729302ef74a4386ddaafb5561cddbd6f588d65c`, 56 files. `candidate-snapshot/` retains these bytes. All selected input hashes are in `selected-inputs-final.json`.
- New companion contract: `docs/specs/framework/runtime/codex-worker-source-identity-v1.md`. Read together with base worker, native-readiness and preflight v1; their old bytes remain unchanged.
- Product changes: one compiled exact plugin-cache junction record; inventory schema 2 binding applicable mappings and physical control files; strict rejection for every other reparse; OS mapping rechecks before/after collection; complete review and executable recheck after spawn-intent preparation, before actual process creation.
- Native Windows developer results: 30/30 required groups, 37/37 unit plus 86/86 integration tests, full line coverage 3191/3339 (95.5675351901767%). Formatting, Clippy and locked/offline build passed. Documentation-test command passed with zero cases. See `developer-results.md` and `receipt-readback.json` for retained failures, commands and limits.
- Independent evidence destination: `C:\Projects\DevForgeAI\docs\plan\framework-worker-source-identity-qa\20260916T021843Z-retest`. Source is frozen throughout this assessment. Use the original manifest plus an independent target directory; `CARGO_MANIFEST_DIR` makes a relocated build unsuitable for installed-source collection.
- Build binaries before focused library tests: the SI-T08 fixture requires `protocol-peer.exe`. Full all-targets tests satisfy this prerequisite.
- The private SI-T08 checker is an ordering seam; production always supplies the fixed full review/identity function. Source collector/review tests establish their behavior separately. Synthetic results do not establish actual Codex startup or effective permissions.

## Continuation limits

After independent QA PASS only, bind its original-package compiled binary and retry `profile-sources` against a fresh immutable native-layout fixture. Use a new distinct evidence directory and preserve the old exit-3 observation. A failed next prerequisite stops dependent work without broadening the reviewed mapping or changing installed files.

A successful inventory still precedes compiled effective-profile inspection and independently supported operator findings. False findings remain unqualified for work. Never manufacture all-true findings. The selected native completion/cancel trials retain their original model, effort, deadlines, one-shot/no-retry and process-cleanup requirements. Do not use the generic recorder's DEVNULL stdin for native run/preflight: EOF requests cancellation.

Native trials remain 0/2 NOT_RUN at this handoff. Authority implementation and provisioning remain separately deferred. Framework acceptance is NOT_EVALUATED and is not established by these tests, this handoff or the future QA verdict.
