# Bounded source-identity development

Selected root: `C:\Projects\DevForgeAI\docs\plan\framework-worker-source-identity\20260916T021843Z-dev`, derived from the repository's distinct docs/plan evidence requirement. Baseline 53 files matches the last qualified candidate (canonical manifest SHA-256 `05ef5b93f6e050668a7e9b4b333a715172ae2183ae65a7686618f10ca4736b87`). No Git metadata. Input hashes and immutable baseline are retained before changes.

## Slices and reuse

1. Read-only discovery: metadata at fixed plugin-cache depths and retained Codex 0.154.0 source role. Preserve installed state.
2. Companion amendment: `docs/specs/framework/runtime/codex-worker-source-identity-v1.md`; old contracts unchanged. SI-T01..10 are the selected amendment cases.
3. Source collection: extend existing `profile_sources` collection/verification; reuse strict path checks, bounded hashes and directory indices. Reuse existing native-identity Windows tag inspection, keeping launcher policy unchanged. Private test seams may supply synthetic mappings; public entry remains compiled-only.
4. Final spawn boundary: extend existing runner review/identity checks after arguments/intents; no new broker/service or source resolver exemption.
5. Developer QA: frozen combined candidate, full locked/offline Rust suite, doc discovery, rustfmt, Clippy and llvm-cov. Then separate independent QA with fresh evidence and unchanged candidate.
6. After independent PASS only: compiled source observation against installed inputs; inspect any actual next prerequisite failure without broadening policy.

## Declared developer denominator, before execution

Required selected groups: **30**: WF-01..20 (every existing subfixture conjunctive) plus SI-T01..10. Existing F-01/F-02 and native-readiness offline regression tests are mandatory supplemental checks within SI-T09, not duplicate denominator inflation. All tests in the package run. Full native Codex readiness remains separately 0/2 WN cases pending profile prerequisites; this amendment cannot pass those cases.

Coverage source denominator: **every first-party executable line in package src/**, including any newly introduced Rust module, Windows FFI, collector, runner, error and CLI paths. Enumerate final source files and raw LLVM line denominator before assigning coverage status. Exclude only tests/support fixtures and third-party dependencies; no src path exclusions. Existing lib.rs contains declarations only and may be 0/0. Both floors >=95%, exact fractions; below-floor full measurements trigger stop/handoff. Branch coverage is separately unperformed unless supported by the discovered toolchain.

Platform: native Windows x64, C: filesystem, package Cargo manifest/lockfile, installed Rust 1.97.1, no optional package features. No Linux/platform substitution or tool installation. Record commands with bounded noninteractive recorder, exact args/cwd/duration/native exit and raw outputs. Preliminary/focused Red failures are development evidence, not qualifying-suite results.

## Traceability and fixtures

SI-T01..06: real filesystem junction/type/path/content/member/schema fixtures in compiled Rust tests, evaluated against the companion's explicit allow/deny cases. SI-T07: review bindings and tampering fixtures. SI-T08: final-boundary mutation stimulus plus child-spawn observation. SI-T09: prior strict-path/bounds and all existing compiled peer/process regressions. SI-T10: before/after package + selected-input manifests and installed mapping observations. Every group starts NOT_RUN; delivery records actual evidence and status. Tests must establish setup success separately; no setup error is a Red.

No operational skill/cache/configuration modifications, no credential reads, and no protected acceptance claims. Write final artifact index once, after all reports and readbacks are complete.
