Preserve that attempt and explicitly label the metadata defect. For final adoption-chain verification, run a fresh independent chain in a NEW disposable project and NEW retained directory suffix -final using the same raw task/specification and exact original locators. Do not re-adopt the already governed target or rewrite its evidence. The same concrete adoption, first revision, conflict-resolution restoration, and later total_quantity change are authorized for the new synthetic target, with separate instruction records/digest bindings. This ensures the full final trial binds current manifest and correct custody metadata. Retain and report earlier attempts separately.
