# Focused finding validation

Authorized by the user's `Implement the plan.` following the approved conversational plan. This run executes validator-only assessment and bounded synthetic tests, then prepares a proposed remediation contract. It does not authorize or apply repairs, install anything, change another package or run the full native campaign.

Target: `C:/Projects/DevForgeAI/src/agents/skills/skill-validator`. Loaded evaluator: `.agents/skills/skill-validator`, a separate location. Exact selected specification and target identities are pinned under inputs/. Current target equals the reported 75-file delivery. Candidate helpers are self-review evidence. Independent expected results and fixtures live here, outside either package. No candidate implementation or prior test functions are imported as an oracle.

Sequence: preserve inputs; inspect code and prior fixture validity; pin expectations; replay R06/R07/P07/P08 read-only; execute independently constructed counterexamples and positive controls; adjudicate semantic support and practical impact; retain evidence; write proposed fixes for confirmed findings; verify references and final source preservation.

QA-01: distinguish duplicated check execution from shared citations, equivalent paths, and independently identified checks. QA-02: derive required handoff coverage from selected input; preserve failure/unknown/unperformed totals and optional branches. QA-03: exact protocol candidate extraction, byte locations, ordinary Unicode and redaction. QA-04: literal fence contents versus actual links, both markers and valid/invalid closing tails.

Every case records expected exit plus output properties before candidate execution; count one case once. Fixtures never claim actual producer execution. They test acceptance of synthetic record evidence only. Results classify each original finding CONFIRMED, REFUTED or UNRESOLVED; malformed-input acceptance does not establish a native workflow failure. Source preservation covers selected manifests and cited prior files, not all operational homes or historical evidence.

Windows-native Python, argv subprocess calls without a shell, 120-second attempts, per-command temporary roots under this run, no new dependency installation. Enumeration precedes recursion; links/reparse points and special files are rejected; 2,000-file/32-MiB ceilings apply to each bounded inventory. Captured target and selected documents are small and disjoint. All failed attempts retained. Final report separates scoped defects from native/resume/tokenizer/integration gaps and numeric package coverage. Repair and post-repair QA remain later stages.
