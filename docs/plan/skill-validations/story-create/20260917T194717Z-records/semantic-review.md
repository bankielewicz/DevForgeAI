# Captured-package semantic review

Scope: all 16 captured files, the selected authored requirements and design, import decisions, linked legacy source requirements, and current consumer entry contracts. This is an evaluator review of story-create, not validator self-qualification, certification, or Rust acceptance. It is distinct from native execution results.

## Identity, source fidelity and format

The target is an adaptive core at version 0.1.0 with no project-specific identity. The byte-bound authored contract and SC-001 through SC-016 govern; no competing origin was reconstructed. The intake verified the delivery and all 102 selected reference digests. Authoring custody does not establish quality. Original legacy requirements remain captured under inputs/repository/src/claude/skills/legacy/spec-driven-stories.

The 16-file package matches the selected publication digest. The installed creator checker and validator structure observations pass. The raw adaptive scan's source-directory identity item is resolved by the source manifest's original root and exact package digest. The quoted TODO/TBD/empty-input examples in recommendations-and-gaps.md explain rejected producer data; they are not unfinished instructions. Template markers deliberately belong to a consumed template and are explicitly removed during assembly. No Unicode candidate was observed across all captured text. The optional UI metadata file is absent and is not required.

All ordinary package links resolve. No unsupported anchor syntax needs a guessed renderer rule. Resource roles are meaningful: SKILL.md loads the descriptor and adaptive contract; its step table routes each conditional reference; the story and session templates have explicit consumers; the terminal command at SKILL.md line 17 reaches the binding helper. All 16 resources have supported use. Helper graph reachability alone was not treated as semantic proof.

Legacy comparison confirms that the import retains recommendation verification.command/expected and remediation alternatives, conditional RCA provenance, selected seed ingestion, XML ACs, technical format 2.0, UI applicability, single-file story delivery, links and recovery. Changes from old phase/CLI enforcement, universal predecessor dependencies, TDD-skipping types, inconsistent template versions and old parser assumptions are explicitly identified in import-decisions.md. The original source's version discrepancies are not silently carried into the new contract. The legacy directory token in the helper is only a read-exclusion constant; no legacy invocation or runtime dependency was found.

## Workflow and ownership

The entry prerequisite permits product effects only after exit 0/MATCH/BOUND, with current user/host authorization still required. The helper is read-only and does not create an identity, install a package, or grant protected authority. Missing Python and every rejection stop dependent product effects. The CLI and unit campaigns separately test the actual interface.

Intake preserves explicit metadata and literal paths, discovers project conventions, defaults transparently, scans active/archive identities and calls for exclusive creation. Explicitly selected all-outcome requests continue without a new approval ritual; proposal-only requests stop before product writes. Missing decisions localize blockers. The batch, seed, recommendation and gap branches have distinct input contracts and route back to common requirements, technical/UI assembly, delivery and final reporting.

SC-007/STC-005 require canonical clause owners, explicit acyclic prerequisites and integration obligations. SC-008 preserves exact verification expectations and rejects unknown/filtered entries instead of silently intersecting selections. Seed order is advisory and never written as a story dependency without a concrete producer. The source data boundary prohibits HTML execution or embedded command authority.

The single-file story contract and template agree on 0.1.0 metadata, the separately versioned technical block, required sections, conditional UI/implementation detail and unchecked future verification. Documentation-only applicability is explicit. Full readback, idempotent linking, reread-before-edit, partial-effect reporting and resume checks are actionable instructions. They do not make multi-file delivery atomic or prove protection against all concurrent races.

Development and QA are manual consumer handoffs. Their current installed entrypoints accept explicit story/specification inputs and do not require an adaptive binding. Consumer execution is assessed through fresh trial contexts containing unchanged producer outputs; a prose statement about compatibility is not the evidence.

## Contextual instruction review

| Exact bounded passage | Context and intended safeguard | Disposition |
| --- | --- | --- |
| “Continue only for exit **0**, status **MATCH**, reason **BOUND**.” | Entry predicate names concrete helper output before effects. | useful_instruction; preserve |
| “An existence check followed by an overwriting write is insufficient against concurrent creation.” | Intake identity allocation requires exclusive creation rather than an assumed reservation. | useful_instruction; preserve |
| “A successful subset is not full batch or epic completion.” | Batch delivery must account for blocked/omitted selected outcomes. | useful_instruction; preserve |
| “Do not silently process only an intersection of a requested set.” | Recommendation intake explicitly reports unknown or filtered IDs. | useful_instruction; preserve |
| “Multi-file delivery is not atomic.” | Delivery reports completed story and failed/pending related update separately. | useful_instruction; preserve |
| “A checkpoint is editable evidence, not approval or proof that a step happened.” | Resume rechecks current input/output identities and actual effects. | useful_instruction; preserve |

No unsupported enforcement claim or unbounded self-scoring loop was confirmed. Repeated preservation and authority warnings occur at relevant effect boundaries. The complete template is substantial, but length alone is advisory; native timeouts do not establish that length caused a target defect. Missing native coverage remains visible separately.

## Static limitations

Static review establishes the instruction paths and captured source relationships. It cannot prove every native branch, consumer, race, denial or platform. The optional tokenizer was not selected and no token-budget compliance is claimed. Static source-to-effect review plus bounded helper and prompt-injection fixtures does not constitute a security certification. Final findings and behavior status depend on retained native results and independent artifact review.
