# Linux and WSL extension

The user extended the active dev remediation with “test wsl & linux coverage” and supplied SSH access to `me@192.168.245.128`, naming `/home/me/Projects/index-qualification.iIHbKX/DevForgeAI/devforgeai`. The supplied credential is authentication-only and is not recorded in evidence.

The selected corrected candidate remains `C:\Projects\DevForgeAI\devforgeai`, bound by the parent source-manifest.json SHA256 `cf3ee99015db0dcc1eb544fdac1bfdb0fcd2f10bfc8b749857d52740d8258094`. The selected specification remains SHA256 `52d33c84435da8f3442c6b1dc4c5370113ff0eb93ee5cd2801903741900c89c4`. Preserve the completed Windows snapshot, builds, metrics, and draft delivery/checkpoint as historical Windows-scope evidence. Write the extension report separately before updating the final handoff.

Literal selected and resolved extension evidence root: `C:\Projects\DevForgeAI\docs\plan\index-service-remediation\20260914T1950304158791Z\platform-extension-001`. Pre-write output bindings: context.md; preflight.py and preflight reports; candidate.tar and transfer manifests; native_runner.py; transport.py; per-platform attempts, raw coverage, profiles, source/build/tool identities and completion records; extension-report.md; extension-manifest.json. Remote and WSL output destinations will be bound after verifying their native directories and source identities, before any remote writes.

Authorization includes starting the installed Ubuntu WSL target for requested tests and copying the exact corrected candidate into isolated native Linux test directories. Preserve the named existing remote checkout and prior evidence; do not synchronize over them. No installation, dependency upgrade, startup/systemd changes or deployment is selected. Missing tools are recorded and resolved only within the user's authorization. Windows and native Linux source identities must match by relative paths and byte hashes before measurement.

Coverage denominator: all compiled first-party src behavior, excluding only tests/examples/fixtures and third-party/generated code, same exclusions as Windows. Report integer executed-line counts and >=95% without rounding; branch coverage separately. Count unique required cases and distinguish original declared units, new regression cases, setup and platform-only cases. Actual WSL bridge tests are separately reported from Linux tests running inside WSL. New evidence cannot independently close QA findings or issue framework acceptance.

Initial discovery: sandbox WSL enumeration returned E_ACCESSDENIED; approved native `wsl --list --verbose` lists Ubuntu and docker-desktop, both WSL2 and stopped. Only Ubuntu is selected; docker-desktop is outside scope. OpenSSH is sandbox-blocked, so the user-supplied SSH endpoint is accessed only through approved native execution. Python Paramiko is not installed; no package installation performed.

Bound native destinations before writes: SSH `/home/me/Projects/index-qualification.iIHbKX/DevForgeAI/dev-remediation/20260914T1950304158791Z`; WSL `/home/bryan/Projects/devforgeai-remediation-20260914T1950304158791Z`. Both contain `devforgeai/` (isolated corrected candidate), `evidence/` (new receipts/profiles/reports), supplied spec/rules, normalized expected manifests and the evidence runner. Local fetched output directories are `linux/` and `wsl/` under this extension root. Neither destination replaces the existing source checkout. SSH host is Ubuntu26.04.1, native Rust1.93.1 and LLVM21; WSL is Ubuntu24.04.5, native Rust1.98.1 and bundled LLVM. No parent WSL AGENTS.md was found; remote AGENTS.md was read and matches the previously bound repository guidance.

Bridge fixture binding: `/tmp/dfr-wsl-20260914T1950304158791Z/bin/` in Ubuntu, with the compiled release CLI/daemon and `examples/wsl_fixture` copied byte-for-byte from the WSL isolated build. The example derives its data path from its binary parent; this short test-only path keeps Unix socket names within native limits. No product source edit or installed executable replacement. Local bridge outputs: `bridge_runner.py`, `bridge/{receipt.json,stdout.txt,stderr.txt}` and `wsl-fixture.json`, with final process readback. The two existing ignored Windows bridge tests will be selected explicitly; all Linux unit/coverage measurements remain independently recorded.

Final observation: Linux 2410/2830 (85.15901060070671%) and WSL 2418/2830 (85.4416961130742%) fail the line floor. Both executed all 77 collected tests successfully (75 product, 2 setup), with 18/18 original declared units. Both release builds and formatting passed; strict Clippy fails due to Windows-only imports in developer tests. No product/source edits were made during this testing extension. The Windows-to-WSL bridge invocation retained 1 pass and 1 failure; post-run readback found the configured /tmp fixture absent, so the pause/identity scenario is an unresolved fixture error, not confirmed product conformance failure. No automatic retry was performed. The retained native WSL builds and source still match; no owned processes remained; SSH session closed.

Final additional output bindings: `collect.py`, `diagnose_wsl.py`, `wsl-final-readback.json`, `write_extension.py`, the existing `linux-evidence.tar.gz` and `wsl-evidence.tar.gz`, `extension-report.md`, `extension-manifest.json`, and parent `final-delivery.md`/`final-checkpoint.md`. Parent Windows draft report/checkpoint remain unchanged historical scope. `wsl-fixture.json` will record only observed hashes and absence, not a fabricated successful bridge run.
