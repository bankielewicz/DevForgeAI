# Source observations

Observed 2026-09-15. These are source summaries and discovery evidence, not native qualification.

- [Official app-server documentation](https://learn.chatgpt.com/docs/app-server): fetched through OpenAI documentation search/retrieval. Documents JSONL stdio, initialization, threads/turns, interruption, managed ChatGPT authentication, account/rate-limit fields and version-specific schema generation. The command remains experimental and unsupported for production. The contract deliberately avoids external-token and unsandboxed process methods.
- [Official non-interactive documentation](https://learn.chatgpt.com/docs/non-interactive-mode): fetched after search identified this exact page. Documents `codex exec`, JSONL observations and reuse of saved CLI authentication. It remains a possible future alternative. An attempted `/docs/noninteractive` fetch returned 404; no conclusion relied on it.
- [Microsoft Job Objects](https://learn.microsoft.com/en-us/windows/win32/procthread/job-objects): documents group lifetime, kill-on-close and nested jobs. [Atomic job attachment](https://devblogs.microsoft.com/oldnewthing/20230209-00/?p=107812) documents `PROC_THREAD_ATTRIBUTE_JOB_LIST`, avoiding the crash interval between process creation and later assignment. These support the selected Windows process mechanism; Codex sandbox compatibility still needs native evidence.

The installed executable, rather than current web examples, owns the exact selected wire fields. See [schema command receipt](schema-command.json) and [schema file hashes](schema-manifest.json). The receipt retains its original output-directory argument ending `20260915T160000Z`; the evidence directory was renamed within the same parent to `20260915T151300Z` before document publication. This rename did not rerun the command or change schema bytes. Directory names are run identifiers; `started` in receipts is the observed execution time.

Read-only command discovery is retained in [discovery.json](discovery.json). All ten listed help/version commands exited 0. Codex emitted access-denied warnings for stale temp-directory cleanup and PATH aliases; those warnings remain in the output. Schema generation exited 0 with the same class of warnings. No app-server connection, login, account read, worker turn, model trial or Rust test was run.

The sandboxed `Get-CimInstance Win32_OperatingSystem | Select-Object Caption,Version,OSArchitecture` failed with Access denied. The same read-only command was rerun with approved escalation and returned Windows 11 Pro, 10.0.26200, 64-bit. `Get-Volume -DriveLetter C` also returned Access denied; the filesystem format was not needed and was not established. All work used native Windows executables on the Windows `C:\Projects\DevForgeAI` filesystem, not WSL or a remote checkout. PowerShell reported 7.6.6. No Git metadata exists at the workspace or index-package root; branch/HEAD are unavailable.

Scoped code inspection read `devforgeai/Cargo.toml`, `Cargo.lock`, `src/lib.rs`, `src/cli.rs`, and `src/bin/devforgeai.rs`, and listed source/tests. The manifest identifies `devforgeai-index` 0.1.0; CLI groups concern daemon/project/index/job/environment/tray/bridge. No workflow engine is inferred from those index jobs. The prior handoff's ten recorded document hashes all matched before edits. No handoff drift required restoration.

Applicable-instruction checks found no additional AGENTS.md at docs/, docs/specs/, docs/specs/framework/, docs/specs/framework/runtime/, docs/plan/ or devforgeai/. The root instructions govern. A broad AGENTS filename listing also exposed historical fixture instructions; those were not selected as applicable rules.

Memory lookup found no matching entry for this handoff/topic; no memory-derived facts were used. The supplied `dev` package and handoff both exclude contract authoring from dev implementation. OpenAI Docs was used for the integration research. No subagent or independent reviewer was run during this planning session; the semantic review is an author review, and independent QA is a later explicitly selected boundary.
