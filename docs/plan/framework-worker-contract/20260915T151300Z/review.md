# Contract author review

This is a factual and consistency review by the contract author, not independent product QA or framework acceptance.

Corrections applied before delivery:

1. Kept Codex app-server's experimental command status separate from use of non-experimental schema fields. The full framework remains not ready.
2. Chose a standalone nested Cargo workspace to avoid changing the existing index workspace or its acceptance denominator.
3. Defined atomic Windows Job Object attachment after Microsoft documentation identified the orphan interval in create-then-assign designs. Containment failure stops dispatch rather than relaxing it.
4. Kept acknowledgement of `turn/interrupt` separate from turn interruption and process-tree shutdown. Defined both completion/cancellation event orders and cleanup uncertainty precedence.
5. Made native profile review and source-digest checking explicit, including their inability to prove omitted configuration absent. Actual profile/account/model observations remain unperformed.
6. Kept synthetic fixture semantics separate from unresolved OrderDesk business policy and actual concurrent transactions. The fixture cannot establish useful full-product delivery or adaptation.
7. Defined the peer's case selection and synthetic account path so offline tests exercise the real adapter protocol without native authentication.
8. Made final message selection/deduplication, null usage, no-replay inspection, partial journals and required-case subfixture accounting explicit.
9. Retained broad MVP cases as prerequisites only; the 20 offline cases and two native cases have separate denominators and qualification claims.

Remaining limitations are intentional scope boundaries: reviewed native profile and trial authorization absent; production support unresolved; protected authority absent; full workflow/scheduler/persistence topology and comparator campaign deferred. A successful offline implementation must report those gaps, not close them with local JSON.
