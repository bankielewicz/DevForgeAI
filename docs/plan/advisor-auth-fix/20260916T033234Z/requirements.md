# Authorized advisor authentication repair

User approved explicit child authentication selection, subscription default for newly prepared requests, source repair, validation, installation, and bounded native completion. Legacy requests default to inherit. No global credential or authentication configuration changes.

Implementation: request auth_mode inherit/subscription; skill auth= convention; copied child environment removes only ANTHROPIC_API_KEY (case-insensitively for Windows); probes and review receive identical policy. Record mode without credential values. Preserve immutable legacy requests/receipts and two-attempt budget limits. Keep 300-second timeout and USD 2 total / USD 1 attempt caps.

Quality: retained red/green/refactor/QA; real local child-process checks; JSONL deterministic fixtures; all first-party scripts/ and evals/ in coverage denominator, tests and fixtures excluded; >=95% line coverage and unique required-case pass rate, all mandatory cases passing. Report native result separately from deterministic evidence and framework acceptance.

Delivery: back up source/installed bytes, publish validated changes only, compare installed hashes. Scope excludes other skills and Rust source modifications. Prior exhausted review docs/plan/advisor-runs/20260916T032132Z-3c7857 remains unchanged. The changed-package qualification is explicitly authorized by the approved implementation plan.

Host: Windows / PowerShell; cwd C:/Projects/DevForgeAI on native C: filesystem; Python C:/Program Files/Python310/python.exe; Claude C:/Users/bryan/.local/bin/claude.exe. Git metadata absent.

External contract was read before implementation. Its current required labels, verdict values, citation rules, read-only mandate and evidence requirements remain compatible with the parser. Current SHA256 eec3af8fa530175c1da9f31fcf05cab59c591dd30456be13ee9deb2ba234934e replaces the stale manifest dependency af4cd7f0505c92a186c29edc17328107ac2103f6069109c5aacac04da32b18ab only after this review and new package checks. Do not alter the external contract.
