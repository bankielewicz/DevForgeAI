Declared maintenance QA scope: all advisor unittest cases (89 existing plus 11 new) and all 38 existing JSONL cases. Count each once in final QA; focused and mutation runs are separate evidence, not extra passing required cases. No live Claude calls or operational writes.
Coverage denominator unchanged: all first-party executable Python files under scripts/ and evals/. Exclude tests/fixtures only; no executable first-party exclusions. Gate: >=95% aggregate executed-line coverage and >=95% required-case pass rate. Branch and per-file numbers are reported, with no added gate.
Host: native Windows, PowerShell, C:/Program Files/Python310/python.exe, cwd C:/Projects/DevForgeAI on Windows filesystem. PowerShell launcher source unchanged; launcher native qualification and independent skill validation are not part of this test-only scope.
