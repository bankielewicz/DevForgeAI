# Development source review before first adoption manifest binding

Reviewed intentional changes against the retained `builder-before/` byte copy and the approved adoption specification. No Git metadata is present. Source changes are confined to `src/agents/skills/skill-builder`; new receipts live in distinct evidence directories. No operational installation/copy update is part of this task.

- Existing six profiles and five legacy grader selections retain their definitions. Schema-1 checks still select schema 1 and reject adoption records. Shared provenance row parsing, build accounting, ordered comparison/delta logic and routing record bounds are reused, with explicit schema/version branches.
- Adoption helper performs accounting only and prints observations. It neither reads absolute identity metadata as locators nor writes a pointer/target. The skill instruction workflow owns ordinary authorized capture, source recheck, external publication and readback, with incomplete publication retained separately.
- Schema-2 origin validation binds adopted bytes and managed selection, before pointer, generated candidate/contract ownership and actual delivered after. Generated origins retain the original adoption and rehash their contract, selected authorized inputs, baseline and observation references in the preserved relative layout.
- Quality evidence can remain failed while custody passes. Model-authored authorization or readback flags are not described as proof of user intent, chronology or real publication. Independent task receipts are required for those observations.
- Structural check passed. The 27 focused adoption tests pass before manifest binding. Baseline legacy regression execution before edits passed 112 tests; complete post-binding regression and behavioral evaluations remain pending at this review stage.

First-run authoring error retained in conversation: an attempted documentation patch matched a nonexistent `#` line in spec-build.md and failed without mutation; the subsequent contextual patch succeeded. No evidence was reset or overwritten to conceal the failed patch.
