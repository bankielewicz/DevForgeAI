# Selected installed-source observation

The user approved the bounded source-inventory proposal, including its item 8: retry compiled installed-profile collection only after independent QA PASS. That prerequisite is now satisfied by the source-identity retest. The generic conditional wording in the QA handoff does not require another selection; the existing approval already covers this read-only observation.

Evidence destination: `C:\Projects\DevForgeAI\docs\plan\framework-worker-native-continuation\20260916T034357Z-source-identity`. Candidate, specification, QA report/index, compiled binary and fresh immutable fixture identities are verified by `prepare.py` before any product execution. No Git metadata exists.

Use the QA-built original-package binary for one `profile-sources` operation. It reads the installed profile's selected control sources and emits hashes/metadata; it does not start Codex. Preserve the prior source failure and every new outcome. No installed cache/configuration/credential/operational changes are permitted. A new prerequisite failure stops dependent work without expanding the reviewed exception. Effective-profile preflight, operator findings and the separately selected native trials retain their existing prerequisites. Authority implementation/provisioning remains outside this selection; framework acceptance is NOT_EVALUATED.
