# Native prerequisite continuation

Original selected evidence value: `C:\Projects\DevForgeAI\docs\plan\framework-worker-native-continuation\20260916T013303Z`. This is a fresh timestamped directory under the repository-required `docs/plan/`, selected for the active-goal continuation. Resolved root is identical. Promised outputs are this context, input/binary bindings, one recorded compiled source-inventory attempt, and a completion/blocker audit.

The preceding goal turn made progress: QA-F-COV-01 was repaired and independently verified on manifest `c27d8ce1742ba7cd0955f0bd18aded8de5f14c898c989654d7298835c3256f9e`, 53 files, with 114/114 tests and 2967/3103 coverage. Native trials and authority remain incomplete. This continuation does not replace the full goal with coverage-only success.

Selected action: read-only `profile-sources` using the uninstrumented executable from the independent QA build against a fresh layout-correct synthetic fixture. This is a prerequisite observation, not a model/native trial. It starts no Codex app-server. No configuration, credentials, installation, cache or reparse changes; no policy relaxation. The source contract requires strict reparse rejection, and the actual `chrome/latest` junction was observed still present before this attempt.

Candidate, contract and executed-binary bindings must be verified before launch. Capture raw streams, native exit, exact command/cwd, duration and source readback. Preserve all earlier artifacts and the QA index-retention limitation. The generic active objective is still `complete the following:`; original conversational selections and concrete contracts determine remaining scope. Authority contract/authorization readiness is being reviewed independently before any source change.
