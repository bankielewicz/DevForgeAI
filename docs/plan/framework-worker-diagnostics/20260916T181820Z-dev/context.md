# Bounded native-denial diagnostics development

Selected project: `C:\Projects\DevForgeAI`, native Windows x64/NTFS, PowerShell. No Git metadata exists. Source owner is `devforgeai/experiments/codex-worker-probe`; no operational package is selected. Authorization is the current user objective selecting implementation from `docs/plan/framework-worker-native-continuation/20260916T034357Z-source-identity/native-diagnostic-handoff.md`.

`selected_evidence_value` and `resolved_evidence_root`: `C:\Projects\DevForgeAI\docs\plan\framework-worker-diagnostics\20260916T181820Z-dev`. Selection source: repository AGENTS.md requires distinct new run evidence under docs/plan; dev context reference permits choosing an established suitable evidence location. Components were compared before first write; no renaming or alternate checkout. All records below resolve beneath this exact root.

Outputs: context.md (this record), diagnostic-contract.md (closed observation contract), baseline-manifest.json/baseline-snapshot (original candidate), input-bindings.json (selected handoff/instructions/companion contracts and retained native evidence), attempts/* and executions.jsonl (actual commands and candidate hashes), candidate-manifest.json/candidate-snapshot (final frozen delivery), coverage.json and coverage-analysis.json, delivery.md (traceability and results), qa-handoff.md, checkpoint.md, final-readback.json.

Inspection: original 56-file package, no nested AGENTS.md found under devforgeai. Original behavior reused: Session::rpc owns process-count guards, effective_profile::validate_config owns config predicates, runner owns source rechecks and cleanup, Journal owns durable worker_event records. Add typed observations at these sites, not a second policy evaluator. Existing launch policy, request validation, identities, deadlines and cleanup remain authoritative within this experiment; diagnostics never authorize work or acceptance.

Tools discovered: C:\Users\bryan\.cargo\bin\cargo.exe (cargo 1.97.1); rustc 1.97.1; cargo-llvm-cov 0.8.4; C:\Program Files\Python310\python.exe (evidence recorder only). Cargo commands use --locked --offline. No dependency installs. Build artifacts use this fresh evidence root. Child-only evidence/target-directory variables are recorded; credentials and parent environment are unchanged.

Requirements/slices: D1 derives closed contract before production edits (handoff item 1); D2 adds source, RPC process-count and config diagnostics with behavioral Red/Green (item 2); D3 developer regression/negative/privacy/no-work/cleanup/deadline checks, format, Clippy, docs, build and >=95% floors (item 3); D4 freeze candidate and prepare independent offline QA handoff (current user selection). Native launch preparation/selection and diagnosis from a new native attempt (handoff items 4/5) are deferred to a separately selected task after independent QA PASS. No native preflight, installed source collection, model trial, operational edit, authority provisioning or predicate relaxation in this task.

Plan: contract and new failing black-box peer observations; minimum typed instrumentation; focused Green and additional source-check/accounting/config negative tests; full offline campaign; freeze/readback and QA handoff. No agent delegation selected. Developer checks are not independent QA.

Metrics declared before run: Windows-only; every original and added Rust test under --all-targets is required once, subfixtures conjunctive, no skipped/ignored passes. Preserve failed attempts without denominator inflation. Full first-party src/*.rs executable-line denominator, zero first-party exclusions, excluding tests/support/vendor/generated code only; >=95% executed lines and >=95% required cases independently, no waiver of mandatory failures. Branch coverage separately NOT_RUN unless measured. Native WN-01/WN-02 remain NOT_RUN; external framework acceptance NOT_EVALUATED.

Read-only discovery attempts before recorder: one PowerShell brace-expansion parse error (no commands executed), corrected read-only discovery succeeded. Memory keyword search found no relevant registry entry; no memory-derived facts used.
