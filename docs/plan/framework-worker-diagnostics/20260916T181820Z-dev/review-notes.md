# Development review and retained attempts

Baseline matches all 56 historical candidate file hashes. Production diff review: the fixed launch vector, executable/source identity records, review predicates and all-false versus all-true semantics are byte unchanged. Config checks retain their original order, predicates, errors and safe success projection. Source checks retain the same verifier calls and order. Process checks retain total==1 AND active==1 using a single held-job query. Cleanup, deadlines, interruption, dispatch and terminal code mapping are not relaxed. New evidence writes fail closed.

Retained failures: 01 is the behavioral Red (three absent diagnostic assertions, original rejection checks passed). 02 is a test-oracle failure: Windows added a console helper to the transient child, actual counts 3 total/2 active rather than the assumed 2/1; new exact-count case uses a detached synthetic descendant, preserving the old test unchanged. 03 passed that corrected fixture. 05 is test setup: a null native profile failed invalid_profile before source review; 06 supplied a structurally valid profile with a missing review and passed the intended real profile_unqualified path. No native attempt was consumed. An atomic multi-file patch was rejected because README context did not match; readback showed no partial edit, then the corrected patch was applied.

Test-integrity search found no ignored/should_panic/mock/coverage-off cases. Added expected diagnostics are literal closed objects; config tests have independent literal error/category tables. Peers execute real local processes, pipes and Job Objects. Private source-review tests distinguish real verifier denial, helper instrumentation and public Session dispatch; they do not claim native runner execution. Generic query_failed handling is reviewed but an OS Job Object accounting-query failure is not forcibly induced. Every first-party executable source file remains in coverage.

Review finding RC-01: the initial fallback RPC category transport_error can include unsupported_request/approval_required or evidence errors. It must become other_rpc_failure rather than falsely attributing an unclassified RPC failure to transport. Original result strings remain unchanged. Preserve candidate-manifest.json and attempts 01..15, then use a fresh candidate-v2 manifest and fresh final validation attempts. Add a peer server-request subfixture and retain a behavioral Red before changing the label. This is a diagnostic correction within the selected scope, not a policy change.

Environment detail: native Windows 10.0.26200 x64, PowerShell 7.6.6, cargo/rustc 1.97.1, rustfmt 1.9.0, Clippy 0.1.97, cargo-llvm-cov 0.8.4. System.IO.DriveInfo reports NTFS for C:. Get-Volume was access denied and is not claimed as successful. No privilege or installation change was needed. All Cargo arguments, working directories, scoped environment overrides, candidate hashes, timestamps, durations, exits and stream hashes are retained in attempt receipts.
