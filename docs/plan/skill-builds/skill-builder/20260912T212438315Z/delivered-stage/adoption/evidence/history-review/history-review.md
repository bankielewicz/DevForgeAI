# Independent target history review

Conclusion: **No governing generated or published adoption baseline was found for `C:/Projects/DevForgeAI/src/agents/skills/skill-builder`. First adoption is appropriate under the current exact 36-path user authorization**, subject to the separate origin-spec fidelity review and actual adoption capture, evaluation, publication, and readback. This review performs none of those operations.

The assessment's `origin-record.json` identifies observed history and explicitly distinguishes enhancement verification from generated/adopted provenance. Its `handoff.json` requests adoption because that baseline was absent. The current authorization resolves the old validation-only permission limitation; it does not convert the enhancement receipt into an origin.

The initial `skill-builder-authoring-log.md` describes four-file authoring with validation not performed. The enhancement implementation log's “baseline” is a retained source snapshot for its implementation comparison. The later adoption-enhancement `REPORT.md` and `FINAL-RECEIPT.json` report development enhancement verification and independent synthetic workflow trials. Their receipt status is `DEVELOPMENT_ENHANCEMENT_VERIFICATION_COMPLETE`, not generated/adopted target provenance. All 14 references in the selected final receipt were rehashed and match. Exact locators, sizes, and hashes are retained in `history-review.json`.

The safely enumerated `docs/plan` history contains 428 readable JSON records whose names contain provenance, pointer, adoption-record, or baseline. None has `target_name: skill-builder` or a top-level exact target-root string. Named target identities are `decimal-ledger-import`, `decimal-ledger-spec`, `receipt-summary`, `receipt-summary-spec`, `receipt-totals`, `skill-validator`, and `synthetic-total`. The independent adoption report explicitly identifies its TEMP `receipt-totals` project. Two malformed pointers belong to retained synthetic-total A16 publication-failure fixtures, matching the failure report's actual 37-byte interrupted publication. They do not describe a failed adoption of this target.

The rename receipt explicitly says changed historical evidence is not valid as an unchanged sealed baseline and preserves original hashes/archive identity. That disclosure is retained and not repaired, ignored, or promoted. No corresponding successful or failed generated/adopted origin for the exact target was located; synthetic historical origins do not govern it. The current first-adoption workflow must preserve all bytes, record unknown historical origin, preserve the known stale summary defect, and retain this earlier history by reference.

The inventory enumerated 3,662 directories and 13,925 files, checking directory entries before descent and excluding backup/legacy names, reparse links, and the current active build run before reads. `history-review.json` contains the complete record inventory, skipped boundaries, parse errors, selected evidence digests, and reference readback. Its SHA-256 at report creation is `1a1b34a13e594e5308d872c0d335aa8c8579ec0d6810a6f47e8d41d546a586f9`.

Limits: this is an independent read-only local-history assessment, not a proof about undocumented external history. It did not inspect remote, WSL, personal, operational-copy history, excluded backups, or the external rename archive. JSON discovery was filename-directed; selected contextual reports were reviewed separately. No historical trial was rerun or quality acceptance established. No target or operational files were written. Only these new review artifacts were created.
