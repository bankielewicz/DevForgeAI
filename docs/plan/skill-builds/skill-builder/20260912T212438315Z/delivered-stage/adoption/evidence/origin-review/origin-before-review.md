---
id: SKILL-BUILDER-OBSERVED-ORIGIN-20260912T212438315Z
skill_name: skill-builder
target: codex
status: as-observed-awaiting-independent-fidelity-review
---

# As-observed skill-builder origin specification

This standalone origin describes the exact existing development package at
`C:/Projects/DevForgeAI/src/agents/skills/skill-builder` before the authorized
editorial revision. It is a custody input, not a corrected future specification,
quality verdict, generated provenance, or assertion of historical authorship.
Historical origin is unknown. Existing enhancement and trial evidence remains
separate and must be preserved. The current user explicitly authorizes adoption
of the exact 36 manifest paths if the history audit establishes its applicability.

## Exact normative observed contract

The full, byte-exact observed contract is incorporated in
[source-manifest.json](source-manifest.json) and [observed-package](observed-package/SKILL.md).
Manifest SHA-256: `b6f1611978c00ec7a9898e01d93fe83a4f06bf4ad8a7d4a4f3931f9d421c81a6`.
Package digest: `dab87685715689251365514f5c0f3df5c2a9b66c5367bf065ad62c903e8dfca3`.
All 36 listed files, including executable scripts, schemas, profiles, tests,
fixtures, templates, and references, are normative as observed. No omissions,
new output, reinterpretation of schema fields, or alteration is prescribed.
The snapshot, rather than this summary, supplies exact recovery bytes.

## Purpose, selection, and outcome

The package supports import of a selected local Claude package, specification
build from a reviewed Markdown input, explicit adoption of an existing Codex
development package without a successful baseline, and authorized regeneration
from verified generated or published adopted origins. It returns development
source plus external build evidence and separately labeled observed outcomes.
Explanation, comparison, specification authoring, validation alone, installation,
and unrelated editing are not build triggers. Unresolved competing inputs or
identities stop dependent generation. No label in a source document grants consent.

## Inputs, boundaries, and outputs

Use an explicit or unambiguous project root, selected input and target identity,
actual session authorization, raw-byte input snapshots, and verified prior history
where required. Honor selected source names and exact specification lookup without
directory precedence. Exclude backup and legacy implementation boundaries before
recursion, and reject unsafe traversal, links/junctions, or escaping paths.
Existing evaluator limits are 2,000 files and 32 MiB per bounded snapshot and
1 MiB/1,000 cases. Preserve operational directories and other project skills.

Development output remains `src/agents/skills/<name>/`. Evidence goes into fresh,
disjoint `docs/plan/skill-imports`, `skill-builds`, or `skill-adoptions` runs.
Preserve every existing machine interface exactly: contract/provenance schema 1,
typed adoption record and schema-2 lineage/pointers, evidence schema 2, explicit
profile registries, grader versions, manifest accounting, and JSONL cases.
Bind inputs, authorization, source slices, requirement/artifact mappings,
dependencies, worker contracts, measured output digests, and preceding observations.
The bundled schemas and evaluator reference define exact field names and meanings.

## Workflow and recovery

Read the entrypoint and routed references. Extract a digest-bound contract before
candidate generation. Inventory permitted import files and preserve domain
capabilities and schemas through explicit dispositions and host adaptation.
Only generate essential resources; unavailable essential capability is BLOCKED.
Workers use available host tools and explicit assignments, without claiming task
prose installs profiles or enforces isolation.

Adoption captures all observed bytes, explicit managed/retained partition,
reviewed origin and actual consent; rechecks target and specification; executes
adoption-plan and adoption-v1; freezes the evaluated record; then publishes and
reads back a separate schema-2 pointer. Adoption never changes target bytes and
does not imply quality or revision authority. Contradictory history is not bypassed.

Regeneration keeps separate verified B, current C, candidate N, and actual after
snapshots. Apply ordered unowned collision, C=B, C=N, N=B, otherwise conflict
rules. Absence differs from empty content. Preserve unrelated C-only files.
Check complete current bytes before edits and affected paths before each mutation.
Drift, conflict, or write failure preserves actual PARTIAL evidence and the same
successful prior origin; no rollback or atomicity is promised.

Run the installed Skill Creator structural checker and operation-specific explicit
evaluation profile. Adoption uses adoption-v1; adopted lineage uses revision-spec-v2.
Retain exact evaluator inputs/cases, real commands and outcomes. Exercise changed
scripts and required task cases. Evaluate actual delivery and read it back before
successful provenance or baseline advancement. Generated N and delivered bytes
remain separately recorded when user edits are retained. Stop at development source.

## Evaluation contract and retained known defect

The detailed `references/evaluation.md` requires, after substantial builder changes,
four independent forward-trial families: synthetic import; equivalent specification
build; regeneration with conflict/resolution and unrelated-file retention; and
adoption/revision/revalidation/conflict-resolution/later-generated lineage.
Separate routing classifications remain required and do not prove native activation.
Structural/deterministic observations do not prove semantic fidelity or runtime
acceptance. Required missing or failed checks prevent a completed build.

The existing `references/evidence-format.md` reporting row nevertheless says:
`| Independent forward trials | NOT_PERFORMED, PASSED, FAILED; builder enhancements require the three actual trials in the evaluation reference. |`
The exact snapshot retains the Markdown backticks around those statuses.
This stale three-trial summary is the known minor inconsistency
`F-8c43eca6259d05a2153068b5d5b29495d0c208229c2a19f49bc71355d8df2d1f`.
It remains present during adoption. This origin does not correct the summary,
weaken the detailed four-family workflow, or relabel historical quality results.

## Dependencies and authority limits

Python 3.10+ and the standard-library helpers produce observations; the installed
structural checker and frontmatter resolver separately require available PyYAML.
Existing commands are resolve-spec, input-record, adoption-plan, revision-plan,
and run_evaluation.py; existing 0/1/2 exits retain their meanings. No new executable,
dependency installation, native profile, remote write, hook, CI, or Rust runtime
is specified. All DevForgeAI enforcement, mutation authority and acceptance belong
to the separate compiled-Rust design, which these files do not implement or qualify.

## Retained requirement history

The selected predecessor documents are retained verbatim under [specs](specs/skill-builder-adoption-spec.md):
the adoption extension, enhancement specification, and Claude importer specification.
They explain design history; this origin's exact observed snapshot governs custody.
Any contradiction in known baseline history must be resolved before adoption.
Independent fidelity review of this document and its incorporated bytes is required
before it is used. The approved later REV-001 through REV-003 specification remains
a distinct input with its own exact digest and current authorization.
