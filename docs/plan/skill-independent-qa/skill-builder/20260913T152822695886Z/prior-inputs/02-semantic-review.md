# Bounded semantic assessment

Assessment owner: the explicitly selected existing skill-validator instructions, subsequently pinned to the original development bytes in assessment-owner/skill-validator. The primary maintenance author performed this semantic review; independent-review.md identifies the narrower independent work. This is not an independent complete validation or an enhanced-validator integration run.

SKILL.md routes propose, author_set and review_updates to adaptation.md, schema/descriptor details to adaptive-contracts.md, and runtime prerequisites to project-binding.md. Existing create/edit/import/adopt/specification routes and metadata remain. Ordinary authoring ends with manual validation-request-v1 and NOT_PERFORMED quality states. The two new helpers inspect custody/binding; neither generates a quality verdict or executes application commands. Existing legacy helpers and schemas are byte-identical.

Proposal instructions enumerate before descending, exclude operational identity and dependencies, bound source capture, preserve unknown facts, and require supporting locators. Existing coverage is retained. For BAT-04's storage/HTTP case, the specified decision is to retain a verified HTTP responsibility and propose storage only from actual storage requirements; no native completed proposal demonstrates that decision in this run. Expertise/domain support and cited authorization still require semantic inspection, beyond the helper's valid-reference check.

Selected-set instructions preserve the full immutable selection, preflight capabilities/destinations/history, process one member at a time and continue independent members after failure. Machine checks cover dependency closure, deterministic order, actual per-member custody publication, exact role/parent binding, reductions and full/subset omissions. There is no generic workflow dispatcher. BAT-10's harness invoked actual per-member authoring custody but scripted the controller; native autonomous set orchestration remains unverified.

Core/variant identity is separate. Parent requirements must be explicitly inventoried; older core Markdown tables are supported without adding an adaptive descriptor or changing core. Full dispositions and current removal authorization are required; matching a cited ID is not proof of consent. Update review maps supplied current inputs and keeps original parent evidence. Equivalent semantic behavior after byte change is reported as changed input, with no automatic rebase or carry-forward validation.

The runtime template is stdlib-only and read-only. It checks root, complete package bytes, selected role and core/variant group ambiguity, and emits sanitized reason codes. Concrete synthetic UUIDs occur only in disposable operational fixtures. Instructions require a fresh check before product effects and after resume/change. A helper MATCH is explicitly an observation; neither it nor prose is a privileged gate. Product-write suppression in an actual generated-skill cold task has not been observed.

All new resources have concrete consumers and no empty scaffold or evaluation campaign was added to ordinary generated skills. Conditional details are routed instead of duplicating the custody manual. Repeated preservation clauses are useful instructions because they identify specific writes and failure exits; no context-count metric or ceremonial self-score was introduced. No supported metadata fields were removed, and no mandatory GUI, MCP, plugin service or imagined Rust transition was introduced.

The final source scan found no concrete UUID literal, original developer/installation-root constant or copied binding record. Generic schemas legitimately name project_id. Unknown/missing-interpreter behavior is instructed, but a cold host with Python removed was not run. Network-free operation is supported by stdlib/local-only call sites and observed local execution; no OS network-isolation qualification is claimed.

Current semantic result: delivered branches are mapped with no remaining known material implementation contradiction from this review. Behavioral verification remains incomplete; see case-coverage.md. No claim of current-live OpenAI standards coverage, installation, framework acceptance or Rust enforcement is made.
