# Independent plan, frozen before source review

Target: only src/agents/skills/skill-builder, digest 338c70995e72637e0ce84991be110d6a371ab6965faf4009d570156ea0e13cf2 (43 files, 276781 bytes). Both selected specification hashes match. Expectations derive from the complete builder specification and companion shared-interface/ownership clauses; prior assessment conclusions have not been read.

All commands use a 120-second limit, including child termination on timeout. All fixture/output writes stay under this run. Python uses -B -X utf8. Raw output, arguments, start/end, exit/timeout, fixture hashes and expected outcomes precede assessment. No validator instructions, imports, tests or graders. No changes to either package, operational copies, specifications or prior evidence. No production red/green/refactor cycle: this is an independent audit, and defects remain unrepaired.

## Acceptance work packages and independent oracles

| Case | Compound observations required |
|---|---|
| BAT-01 | Cold ordinary create and subsequent focused edit; authorization reuse; no adaptive requirements on ordinary skills; no quality/checker/test/validator execution; exact requests/readback; NOT_PERFORMED quality |
| BAT-02 | Monorepo root instructions, two languages, unknown manifest, excluded dependency tree, junction, secrets/operational exclusions; supported citations/unknowns; no project execution |
| BAT-03 | File limit and byte limit independently, aggregate/shared-original accounting, missing Python child environment, required nonexistent Rust authority member; independent prose retained, dependent BLOCKED, no installation |
| BAT-04 | Existing HTTP responsibility retained; observed storage facts justify separate expertise; source-backed assertions; redundant role rejected |
| BAT-05 | Three parent requirements, one changed and one unauthorized removal; missing/invalid disposition blocked; authorized complete map accepted; core bytes preserved |
| BAT-06 | Generated adaptive descriptor/helper/contract; relocation; no source UUID/absolute runtime root/binding copy; no installed identity prerequisite |
| BAT-07 | Correct binding; absent; invalid/duplicate/unknown version; wrong root; unbound; inactive; changed package; wrong role; selected core plus variant; two variants; unsafe path; file/byte limits; I/O; invalid descriptor. Exact status/reason/exit, no UUID, product effects refused on non-MATCH and resume |
| BAT-08 | All seven new record families plus two observations; valid and malformed: duplicate keys, nonfinite numbers, booleans as integers, unknown/extra fields, invalid IDs/Names/paths/digests, local references, unresolved IDs, stale refs, linked records; source unchanged |
| BAT-09 | Unchanged authoring-contract-v1 and validation-request-v1; arbitrary legacy requirement objects preserved; isolated adaptive envelopes; schemas/readers compatible |
| BAT-10 | Selected A fails, transitive B blocked, independent C authored; exact order, partial paths, retained versus authored, aggregate reductions, eligible subset closure and omission/handoff partition; autonomous cold orchestration separately observed |
| BAT-11 | Dependency/handoff cycles, unknown/unselected dependency, colliding/occupied destination, user-edited obsolete file, changed input before publication, existing corrupt custody; no overwrite or dependent changes; preserve partial writes |
| BAT-12 | Unlabeled proposal/create-set/update-review positives and install/test/spec-writing near misses; description-only review distinct from explicit cold and native implicit discovery; manual handoff |
| BAT-13 | Byte-identical parent, byte-changed semantically equal parent, materially removed obligation, missing parent; explicit impact proposal; no auto-rebase or quality carry-forward |
| BAT-14 | Python/pytest/TDD; Rust/Cargo/implementation-first; service-local TypeScript workspace; docs-only/no build; contradictory same-scope convention; facts/tool availability separated, no uniform invented style |
| BAT-15 | Windows and POSIX specifications; spaces, Unicode, shell syntax as argv; missing interpreter in child only; offline helper; no cache/network/install; available host execution separately labeled |
| BAT-16 | Evidence changed before resume; stale ref rejected; fresh linked proposal/current selection required; check again after binding/package changes |
| BAT-17 | Import, observed first edit, authored baseline, legacy generated baseline, legacy adopted baseline, corrupt known history; old COMPLETE unchanged; no invented adoption; B/C/N safety |

## Complete-spec clauses outside BA table

Review sections 1/1.1 ownership and preserved modes; 2 role responsibility/redundancy/no fixed count; 2.1 identity; 3 capabilities and local-only constraints; 4 metadata/mode selection/authorization; 4.2 root selection, discovery exclusions/counting/unknowns; 5.1 every primitive/record field and parser semantics, manifest canonicalization, run IDs; 5.2 fact/requirement citations, complete lineage including older-core inventory, graphs/state; 5.3 selection preflight and all result/request reductions, references and manual publication; 5.4 descriptor identity/resource consumers and executable completion; 6.1 setup boundaries/groups/root normalization; 6.2 runtime exact interface/outcomes/rechecks; 7 update semantics/history; 8 every compound BAT and platform requirement; 9 actual paths/public CLI/legacy compatibility; 10 factual capability evidence and completion limitations. Expand to line-level traceability after first-pass review, without treating keyword presence as semantic proof.

## Coverage interpretation

PASS requires adequate evidence for all applicable subcases. Confirmed FAIL overrides NOT_RUN but does not erase it. Scripted helper control is not autonomous workflow coverage; native timeout is unavailable coverage, not proof of a product defect. Companion consumption is explicitly outside scope. Native installation and Rust qualification are not performed. Required framework line coverage >=95% and required-case pass rate >=95% will be reported as measured or NOT_RUN, never inferred from helper success.
