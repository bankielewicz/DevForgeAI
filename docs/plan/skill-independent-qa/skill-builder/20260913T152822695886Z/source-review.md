# Independent source and workflow review

Target digest: `338c70995e72637e0ce84991be110d6a371ab6965faf4009d570156ea0e13cf2`. Review used the complete selected specifications, all 43 current package files, independently generated fixtures, and later a limited comparison with prior evidence. No validator instructions, implementation, imports, tests or graders were used. Local schema files were systems under review, not the oracle for required behavior.

## Connected workflow and effects

`SKILL.md:7-24` selects ordinary authoring, explicit specification, import, existing-history revision, explicit adoption and three adaptive modes. Destination questions are conditional on missing information; a supplied destination and authorization are reused. The recommendation is resolved from the selected project and permits another development directory. The ordinary path is `references/authoring.md` → `scripts/authoring.py begin` → candidate edits → `publish` → manual request. This permits untested further authoring and prohibits quality execution. Direct helper trials confirm publication and subsequent revision work without importing a validator. The cold task reached staging but did not complete; no entire autonomous workflow PASS follows from helper success.

`authoring.py:151-238` resolves observed/authored/generated/adopted origin. `begin:241-302` captures contract/inputs/current package and creates a candidate. `publish:305-455` verifies source inputs, B/C/N ownership and planned paths, checks before each write, captures delivered bytes, and publishes baseline/request/readback. Divergent edits and user-edited obsolete files were blocked; N=B retained the user's C; unrelated observed-edit files remained. Known corrupt generated baseline bytes were rejected. F-02/F-04 identify actual missing top-level/pointer checks. An external edit before publication is retained, but PARTIAL with no builder-applied paths merits D-01 clarification. Mid-write interruption, all publication fault seams and complete schema-2 generated-after-adoption chains remain unperformed.

`references/adaptation.md:7-21` specifies read-only bounded discovery, unknown facts, distinct responsibilities, existing coverage, project conventions and local capability gaps. There is no universal discovery dispatcher. The agent must enumerate/capture and construct factual records. `adaptive.py:115-175` verifies retained refs and exact package manifests; `evidence` and `proposal` check IDs, reference spans, lineage, graph and proposal-state consistency. Passage meaning, domain sufficiency and removal consent remain semantic duties; an ID in text is never counted here as authorization. Four convention fixtures were created and invoked, but every cold attempt timed out. Missing interpreter was simulated in a disposable child; that launch error does not prove a completed prose proposal or correct dependent-member continuation.

`references/adaptation.md:21-38` selects immutable members and authorizes no automatic dependency addition. `adaptive.py:353-387` preflights order, destination/name/overlaps, capabilities and input gaps. `set_authoring:402-451` and `set_request:453-476` verify selection membership, results, role/parent bindings, reduction and requested omissions. A real C package was published through builder custody after A's occupied destination; the independent controller supplied B's blocked result. Correct PARTIAL/eligible-subset records were accepted; false full-set and omissions were rejected. This is scripted orchestration. F-01 shows the linked legacy field reader checks only top-level key membership and a subset of cross-bindings, allowing malformed requests to pass despite valid hashes. No automatic full-set continuation or integration claim is made.

`adaptive.py:177-211` locates explicit requirement indexes in captured Markdown, normalizing CRLF before parsing. Independent three-requirement fixtures passed as LF, CRLF and an older-core table in another resource; missing rows and removal without user linkage were rejected. Core bytes were never edited. The descriptor branch checks name/resources and occurrence of parent IDs; occurrence is a locator check, not proof of retained semantics or a complete disposition map. Semantic variant construction/authorization and wrong parent-role scenarios remain incomplete.

`references/adaptation.md:40-44` and `adaptive.py:338-351` distinguish changed bytes from defects and permit NO_CHANGE only with the relevant unchanged inputs/history. Independent unchanged/equivalent-byte-changed and false-NO_CHANGE cases behaved as expected. A stale selected manifest was rejected. No material-removal impact proposal, background update mechanism, automatic rebase, or carried-forward quality result was observed. Native changed-input resume remains NOT_RUN.

`assets/adaptive-runtime/check_project_binding.py` is standard-library-only, invokes no shell/network/project code, and writes no output files. It validates operational root, loaded package identity, digest, selected state and active implementation groups. Native Windows/POSIX cases verify ordinary outcomes, argument safety, exact byte ceilings, related descriptor errors and junction/symlink refusal. Windows file ceilings passed; Linux file ceilings timed out. F-03 covers omitted private-key filename exclusions. The entrypoint instructions require fresh runtime checking after change/resume and no product effects after non-MATCH; helper exit status alone does not prove the agent follows that boundary. Real binding setup/installation and protected Rust authority are outside scope.

## Resource consumers and metadata

- SKILL.md explicitly routes all 11 focused references. The three adaptive references route the new closed schemas and copied runtime template before adaptive staging.
- `scripts/adaptive.py` dynamically loads only the bundled binding template and local authoring module. It resolves shipped local schema definitions, not remote JSON Schema references. The manual legacy reader is incomplete as F-01 demonstrates.
- `scripts/authoring.py` imports `custody.py` only for selected historical paths. `build_evidence.py` provides real resolve-spec, input-record, revision-plan and adoption-plan interfaces; it does not write destination files or import graders. The old schemas describe historical evidence rather than new quality authority.
- `init_skill.py` imports the bundled metadata helper; `generate_openai_yaml.py` imports local custody path/read helpers and installed PyYAML as needed. Fresh initialization refused an occupied directory; focused UI updates preserved unrelated policy/dependencies/color. Placeholder text in initializer templates is intentional authoring scaffolding with an explicit finish/remove instruction, not an unfinished production claim.
- Build/conversion reports are optional authoring templates. The worker-task template is conditionally used only when a selected skill requires workers; it does not require delegation for this audit. Its brackets are template inputs. The source notice links the preserved Apache license. No resource is classified as orphan solely because it is not a runtime import.
- `package-manifest.json` exactly matches the other 42 files. All local inline Markdown link targets resolve. Schema definitions/field paths were independently inventoried in `schema-review-inventory.json`; syntax/field agreement alone is not workflow acceptance.
- No concrete framework UUID, author-machine installation root or copied operational binding was found in the selected package. Generic project_id/schema fields and example API URLs are allowed interface text, not executed network calls.

## Description-only routing review

| Unlabeled task meaning | Manual classification from description | Reason |
|---|---|---|
| Recommend project-specific skill responsibilities | Match: propose | Description names project adaptations |
| Author explicitly chosen proposal members | Match: author_set | Description names selected skill sets |
| Review a variant after its core changes | Match: review_updates | Description names variant updates |
| Create or edit an ordinary skill | Match: ordinary | Creation/editing and conversational tasks explicit |
| Install the package in active skills | Near miss | Installation is a separate task |
| Run skill-quality tests only | Near miss | Testing/validation are separate tasks |
| Write only a standalone specification | Near miss | Standalone specification writing is separate |

This is manual semantic classification by the current auditor, not a separate model classification or native implicit selection event. The package has no agents/openai.yaml override; the enhancement preserved the existing absence. Native implicit discovery remains unverified. This audit makes no refreshed universal OpenAI metadata conformance claim.

## Prior evidence comparison

The prior delivered receipt explicitly said verification INCOMPLETE and integration NOT_RUN. Its semantic review accurately distinguished scripted set control, runtime observations and missing native product-effect trials. The independent reviewer did not certify the final adaptive.py hash. The later compatibility report targeted validator differences; those verdicts do not become builder findings here. Independent current tests corroborated the builder's LF/CRLF and older-core inventory paths without executing those prior test sources.

The prior adaptive test source was read only after independent planning/source review. Its tests cover many ordinary shape/runtime cases but have no individual adversarial linked legacy-field grid or private-key basename/container rejection case. A matching schema hash or previous passing suite cannot establish those behaviors. Seven selected historical files were retained with hashes for comparison; full historical trial trees and excluded validator packages were not captured.

## Audit method limitations

This is an independent audit by the primary Codex agent with its own fixtures; no subagent or builder self-verdict is used as the oracle. Native child tasks inherited the user's host configuration and are explicitly labeled. Bootstrap terminal output is available in the conversation but not all of it was transcribed into an artifact before execution. Some initial helper cases lack full pre-execution fixture/effect manifests, and this limitation is retained in E-01. Required framework acceptance, native installation, enhanced-validator consumption, and certification are not claimed.
