# Independent QA preparation

The governing specification and requested audit determine expected outcomes before implementation inspection. This run uses no validator instructions, scripts, tests, graders or verdicts. The copied companion specification supplies shared interface and ownership context only. Prior conclusions are not test oracles.

Source-dependent execution is paused until the maintaining parent supplies a finalized source digest. Source capture will enumerate without following links, reject special/reparse entries, enforce 2,000 files and 32 MiB, and hash sorted relative-path rows with key order path/bytes/sha256 using compact unescaped-Unicode UTF-8 JSON. Final source/specification readback will compare the same manifest.

`requirements.json` records BA-001 through BA-014. `cases.json` splits every BAT into compound subcases. `complete-clause-inventory.json` retains all specification prose/table lines outside BA/BAT; semantic review will map applicable obligations and give specific nonnormative/not-applicable dispositions rather than silently dropping them. These are plans with NOT_RUN results, not acceptance claims.

Independent helper fixtures will invoke inspected builder helpers through argument vectors on Windows Python and Ubuntu Python. Positive fixtures establish usable shapes before single-defect negative mutations. Binding cases cover every named reason, output shape/exit codes, no UUID leakage, resource paths, unrelated selected descriptors, count/byte bounds, safe hostile names and read-only effects. Binding-only results do not establish model no-product-write behavior.

Cold workflows receive only the selected source entrypoint, raw local documents, requested outcomes and their disposable write scope. Expected answers, grader labels and intended fixes remain outside prompts. Native case workspaces live under this run's `native/`. Explicit invocation and implicit discovery are separately labeled. A disposable `.agents/skills/skill-builder` copy may support implicit discovery; it is not operational installation. Each attempt has an independent project, prompt, before/after manifest, JSONL stdout, stderr, final message and 120-second process-tree bound. Retried attempts use a fresh directory and reason.

Cold case groups: ordinary create then focused edit; monorepo/exclusions/discovery limits; HTTP retention and storage proposal; variant lineage; adaptive source relocation; partial A/B/C selected set; routing positives/near misses; unchanged/equivalent/material/missing update sources; four project conventions; changed-evidence resume; import/first-edit/known-history custody. Helpers supplement but never substitute for autonomous orchestration evidence.

Host discovery found Windows Python 3.10.11 and Codex CLI 0.154.0. Normal WSL enumeration was initially denied; the normal escalation succeeded and enumerated Ubuntu. Read-only Ubuntu capability discovery reported Linux 6.18.33.2-microsoft-standard-WSL2, Python 3.12.3, Codex CLI 0.154.0, and `/usr/bin/timeout`. No configuration changes or dependency installation occurred. Inherited hook/control inspection must precede native execution. A host restriction remains retained evidence; no bypass flags are authorized.

Coverage denominator for executable helper coverage is all first-party builder Python files in `scripts/` plus the portable Python runtime template in `assets/adaptive-runtime/`, excluding generated synthetic artifacts and independent harness code. Availability of an existing coverage collector will be checked; no collector is installed. Executed-line/branch coverage, required-case pass rate, semantic review, native workflows, enhanced-validator consumption (excluded), installation (not authorized) and Rust qualification (not implemented by this task) remain separate dimensions.
