# Execution admission and deviations

The user's explicit execute instruction supersedes the saved plan's planning-only authorization. The exact plan/spec/candidate hashes were verified with no drift before staging. binding.json retains literal local/remote evidence, candidate and scratch paths. Root selected before writes: C:/Projects/DevForgeAI/docs/plan/index-service-qa/20260914T184442947070Z/executions/20260914T191133863776Z. Remote source and selected local spec are bound; the stale remote spec did not govern execution.

12 independent Rust tests (7 unit/5 integration) plus9 native CLI acceptance cases were authored in QA locations;18 total declared component unit cases =11 existing+7 independent. Source48 files copied byte-for-byte; only tests/qa_independent.rs added to the QA copy. Product and developer tests unchanged. The native CLI scripts act as evidence drivers, never framework authority.

Native UI/process effects were authorized by the user and escalated for Windows DACL/GUI capability. WSL and startup/systemd unperformed by selected scope. Cross-account fixture not supplied. All other uncompleted acceptance subcases are explicit in report/acceptance-results.json; no full-plan completion claim.

Before tests: product executed-line denominator all compiled src files, excluding test/example/fixture and third-party code. Both unit and line floors95%, plus project required-suite rate. No zero/missing denominator treated as100%.

Retained attempts: Windows collector option error no product tests; fixed collector002 ran all targets and recorded failures. Ubuntu collector initial missing default tools used installed LLVM21.1.8 in002, no installation. Remote short temp root /tmp/devforgeai-qa-20260914T191133863776Z was bound before native tests to avoid Unix socket path-length setup errors; candidate/output remained in selected native checkout QA subtree. No remote test needed retry after executing. Windows Clippy initial QA-helper needless clone corrected without expectation/behavior changes; coverage v1 source retained and static helper v2 separately bound. Only evidence/helper code changed, not product.

Exact commands/cwd/env/times/exits/stdout/stderr digests are in attempts/*/receipt.json and Ubuntu equivalents. native-acceptance/commands.json binds CLI actions/input. Figures calculated from raw reports; ignore-run-fail only preserves coverage. No product retry erases failure. Final source/binary/process readback in windows-completion.json and ubuntu/completion.json; original plan artifacts preserved. SSH archive SHA256294371e6c8111cdb5f266bed000cb021eb8b7eb9f4fc1878c89e10119b7c4e24 verified after transfer. Secrets not saved.

Remaining QA prerequisites are documented gaps. This FAIL report and proposed dev handoff complete the bounded executed assessment; they do not claim full scenario/platform acceptance or authorize repair by QA.

