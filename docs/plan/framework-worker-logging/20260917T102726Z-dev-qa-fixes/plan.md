# Two-defect development plan and traceability

Selected scope: QA-LOG-01 and QA-LOG-02 from the verified qa-fix.md. Source changes belong only to the new sibling candidate. The existing inspector owns journal interpretation; Capture::validate owns shape and conditional EOF/error validation. Retain typed capture/post-stop state instead of adding another evidence writer or changing live capture. Public CLI/library tests use fresh real successful peer runs; preflight success branch tests use explicitly synthetic record fixtures and never native Codex.

| Slice / source requirement | Required behavior | Planned implementation and checks |
| --- | --- | --- |
| S1 / QA-LOG-01; LG-02/03/06; design case 12 | Current successful terminals require complete capture for both streams | journal.rs retains Capture through terminal. Seven independent mutations: drain false, either stream EOF false, either read error, either byte overflow. All four levels, completed/preflight branches and current schema 1/2/3 records. |
| S2 / QA-LOG-02; LG-02/06; base sections 4/7 | Mandatory post-stop field presence/type; successful exit agrees with terminal zero | journal.rs checks key presence separately from nullable value and reconciles successful terminals. Missing/null/nonzero/string/negative/overflow/bool and terminal mismatch controls. |
| C1 / handoff compatibility; LG-06; WF-09..12 | Sound failed/nonzero/null, interrupted and historical observations remain readable | New inspection_consistency tests cover actual blocked/failed/cancelled/timed_out/cleanup_uncertain runs, incomplete capture, explicit null exits, historical schemas 1/2, temporal pre-stop null and dropped detail. Every CLI inspect checks exact unchanged root bytes/membership, including peer trace. |
| R1 / LG-01..08; WF-01..20; NI-T01..10 offline; preflight; SI-T01..10 offline; diagnostic v1 | Preserve inherited admission, runtime, fault, cleanup, query_failed, persistence and privacy behavior | Full unfiltered package all-targets coverage campaign, inherited supplemental real Windows malformed-byte capture, formatting and Clippy. Existing private fault seams remain unchanged. |
| D1 / user literal output and preservation | Corrected source/build identity, changes, metrics, per-defect handoff; old bytes intact | candidate/build/changed-file manifests, source diff, preservation readback, resolution-map, delivery and sealed handoff at context.md's concrete destinations. |

Dependency order: establish fresh copy/test harness and real behavioral red for S1/S2; minimum shared journal correction; identical focused green; refactor review and affected rerun if code changes; freeze case inventory and candidate; full offline regression/coverage; publish read-back evidence. No architectural gap or external authority prerequisite affects these ordinary source/test writes. No refactor is required merely to add churn; record the assessed decision.

## Declared metrics before execution

Platform is native Windows x64 only. Required package set is all existing 152 cases (49 library units, 103 integrations) plus the 10 new integration tests in inspection_consistency.rs. The inherited supplemental Windows capture case runs against the corrected package in a fresh evidence-owned harness: 163 required development cases total, 49 required units. Confirm names/count through `cargo test --all-targets -- --list --format terse` before the final campaign; investigate any mismatch rather than dropping cases. Each table-driven test is conjunctive, including all listed mutations; repetitions do not inflate the denominator. Earlier red failures remain evidence of the original behavior and do not count as new final-candidate cases. No case may be skipped, ignored or silently retried.

Coverage denominator: all 15 Rust files directly in candidate src/, including journal, capture, process, CLI, request, protocol, source/identity and error paths. No executable first-party exclusion. Declaration-only lib.rs may report no executable lines. Exclude tests/support, test fixtures and dependency/generated third-party code only. cargo-llvm-cov JSON supplies actual line counts; independent evidence arithmetic recomputes all first-party totals. Both coverage and required unit/project-suite rates must independently be >=95%, comparing exact integer ratios. All mandatory cases must pass regardless of numeric floors. Branch coverage is separately NOT_RUN with installed stable tooling; no new tool installation. Native NI-T11/12, WN-01/02 and installed-profile qualification are NOT_RUN and outside this offline denominator. Independent QA remains separately selected; these checks do not close findings.

Bound commands: record.ps1 invokes discovered Cargo with locked/offline package manifest, fresh build/coverage targets, sequential tests and retained fixture roots. Focused red/green targets inspection_consistency. Formatting uses cargo fmt --all -- --check; static analysis uses cargo clippy --all-targets -- -D warnings; full measurement uses cargo llvm-cov --all-targets --json --fail-under-lines 95 with only tests path filtering. Time bound is 420 seconds, including the inherited real 120-second watchdog. All attempts retain source manifest, exact argv, environment overrides, stdout/stderr, timing and exit.
