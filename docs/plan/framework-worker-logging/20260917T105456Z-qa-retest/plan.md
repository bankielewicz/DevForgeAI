# Independent QA retest plan

## Identity, selection and authorization

Run 20260917T105456Z-qa-retest; intent **retest**, readiness **READY**, execution **NOT_STARTED**. The user selected the corrected candidate and QA-LOG-01/02, with a digest-bound development handoff. Proceed into authorized offline work after plan readback and integrity inspection; no further scope decision is needed.

Project: C:\Projects\DevForgeAI, native Windows x64 / Microsoft Windows 10.0.26200, PowerShell 7.6.6, native C: filesystem. No Git metadata. Candidate: C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe-logging-qa-fixes. All 68 current files match the delivered candidate; exactly README.md and src/journal.rs changed and tests/inspection_consistency.rs was added relative to the failed candidate. candidate-manifest.json binds the actual source; changed-files.json independently recomputes the diff. Cargo.toml/lock and all 15 src Rust files are bound.

Development handoff: C:\Projects\DevForgeAI\docs\plan\framework-worker-logging\20260917T102726Z-dev-qa-fixes\handoff-manifest.json; verified SHA256 1e4e0800a737f07d658a50383ccdf98c6aa5237c73675d2539ccb7689065f9f5. Claims 163/163 development cases and 3871/4057 lines. Those are inputs, not QA results. Both defect states are FIX_REPORTED; only independent evidence may establish VERIFIED_FIXED or REOPENED.

Exact eight governing contracts/clarifications are in specification-bindings.json, with absolute paths/hashes: logging-contract LG-01..08; logging-design acceptance 1..12; investigation temporal/privacy clarifications; worker feasibility v1; native-readiness offline slices; preflight companion; source-identity companion; diagnostic contract. Original QA report/fix/plan/manifests are bound inputs under 20260917T023835Z-qa. Current AGENTS.md and installed qa references/templates are unchanged and bound in input-manifest.json. Inspection of dependency contracts does not select native/authority implementation.

Allowed effects: isolated QA fixtures/harnesses, cached offline Rust build/test/instrumentation, read-only CLI inspection and evidence publication. Preserve product/developer test files, failed candidate, frozen original/snapshot, prior evidence and PowerShell helper. No dependency installation, native Codex, installed-profile collection, credentials/config changes, operational skills, startup changes, deployment or framework acceptance.

## Output and checkpoint binding

selected_evidence_value: C:\Projects\DevForgeAI\docs\plan\framework-worker-logging; established project evidence family. Fresh literal destination: C:\Projects\DevForgeAI\docs\plan\framework-worker-logging\20260917T105456Z-qa-retest. All relative outputs below resolve here.

- plan.md and external plan-binding.json; checkpoint.json tracks current intent/readiness/execution/defects/owned operations.
- candidate-manifest.json, preserved-manifest.json, input-manifest.json, specification-bindings.json, source-denominator.json and intake-verification.json.
- required-cases.json, case-results.json, criterion-results.json, findings.json, defect-lifecycle.json and metrics.json.
- attempts/unique-id/{launch,process,receipt}.json, stdout.txt, stderr.txt, fixtures; separate build-target, coverage-target, independent-target, supplemental-target.
- qa-harness contains a newly bound copy of the original independent helper, with only corrected-candidate paths changed; run_command.py and measure.py retain raw evidence and recompute results. cli_matrix.py adds independent CLI evidence interpretation cases.
- coverage.json, coverage-analysis.json, integrity.md, qa-report.md; qa-fix.md only on FAIL. handoff-manifest.json externally hashes final artifacts, then final-readback.json records literal-path verification.

Initial verification: 68 corrected files; 183 preserved failed/original/snapshot files; 108 inputs; 3,689 prior QA evidence files and 9,691 development evidence files verified with no mismatch. No product tests or owned worker processes exist for this run yet. Next: finish integrity, prepare bound helpers, build/inventory and execute the complete metric campaign.

## Acceptance inventory, risks and independent oracles

Every row below is mandatory in the selected offline scope. No unresolved requirement or advisory expansion is present. LD numbers refer to the original design's numbered acceptance cases; original WF/NI/SI identifiers retain their source scope.

| Criteria | Required behavior and risk | Cases / independent observation |
| --- | --- | --- |
| QA-LOG-01; LG-02/03/06; LD-12 | A successful current observation needs complete capture; both streams must have EOF, no reader error or byte overflow. | Original QA-09 negatives, new REG cases, RT-01 independently authored CLI mutations with accepted controls; retain exact outputs and bytes. |
| QA-LOG-02; LG-02/06; base exit/inspection | Mandatory typed post-stop key, successful zero exit agrees across process_exit/terminal. | QA-09, REG cases, RT-01 missing/null/type/range/contradiction negatives; actual CLI exit4/evidence_corrupt and unchanged inputs. |
| Compatibility; historical inspection | Honest failed/blocked/cancelled/timed_out/cleanup_uncertain observations may retain incomplete capture and explicit null exit; historical schemas and interrupted prefixes retain semantics. | QA-10, REG cases, RT-02 fixed expected historical states, failure/null controls, complete current-schema legacy request controls, dropped-detail compatibility, pagination must not bypass validation. |
| LG-01/04/05/07; LD-01..11 | Closed digest-bound schema3 config, levels/privacy, bounded streams/sink, classification and exact policy. | Original independent QA-01..08/11..13, full inherited package and supplemental byte case; exact independent literals, canary absence, real owned child cleanup and byte hashes. |
| LG-08; WF-01..20; F-01/F-02 regressions | All inherited admissions, protocol/oracle, errors, ownership/recovery, evidence, full watchdog and quality floors remain valid. | Entire 162-case package campaign, unchanged supplemental case, all 13 original independent cases; source/test inventory and raw result arithmetic. |
| NI-T01..10 offline; SI-T01..10 offline; diagnostic contract | Launcher/source junction and review restrictions, real query_failed/no-RPC/cleanup, closed error projection, preserved files. | Real synthetic Windows fixtures; all inherited package cases. Existing static integrity evidence is reusable only for byte-identical files, with current composition review. No native qualification. |
| Delivery/preservation/docs | Current corrected and prior selected bytes remain unchanged; documentation matches limited claims and links resolve. | QA-13 plus final manifest/readback, changed-file review and local link checks. |

Native WN-01/02, NI-T11/12, actual installed profile and rendered UI: NOT_APPLICABLE to this offline retest, remain NOT_RUN for native qualification. Framework acceptance remains NOT_EVALUATED.

## Environment and capability

Discovered C:\Users\bryan\.cargo\bin\cargo.exe and rustc.exe; Rust/Cargo 1.97.1, rustfmt 1.9.0, Clippy 0.1.97, cargo-llvm-cov 0.8.4, LLVM 22.1.6. Python 3.10 at C:\Program Files\Python310\python.exe supports evidence only. Installed stable and 1.93 MSVC toolchains; no nightly. Branch collector is unstable: branch coverage NOT_RUN, no install implied. Windows offline build/cache availability is checked by BUILD; no external service is required. All listed preparation is READY, subject to bound identity and integrity.

## Required cases and execution procedures

Required behavioral denominator: **178** unique cases: original PKG-001..152 (49 unit / 103 integration), PKG-153..162 (ten new developer integration regressions), SUP-01, QA-01..13 and RT-01/02. required-cases.json will bind names before execution by comparing the original QA inventory and current development declaration; Cargo LIST must match the 162 package names exactly. Subfixtures count conjunctively once. No repeated attempt or helper/setup command increases the denominator.

All cases initially READY / NOT_RUN. Each uses a new attempt directory, exact argv/cwd/environment receipt, current hashes and retained raw stdout/stderr. Commands run from the corrected candidate using discovered Cargo. Common arguments: --offline --locked --manifest-path C:/Projects/DevForgeAI/devforgeai/experiments/codex-worker-probe-logging-qa-fixes/Cargo.toml. No shell-composed child commands.

1. BUILD: cargo build with common arguments and --bins; fresh build-target, 180s. Expected exit0; hash produced binaries before CLI use.
2. LIST: cargo test with common arguments and --all-targets -- --list; 180s, no behavior credit. Exact package inventory match required.
3. METRICS / all PKG cases: cargo llvm-cov with common arguments, --all-targets --json --output-path C:/Projects/DevForgeAI/docs/plan/framework-worker-logging/20260917T105456Z-qa-retest/coverage.json --ignore-filename-regex [/\\]tests[/\\] --fail-under-lines 95 -- --test-threads=1. Separate coverage-target; outer bound 420s includes the real production 120-second watchdog. No filtered contributing case. Compute final unit and line metrics immediately after the complete campaign before launching other tests.
4. Independent preparation: copy the previous inspected QA helper to qa-harness, change only absolute selected candidate paths in Cargo.toml and the README check, retain Cargo.lock and independent byte expectations. Build with cargo test --offline --locked --manifest-path <this root>/qa-harness/Cargo.toml --test independent --no-run; hash/read back helpers and binaries. No dependency installation or new mocking seams.
5. QA-01..13: run one exact name per attempt using the same harness command with -- EXACT_NAME --exact --test-threads=1 (180s outer bound). Exact names/oracles remain in the bound original independent-plan.md and required-cases.json. Every original QA-09 mutation must reject while all original unchanged controls pass; preserve every result. Other cases assert literal byte hashes, schema/path/config denials, caps, privacy, real child cleanup, trace/oracle, legacy/interrupted inspection and preservation.
6. SUP-01: cargo test --offline --locked --manifest-path C:/Projects/DevForgeAI/docs/plan/framework-worker-logging/20260917T102726Z-dev-qa-fixes/supplemental-harness/Cargo.toml --test raw-windows-capture -- --test-threads=1, fresh supplemental-target, 180s. This supplied harness now targets the corrected source; its five files are bound inputs. Expected exact raw UTF8/final-line hashes and real child cleanup.
7. RT-01: cli_matrix.py success. Build independent current-record copies from fresh successful schema3 peer runs and synthetic preflight records retained by the package campaign. Rebind only copied request.run_dir and its input digest using independent SHA256. Each control must first inspect as completed/preflight_checked. For both streams, independently mark incomplete drain, missing EOF, reader failure or byte overflow; remove/post-null/type/range/nonzero/mismatched exit fields; remove capture/status and tamper logging bytes. Each must reject with CLI exit4 and specified corrupt/incomplete error, including --limit1 / end-cursor views. Preserve the original fixture and copied before/after bytes. Synthetic preflight interpretation is explicitly not native execution.
8. RT-02: cli_matrix.py compatibility. Fresh actual failed/blocked/cancelled/timed_out/cleanup_uncertain fixtures from the complete package campaign, plus historical schema1/2 and interrupted records. Accepted controls retain their known terminal states. Incomplete capture and explicit null failed exits remain honest inspectable failures; absent mandatory exit key rejects. Dropped detail and pre-stop timing alone must not invalidate complete capture. Current capture-bearing schema1 cannot bypass success checks; no capture_schema on schema3 rejects. CLI inspection is read-only, with no historical PID operation. Each child inspect command is bounded to 15s.
9. FMT: cargo fmt --all --manifest-path selected manifest -- --check (60s); CLIPPY: cargo clippy common arguments --all-targets -- -D warnings (180s). Documentation and final preservation/readback are required, with no new product invocation merely to improve a result.

RT procedures and exact command matrices will be saved/read back before their execution; selected expected results are already fixed above. Recorded seed selection must be unique and based on fixture content/expected state, not a favorable inspector output. No worker is launched by RT inspection. Build/test fixtures use test-owned process handles and Job Objects. Retain all fixture, junction and target directories; do not kill a historical PID. Timeout/error is never a product PASS. No automatic product retry. One bounded correction for a demonstrated QA-only preparation fault is allowed before any terminal stop, preserving both attempts and helper bytes.

## Integrity and evidence reuse

Prior QA integrity.md and inspected helper code are bound and read in this conversation. Reuse the static inspection only for the 65 byte-identical candidate files; new roots are checked for correct source/admission composition. Re-read changed journal.rs, all 453 lines of new tests and README changes; verify attributes/imports/helpers/assertions rather than treating search hits as absence proof. No new proc-macro/dependency/build-script change is present. The new tests' observation helper exercises both public library and compiled CLI, snapshots actual bytes, has explicit expected states/errors, and retains false observations until a conjunctive assertion fails. Synthetic history is labeled interpretation-only.

Required scrutiny: capture conditional validity and success terminal checking; Option missing/null handling and duplicate exit records; current versus historical input discriminator; failure state compatibility; pagination validation; real held-job query_failed mapping. New QA helpers must be read back before execution, preserve exits/raw evidence, count cases once, and use independent content/state oracles. Any unresolved dynamic integrity region remains a gap. No prior pass or coverage measurement qualifies changed runtime bytes; all numerical evidence is fresh.

## Metrics, stop rules and final handoff

Executed-line denominator: all 15 src/*.rs files; declaration-only lib.rs may have no executable lines. Exclude third-party and tests/support fixture code, never uncovered first-party behavior. Raw LLVM file set must exactly cover the declared executable sources. Coverage is contributed only by the complete unfiltered 162-case package campaign; supplemental, original independent and RT cases contribute no line-coverage credit. Unit metric is exactly the 49 required package unit cases. Overall project suite is all 178 cases, failures/errors/skips/blocked/unexecuted included; each once. Windows is the only selected platform and is also overall.

Each line/unit/project-suite floor is >=95%, compared using integer counts without rounding. Missing or interrupted collection is NOT_RUN/BLOCKED/partial, not a guessed failing or passing ratio. No complete metric is finalized before every contributing case has a terminal outcome and raw collector evidence is usable. No new passing cases, suppression, altered denominator or repeat collection to improve a metric.

Confirmed INTEGRITY_FAILURE, complete valid METRIC_FAILURE or CRITICAL_PRODUCT_DEFECT (demonstrated authorization/security violation or unintended loss of protected/preserved data) immediately stops the whole run. Record STOP.json and do not launch another test/build/coverage/reproduction. Only bounded containment, necessary evidence/assessment/reporting continues. Ordinary mandatory conformance failures retain final FAIL while safe independent checks proceed. Local preparation gaps block dependents; uncontained identity/ownership drift stops the run.

Final verdict is PASS only with both selected defects independently verified, all mandatory offline checks/integrity and valid floors satisfied. Otherwise FAIL takes precedence over INCOMPLETE. Publish qa-report.md, findings/lifecycle/checkpoint/case/criterion maps and external bindings; on FAIL add qa-fix.md and a complete manual dev handoff. On PASS identify separately selected downstream native diagnostic preparation/review without launching it or inventing release permission. Current host skill catalog exposes dev if remediation is required. Preserve old findings/reports and record only new lifecycle transitions in this root. Framework acceptance remains NOT_EVALUATED; release/deployment authorization is not granted.
