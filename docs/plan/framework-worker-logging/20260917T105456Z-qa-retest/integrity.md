# Retest integrity inspection

The corrected 68-file inventory and exact three changed paths were independently verified against the failed candidate and development manifest. The previous QA integrity record is preserved and reused only as static evidence for 65 byte-identical files. Runtime success/coverage is not reused. Current Cargo dependencies/lock, same-root include/path modules, new root's peer admission and the fresh harness's path dependency were checked for correct composition.

Re-read the changed journal::inspect control flow with numbered source, unchanged Capture::validate, and the complete new tests/inspection_consistency.rs (453 lines). The correction retains Capture and Option<u32>, requires field presence before decoding, and checks both successful terminal kinds. Capture::validate checks both EOF/read_error when drain_complete is true; the terminal branch requires that flag and excludes either byte overflow. Failure states keep nullable codes/incomplete capture. Presence/type and success agreement need fresh independent behavior, not just this static assessment.

The ten new test functions use built-in #[test] only, with ordinary imports and helpers. observation() invokes actual public library and compiled CLI, takes before/after byte inventories, compares fixed expected error/state/exit, retains stdout/stderr and returns its comparison. Callers assert the returned boolean or collect every planned nonconformance before asserting empty. There is no unconditional passing result, swallowed assertion, skipped case, mock decorator, re-exported mocking attribute or coverage exclusion in the changed source. Synthetic preflight records are explicitly labeled record interpretation; they cannot establish native preflight. Real failure controls and actual failed child exits remain distinct. Existing private fault seams were inspected previously and are byte-identical; the real query_failed handle/API mapping remains required in the fresh regression run.

Candidate attribute/import/include locators were rechecked. Cargo has no new build script, proc-macro crate or dependency. Prior first-party aliases and serde derives retain their inspected meaning. No confirmed integrity failure or unresolved dynamic wrapper was found in the selected changed scope. This is bounded product/test integrity review, not an exhaustive repository security audit.

Original independent cases and fixed byte oracles are retained byte-for-byte except the new candidate paths in Cargo.toml and the README read. That relocation is recorded by helper-provenance.json. run_command.py preserves raw failures, rejects manifest drift and STOP.json, invokes bounded argument arrays without a shell, and owns its process/fixtures. measure.py compares all raw unique names and every declared executable source before deriving exact ratios; only the package inventory constant changes from 152 to 162. No setup/list/build result receives behavioral credit. New cli_matrix.py will be separately read and bound before execution.

Scope limits: unchanged-file semantic review is explicitly inherited from verified prior QA evidence. Fresh full package, original independent and additional CLI cases are needed before a PASS or defect closure. Any newly confirmed integrity, complete metric, or critical preservation issue stops immediately under plan.md; ordinary evidence-consistency defects retain FAIL while safe independent checks continue.
