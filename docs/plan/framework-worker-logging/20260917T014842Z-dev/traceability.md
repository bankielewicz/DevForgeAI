# Selected requirement traceability

Source: selected logging-design.md plus the investigation report's temporal-exit/privacy clarifications, implemented concretely by candidate logging-contract.md. Original worker, preflight and source-identity contracts remain mandatory except the explicit schema/policy/logging amendments. Output locations come from the user's fresh-candidate/fresh-evidence selection as recorded in context.md.

| ID | Required behavior | Rust implementation | Executed verification |
| --- | --- | --- | --- |
| LG-01 | Closed digest-bound config, v3 composition, legacy off, reject malformed/unknown/oversize/reparse/drift before spawn | request.rs; logging.rs::load; Journal::create; runner final check | logging integration config tests; exact 4096/4097 bytes; actual Windows directory junction; after-intent drift |
| LG-02 | Pre-stop/post-stop observation, two EOF summaries, owned reader join and bounded stop | process_windows.rs; runner.rs process_exit | early/silent exit 1; real child exit 23 in attempt 24; inherited process tests and watchdog |
| LG-03 | Independent aggregate SHA256/counting; nonblocking queues and compiled caps | capture.rs; Session::receive post-wait failure recheck | 600 KB delayed consumer; exact/over 8 MiB and 1 MiB/128-line/64-detail limits; actual malformed UTF8 Windows pipes; WF-16 overflow race reproduction and correction |
| LG-04 | Four levels; typed JSONL; no raw data; optional failure reported, cleanup preserved; mandatory write failure remains fatal | logging.rs::Sink; journal.rs; protocol.rs closed unknown-method projection | all levels, stderr/protocol canaries, real optional file open/write denial, exact/over event and file-byte caps, required journal failure cases |
| LG-05 | Closed startup-v1 classification; unknown stays unknown | capture.rs::classify | exact observed config-error fixture, invalid MCP transport fixture and negative patterns; classifier cap; hashes over all read bytes |
| LG-06 | Current mandatory summaries/status and bound optional artifacts required; historical inspection compatible | journal.rs::inspect; logging.rs::inspect; Capture::validate | missing/changed log; missing summary; incomplete status; malformed/partial JSON; preflight vs completed-turn inspection; legacy inputs |
| LG-07 | Exactly 15 key-quoting corrections, new compiled v3 policy identity, remaining arguments preserved | restrictive-launch-policy.json; launch_policy.rs | independent literal CLI-key test red 04 / green 05; inherited exact 116-argument vector and wrong-binding denials |
| LG-08 | Full inherited tests, real query_failed, Windows platform, fmt/Clippy, full-source line floor | all candidate src files and inherited/new tests | 49 units including actual Windows ERROR_ACCESS_DENIED query path and write precedence; 152-case candidate suite; one additional real-pipe harness case; final measurements in delivery |
| OUT-01 | Fresh candidate and evidence paths; preserve frozen candidate, snapshot and earlier evidence | candidate-manifest.json; preservation-readback.json | all 58 original + 58 snapshot bindings; all selected input hashes; literal absolute output-path readback; console helper unchanged |

The supplemental harness in qa-harness is development-owned verification of the unchanged candidate's public OwnedProcess API, with a distinct synthetic Rust executable. Its byte oracle is Python hashlib over literal fixture bytes. It is not a production policy implementation, not a replacement acceptance authority, and not independent product QA. Every applicable inherited subfixture is conjunctive and required. Repeated runs count no additional passing cases.

Historical failed attempts remain: 01 requested-behavior red; 03 zero-selection harness mistake (no pass credit); 04 policy red; 08 Clippy finding; 11 stale inspection fixture; 12 privacy red; 17 stderr-overflow/EOF race. The changed final candidate, not repeated attempts, supplies the final case denominator. Full coverage includes every executable src file; lib.rs has only module declarations. Tests/fixture binaries and dependencies are excluded, with no first-party executable exclusion. No native Codex, operational installation, independent QA, or protected framework acceptance is claimed.
