# Independent QA handoff: Rust logging candidate

Selected candidate: C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe-logging. Binding: candidate-manifest.json in this evidence directory. Selected behavioral amendment: candidate logging-contract.md and the original selected logging-design/investigation report in input-bindings.json; inherited worker/preflight/source-identity requirements remain except the explicitly documented amendments. The [original QA handoff](../../framework-worker-diagnostics/20260916T181820Z-dev/qa-handoff.md) and [reviewed addendum](../../advisor-runs/20260916T185825Z-diagnostic-handoff/qa-handoff-addendum.md) remain relevant to preservation, complete inventories, test integrity and the mandatory runtime query_failed case.

Preserve this candidate, the frozen original candidate/snapshot and all previous evidence. Author independent tests in a new QA evidence directory. Do not modify the root console script, installed configuration, operational skills, identities or saved bindings to obtain success. No native Codex launch is authorized by this handoff. Native diagnostic selection follows independent QA PASS and separate user authorization.

Development evidence: delivery.md, traceability.md, candidate.patch, candidate-manifest.json, coverage*.json, suite-summary.json, tool-inspection.txt and attempts. Inspect the known failures and repairs as well as the green results. In particular review the stderr overflow/EOF race (17/21/23), arbitrary method-name privacy repair (12/13), inspector schema compatibility, the closed classifier's limited interpretation, and optional versus mandatory evidence failures. Reproduce independently; development claims are inputs, not QA conclusions.

Required inventory baseline: 152 package cases (49 units + 103 integration/regression cases), plus the external real Windows pipe case from qa-harness (one case with conjunctive byte/EOF/exit/cleanup assertions). Add every independent required case before execution and count once. Keep required-unit and overall rates separate. No unresolved required scenario, including query_failed, may be waived because aggregate numeric floors pass. The included test-only restricted-handle helper uses DuplicateHandle with TERMINATE and without QUERY to obtain an actual Windows access-denied query. Inspect and independently verify its mapping to production code and held-job cleanup; no production API interception occurs.

Run the unfiltered `cargo test --offline --locked --all-targets -- --test-threads=1` campaign with QA-owned CARGO_TARGET_DIR and WF_TEST_EVIDENCE. A unit-only invocation first needs `cargo build --offline --locked --bins` in that same target directory. Use the actual manifest at the selected candidate path. Retain the production 120-second WF-16 watchdog and all inherited subfixtures. Use new CARGO_LLVM_COV_TARGET_DIR for coverage; the executed command shape is in attempt 23. Declare every src/*.rs file before collection: 15 source files including declaration-only lib.rs, no first-party executable exclusions. Recompute executed-line coverage >=95% and required-case pass rate >=95% independently; report branch coverage separately. Default package feature set has no optional features.

For the supplemental harness, preserve its inputs, fixed Cargo.lock and synthetic peer, and choose a fresh target/fixture directory. `cargo test --offline --locked --manifest-path <absolute-qa-harness-Cargo.toml> --test raw-windows-capture -- --test-threads=1` is a real Windows pipe check of the selected library's public OwnedProcess API. It does not use the production request admission path and does not qualify native Codex. Independent QA should add its own canaries and absence/content oracles, not simply rerun this helper and call the result independent.

Mandatory adversarial cases: both stream EOF/join on early and silent exit; invalid UTF8/final partial line; >32 chunks before receive; exact/over byte, line, queue and log budgets; denied reader/file writes and cleanup; unknown/malformed/oversize/reparse/digest-drift config before spawn; level-specific output and privacy in journal/diagnostics/emitted output; mandatory summary/status/file inspection; new v3 native composition without policy relaxation; original process 1/1 predicate and actual query_failed. Optional logging failure must be visible as diagnostics_incomplete and may not authorize or change dispatch. Inspectors must not silently treat missing/incomplete required diagnostics as complete evidence.

Finish with source/input/snapshot preservation and literal-path readback. Report independent QA separately from native readiness and framework acceptance. A model-written report, Python-derived metric, or synthetic peer success cannot supply protected framework acceptance.
