# Checkpoint 003: implemented harness, review-dependent final preparation

Continue from operator-handoff.md and the sealed handoff-manifest.json in this directory. The original selected outstanding-work document and every earlier checkpoint remain intact. Full goal is active and incomplete; do not mark complete after the code-only delivery.

Final runtime source is root Invoke-CodexWorkerDiagnostic.ps1 plus operator-harness-001/diagnostic.py and recorder.py. The latter is a preserved-recorder derivative fixing the Python 3.10 interruption wait hang. The current runtime-manifest.json SHA is 308f993b7821ebcca2c5b1a449c01aa435f23f6a8a8ff391142e5f7e8221c3f2, pinned by the root script. Runtime Python checks: attempt 15, 42/42, 295/296 lines. PowerShell checks: attempt 16, 9/9, 30/30 lines. PSScriptAnalyzer: zero errors, four deliberate Write-Host advisories. Two positive live-readiness tests in attempt 17 have raw failed assertions; their changed-input prerequisites are classified BLOCKED and they are not credited as passed. Total required cases 53, passing 51.

The current active configuration has NOT been rebound to the new source state. Its default preparation stops on the retained launch-invocation.json. The fully prepared proposal under current-source-review binds the new 26-file inventory and the old zero-dispatch preparation reservation. Its reviewer is null and it is explicitly not approved. The proposal preparation freshly checked 21,828 path bindings and reproduced the current inventory; preservation-final.json separately confirms all 393 original scoped bindings unchanged.

Review needed: config.toml gained only [features.context_management] experimental_mode = true. config-drift.json proves exact byte reconstruction of the prior hash without modifying the file. The second inventory difference is the exact host permission rule already approved for the offline Pester runner; permission-rule-addition.json proves it and no repeat approval is needed. The operator is Bryan. Do not re-ask existing model/environment/budget/reviewer decisions.

Retained preparation reservation: trial root launch-invocation.json points to operator-harness-001/invocations/20260917T165326269219Z-3a2ceb54/stop.json. It resulted from Pester's failure to intercept a fully qualified executable. The stop says PREPARATION_FAILED on config drift, native_preflight_invocations=0; no dispatch.json, native-001 or run exists. It must not be deleted/reset. An optional prior_preparation_stop configuration binds this reservation/stop exactly and enables a new exclusive operator-harness-001/launch-invocation-after-preparation-stop.json. That optional selection is implemented and tested but not active.

After Bryan reviews the concrete proposal, use his actual response to create an attributed selection, preserve the active configuration/seal and finalize new input bindings. Recollect sources and stop on further differences. Run the two live readiness cases against that finalized selection and relevant changed-seal checks, preserving all attempts. The PowerShell suite currently has a real occupied-reservation stop case selected for the current pending state; do not mislabel it as positive readiness after finalization. Then return the ready native command for Bryan's console. Native Codex must not be launched by the assistant during this preparation task.

The user reported a PowerShell parsing error because the leading call operator & was omitted from a quoted Python path. This was explained; the displayed offline record_powershell_checks.py command has already completed and its evidence directory is single-use. It is not the native operator command. No further processes or tool sessions are running at this checkpoint. Native Codex remains NOT_RUN and framework acceptance NOT_EVALUATED.
