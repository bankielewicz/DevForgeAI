# Selected requirement accounting

Authority is the user's selected outstanding-work document and bound diagnostic contracts. These statuses are development evidence, not compiled-Rust framework acceptance.

| Requirement | Implementation and evidence | Current disposition |
| --- | --- | --- |
| OW-01: remove stale live-Sites dependency while preserving history | Separate immutable manifests/current inventory; attempts 01/03 retain red/green; the stopped executor remains intact. | Implemented. |
| OW-01: fresh complete inventory and attributed exact-byte inputs | Current-source-review contains a fresh schema-2 inventory, exact diff proof, draft review/request/diagnostics/configuration and manifest. | Proposal prepared; Bryan's review of the added context-management setting remains required. |
| OW-01: stop on material source change in the actual launch context | Source equality covers files, absent paths and directory membership; same process/environment collector immediately precedes proposed dispatch. | Offline verified; two live readiness cases are blocked pending final input selection. |
| OW-02: native PowerShell entrypoint | Root Invoke-CodexWorkerDiagnostic.ps1, default preparation, explicit RunOnce, sealed Python runtime, literal argument boundary. | Implemented; 9/9 current PowerShell checks pass. |
| OW-02: visible progress and exact output retention | Background bounded display is independent of raw files; stdin stays open; safe owned-handle wait/cancel/reap. | 42/42 Python cases pass, including 17 inherited recorder cases. |
| OW-02: one native attempt and failure preservation | Exclusive reservation, occupied native/run checks, no retry. An explicitly selected zero-dispatch prior preparation may be retained with a fresh reservation; changed/consumed stops cannot be reused. | Offline verified. Prior stopped reservation retained; resumed selection not active. |
| OW-02: >=95% line and pass-rate thresholds | 325/326 runtime lines; 51/53 required cases pass with two readiness blockers. Historical attempts are retained separately. | Numeric floors met; mandatory readiness gaps still prevent overall completion. |
| OW-03: Bryan's single native diagnostic | Exact QA binary and original 120/145-second restrictions remain selected. | NOT_RUN. No native command is issued as ready while source review is pending. |
| OW-04: compiled Rust inspection and preservation | Existing compiled inspector is unchanged; actual run does not exist. | NOT_RUN, depends on OW-03. |
| OW-05: evidence-led cause assessment and targeted follow-up | Harness defects were addressed independently of the historical worker exit; original cause remains unresolved. | Pending native evidence. No speculative configuration repair, Rust rebuild, automatic QA, or acceptance. |

The PowerShell test isolation incident is part of the retained history, not erased by later green tests. The subsequent tests explicitly intercept the support function and use a separate synthetic Python process for literal argument handling. All operator-run source paths and exact identities are listed in source-identities.json and changed-file-manifest.json.
