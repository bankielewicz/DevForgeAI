# Checkpoint 002: offline refinement and two newly observed issues

The prepare CLI passed its real red/green contract in attempts 04/05 and produced a concrete packet without native dispatch. Attempt 06 retains the unsuccessful broad run. Synthetic fixtures used an empty environment, which Windows rejected with ERROR_INVALID_PARAMETER; the fixtures now preserve ordinary host variables and remove guarded names. This is test setup correction, not product green credit. No production assertions or source-drift requirements were relaxed.

During that run, the installed config changed externally at 16:32:14Z. Its only difference is the addition of `[features.context_management] experimental_mode = true` (56 bytes including line endings). Removing that section in memory reconstructs the exact previously approved hash; config-drift.json records the proof. The configuration was not modified or restored by this work. All other 25 currently selected file bindings matched. New final source review remains pending this concrete material difference. The two real preparation integration cases are blocked by changed input, not counted as passing or silently excluded from the overall required-case denominator.

The inherited recorder's Ctrl+C case also hung. A bounded isolated reproduction in attempt 07 retained a stack trace in Python 3.10 subprocess.wait's KeyboardInterrupt path, followed by its Windows _wait. Local source shows an unchecked possibly negative remaining timeout converted to Windows wait milliseconds. The original test requests a 2.9-second total; it remained waiting after four seconds and was stopped at the outer eight-second limit. This is a reproduced runtime issue, separate from the configuration change and Rust's historical exit cause. The prior recorder and its tests remain unchanged.

The selected support package now contains a derived recorder.py. The only behavioral change is replacing Popen.wait with repeated poll calls on the owned live handle and short sleeps bounded by the same deadline. Cancellation bytes, time budgets, exact file capture and handle-only termination remain unchanged. The inherited 17 tests will run against this new copy. This is authorized OW-02 harness repair, not a Rust candidate rebuild or independent QA invocation.

Updated runtime coverage denominator, declared before the next run: every executable line of diagnostic.py, recorder.py and Invoke-CodexWorkerDiagnostic.ps1. The original supervisor.py is preserved historical evidence and no longer imported by the operator runtime; evidence-only test orchestration still uses it. No uncovered operator code is excluded. Required case totals include separately reported blocked real preparation cases. Native Codex remains NOT_RUN; framework acceptance remains NOT_EVALUATED. The root PowerShell script still needs its final runtime seal and fresh Pester evidence before delivery.
