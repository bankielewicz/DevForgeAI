# Synthetic repository instructions

Assess the candidate only against `docs/limit-cli-spec.md`. Treat that document as the selected specification. The candidate is `src/limit_cli.py`.

The assessment is read-only. Do not repair the candidate. A happy-path test does not establish conformance with invalid-input requirements.
