# Independent adoption task trial

This is actual execution against a disposable TEMP development project. The
executor read current builder instructions, references and declared interface
schemas, and did not read builder implementation, implementation tests, or other
trial conclusions. No builder source or operational skill directory was edited.

The original skill was created with a realistic float-based implementation. Its
actual command returned `{"total": 0.30000000000000004, "line_count": 2}` for
0.1 plus 0.2. Raw output and command arguments are retained in
commands/origin-known-defect.json. The package and reviewed specification are
retained under initial-package/ and origin-spec.md.

The new TEMP project path and selected roots are in trial-state.json. The adoption
capture/request and all later run outputs are copied under retained/. External
pointer publication occurs first in the project's docs/plan evidence directory;
a later retained copy is distinguished from the original publication/readback.

The coordinating trial controller reviewed revision-proposal.md and separately
authorized it. The distinct verbatim instruction is retained under prompts/.
Final evaluator execution awaits the stable builder manifest signal because the
builder is being edited concurrently. No appearance of a manifest or timestamp
has been treated as that signal.

An early reference read observed the adoption schema path absent while the
builder was being edited. The subsequent read succeeded. This transient state is
not classified as a stable builder defect.
