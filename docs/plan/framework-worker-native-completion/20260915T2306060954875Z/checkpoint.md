# Checkpoint: integrated preflight development

Candidate: original 36-file package baseline matched before editing; source is now being developed concurrently in bounded owned slices. No Git metadata. Evidence root and authorized scope remain those in context.md. No Codex app-server/model session or WN trial has been launched.

Completed focused slices: fixed native policy; compiled source inventory (6 tests); closed review v2 with exact inventory/file bindings and all-true work gate (5 tests plus 3 legacy); terminal preflight/source operations; preflight journal recovery distinct from completed work. Exact command attempts are retained in root and policy-dev/source-dev/review-dev/integration-dev/effective-dev directories. In-progress candidate manifests do not establish a frozen qualification.

Development failures retained and classified:

- CLI red correctly returned invalid_arguments before the new operation existed.
- Recovery attempts 001/002 were setup failures (concurrent missing module, then synthetic peer selector mistakenly retained for native-shaped fixture); corrected 003 reached the intended evidence_corrupt red. Green passed.
- Concurrent module-write gaps caused E0583 setup failures in child attempts; they are not product defects or valid TDD red.
- Early integration rejection tests were vacuous because startup failed before their target RPC. Assertions now require target RPC evidence. Original attempts remain.
- Held-job accounting and live image observations showed CREATE_NO_WINDOW spawned conhost.exe. The child now uses DETACHED_PROCESS with the same atomic job and pipe inheritance. Positive exact preflight trace then passed; full process regressions still pending.
- Optional omitted final nextCursor is valid in pinned schemas; retained focused red led to the absent-as-final protocol fix. Full focused preflight rerun pending.
- A real compiled `profile-sources` invocation returned exit 3 for `C:\Users\bryan\.codex\plugins\cache\openai-bundled\chrome\latest` reparse. Original raw stdout/stderr and source/binary bindings are in native-source-inspection-001. This is a prerequisite denial, not a completed native trial.

Remaining: finish version-matched feature policy extension and effective validator tests; finish focused integration tests; format/static/regression checks; freeze full candidate and specs; independent QA in a separate directory; act on only demonstrated prerequisites. Strict cache applicability is under source review; no exception, operational deletion, configuration edit or fabricated review is authorized. The two selected native trials remain one attempt each only after prerequisites. Authority remains deferred/NOT_EVALUATED.

No long-running root process is active at this checkpoint. Child agents may run bounded focused Cargo commands. Every later source change invalidates dependent final qualification; preserve all historical attempts.
