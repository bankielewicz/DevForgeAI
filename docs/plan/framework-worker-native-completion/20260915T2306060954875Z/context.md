# Native completion context

Selected evidence value: `docs/plan/framework-worker-native-completion/20260915T2306060954875Z`. Source: repository rule for fresh timestamped evidence under docs/plan; selected by this development run. Resolved root: `C:\Projects\DevForgeAI\docs\plan\framework-worker-native-completion\20260915T2306060954875Z`. No path components were renamed. All development records, command attempts, coverage and final handoff resolve beneath this root. Native disposable requests retain the contract's separate `docs/plan/framework-worker-trials/<run_id>/{fixture,run}` layout; run directories are created only by Rust.

User selected completion of NI-T05 and NI-T07–T12: define and implement compiled preflight/restrictive launch policy, independent QA, then the previously selected one-shot WN-01/WN-02 with Codex 0.154.0 / gpt-6-astra / high. Protected authority is deferred. No installation, operational configuration, credentials or original evidence edits.

Candidate is `devforgeai/experiments/codex-worker-probe`; 36/36 delivered file hashes verified before edits (`baseline-readback.json`). No Git metadata. Native Windows x64, NTFS C:, PowerShell 7.6.6. Required platform is Windows only; Linux and native tray are outside this isolated harness scope.

Governing inputs: AGENTS.md; DFF-WORKER-FEAS-01 v1.0.0; DFF-WORKER-NATIVE-01 v0.1.0; captured 0.154.0 protocol schemas; compiled-Rust enforcement design for the authority boundary. The next companion contract will define the bounded preflight without weakening existing unknown/active-integration rejection.

## Declared denominators and checks

- Overall native readiness: NI-T01..NI-T12, 12 requirement groups. Changed selection: NI-T05 and NI-T07..12, seven groups. Native actual trials separately WN-01 and WN-02, two cases, one attempt each. Blocked/unexecuted cases remain nonpasses.
- Mandatory offline regressions: original WF-01..20 with every subfixture; F-01/F-02 negative regressions and all 62 delivered functions retained. New focused cases will be enumerated before their execution. Test-function counts are supplemental and do not replace requirement groups.
- Coverage baseline: every executable first-party line under package src/, including new preflight/policy code, main, errors, native process handling. Tests/support/fixtures and third-party/generated dependency source excluded, no first-party behavior excluded. Exact executable line denominator comes from raw LLVM report. Branch coverage reported separately if available.
- Mandatory floors: >=95% lines and required cases independently, without rounding; mandatory/security failures block readiness regardless of aggregate counts.

## Slice plan and reuse

1. Pinned schema/configuration research and explicit preflight/policy contract. Reuse retained version-matched schema capture and prior flag-rejection observations; do not infer runtime inactivity from CLI help.
2. Fixed compiled policy and typed effective-state validators, with focused Red/Green tests. Extend request v2 review admission only after complete binding; preserve legacy v1 behavior.
3. Compose existing OwnedProcess containment, Session correlation, cancellation/deadlines and Journal persistence for a pre-thread inspection path. No thread/turn in standalone preflight; trial dispatch rechecks current profile before work. New observations retain only approved typed fields, never raw config/account secrets.
4. Freeze candidate; formatting, Clippy, regressions, meaningful negative cases, full-source coverage. Independent QA uses fresh evidence and source readback.
5. If every prerequisite is demonstrated, run selected WN-01/WN-02 once with 145-second external supervision. Unknown profile/integration state blocks, without fabricated approval or automatic replay.

Execution records: each distinct attempt has raw stdout/stderr, exact argv/cwd, start/end/duration, exit/timeout and source manifest. Native protocol payloads are sanitized inside Rust; raw command console retention does not authorize storing secrets. Final delivery and readback distinguish implementation, offline verification, native qualification and protected framework acceptance.
