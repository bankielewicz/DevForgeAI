Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\effective_profile_edges.rs:342:
     inert["config"]["openai_base_url"] = json!("");
     inert["config"]["api_base"] = Value::Null;
     inert["config"]["base_url"] = json!("");
-    assert!(
-        effective_profile::validate_config(&inert, &cwd, std::slice::from_ref(&user)).is_ok()
-    );
+    assert!(effective_profile::validate_config(&inert, &cwd, std::slice::from_ref(&user)).is_ok());
     inert["config"]["model_providers"] = json!({});
-    assert!(
-        effective_profile::validate_config(&inert, &cwd, std::slice::from_ref(&user)).is_ok()
-    );
+    assert!(effective_profile::validate_config(&inert, &cwd, std::slice::from_ref(&user)).is_ok());
 
     let valid = config_response(&user);
     for (field, value, expected) in [
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\effective_profile_edges.rs:382:
     );
 
     let mut case = valid.clone();
-    case["layers"][1]["config"].as_object_mut().unwrap().remove("apps");
+    case["layers"][1]["config"]
+        .as_object_mut()
+        .unwrap()
+        .remove("apps");
     assert_error(
         effective_profile::validate_config(&case, &cwd, std::slice::from_ref(&user)),
         "profile_unqualified",
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\effective_profile_edges.rs:453:
     );
 
     assert_error(
-        effective_profile::validate_feature_pages(
-            &[],
-            launch_policy::REQUIRED_DISABLED_FEATURES,
-        ),
+        effective_profile::validate_feature_pages(&[], launch_policy::REQUIRED_DISABLED_FEATURES),
         "protocol_error",
     );
     assert_error(
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\effective_profile_edges.rs:492:
     let mut case = vec![json!({"data":complete_feature_data(),"nextCursor":null})];
     case[0]["data"][0]["stage"] = json!("future-stage");
     assert_error(
-        effective_profile::validate_feature_pages(
-            &case,
-            launch_policy::REQUIRED_DISABLED_FEATURES,
-        ),
+        effective_profile::validate_feature_pages(&case, launch_policy::REQUIRED_DISABLED_FEATURES),
         "protocol_error",
     );
 
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\effective_profile_edges.rs:710:
 
     let mut case = value.clone();
     case["marketplaces"][0]["path"] = json!(7);
-    assert_error(
-        effective_profile::validate_plugins(&case),
-        "protocol_error",
-    );
+    assert_error(effective_profile::validate_plugins(&case), "protocol_error");
 
     let mut case = value.clone();
     case["marketplaces"][0]["plugins"][1]["id"] = json!("local-plugin");
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\effective_profile_edges.rs:729:
         "profile_unqualified",
     );
 
-    for (field, bad) in [
-        ("authPolicy", "SOMEDAY"),
-        ("installPolicy", "SECRET"),
-    ] {
+    for (field, bad) in [("authPolicy", "SOMEDAY"), ("installPolicy", "SECRET")] {
         let mut case = value.clone();
         case["marketplaces"][0]["plugins"][0][field] = json!(bad);
-        assert_error(
-            effective_profile::validate_plugins(&case),
-            "protocol_error",
-        );
+        assert_error(effective_profile::validate_plugins(&case), "protocol_error");
     }
 
     for source in [
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\effective_profile_edges.rs:749:
     ] {
         let mut case = value.clone();
         case["marketplaces"][0]["plugins"][0]["source"] = source;
-        assert_error(
-            effective_profile::validate_plugins(&case),
-            "protocol_error",
-        );
+        assert_error(effective_profile::validate_plugins(&case), "protocol_error");
     }
 
     let many_plugins: Vec<Value> = (0..1_001)
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\effective_profile_edges.rs:808:
 
     let mut case = valid.clone();
     case["apps"][0]["runtimeName"] = json!(42);
-    assert_error(
-        effective_profile::validate_apps(&case),
-        "protocol_error",
-    );
+    assert_error(effective_profile::validate_apps(&case), "protocol_error");
 
     let oversized = json!({"apps":vec![Value::Null;1_001]});
     assert_error(
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\effective_profile_edges.rs:840:
         json!({"disabled_server_count":5,"pages":1,"server_count":5})
     );
 
-    assert_error(
-        effective_profile::validate_mcp_pages(&[]),
-        "protocol_error",
-    );
+    assert_error(effective_profile::validate_mcp_pages(&[]), "protocol_error");
     assert_error(
         effective_profile::validate_mcp_pages(&vec![json!({"data":[],"nextCursor":null}); 11]),
         "protocol_error",
