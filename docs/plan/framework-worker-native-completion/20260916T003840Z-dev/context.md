# QA-F-COV-01 development remediation and independent retest

Selected evidence value: `C:\Projects\DevForgeAI\docs\plan\framework-worker-native-completion\20260916T003840Z-dev`. Selection source: the user-selected `20260916T000205Z-resume-audit/dev-invocation.md` requires a fresh timestamped directory under this exact parent. Resolved root is the same literal path. No path components were renamed. This context precedes product changes.

The user explicitly selected: “Start remediation and independent retest.” This starts a separate dev assignment for QA-F-COV-01 and authorizes fresh independent qa of the returned frozen candidate. The prior terminal FAIL remains historical and is not resumed or erased.

Candidate: `C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe`. Failed candidate manifest: `809a8beb50f8b57198a963725fd0d2ccd5e586a1243e177284bff3d6dfa471dd`. Selected contracts are DFF-WORKER-FEAS-01, DFF-WORKER-NATIVE-01 and DFF-WORKER-PREFLIGHT-01 at the prior bound hashes. The source/handoff readback must pass before test edits. No Git metadata is expected; verify it. Required platform is native Windows x64; no Linux/tray scope or dependency installation.

Selected failure: complete first-party executed-line coverage 2,852/3,103 = 91.91105381888495%, below 95%. This is a metric failure, not proof of a functional defect. Add meaningful requirement-derived tests for uncovered behavior. Tests of already-correct behavior may pass initially; do not manufacture Red. Any production behavior change requires a valid defect reproduction before repair. Preserve original assertions, fixtures, source requirements, all prior evidence and operational copies.

## Ownership and sequence

1. Root verifies identities, reads contracts/implementation and maps retained uncovered lines to behavior. Root owns orchestration/test seams only if justified, and all shared evidence/check coordination.
2. Bounded agents own disjoint new test files for effective-profile validation, source inventory and protocol/CLI gaps. No original test weakening or product behavior edits without a reported valid Red and root coordination.
3. Retain focused characterization results and any actual failures. Format and integrate once file ownership returns. Freeze all candidate bytes before final developer checks.
4. Declare the complete unit/integration/acceptance and source denominators; run the complete locked/offline all-target suite, formatting, Clippy and one full-source line-coverage collection. A valid subthreshold result triggers the same stop, not a repair/retest loop.
5. If developer checks support FIX_REPORTED, an independent agent reads the frozen candidate and owns a fresh QA retest in a distinct directory under `docs/plan/framework-worker-native-completion-qa`. Only that retest can mark VERIFIED_FIXED.

First-party coverage includes every executable line under package `src/`; no first-party source exclusions. Tests/support, fixture data and third-party dependencies are excluded by resolved ownership, not coverage outcome. All existing 89 test functions, 20 WF parent cases/all subfixtures, F-01/F-02 and the native-readiness offline checks are preserved. New required cases and exact denominators are declared before collection. Synthetic peers prove offline protocol behavior only. Actual native trials remain 0/2 unless their separate quality/source/profile prerequisites are met; this remediation does not authorize operational cache changes or a reparse exception.

Protected authority remains deferred. Python helpers retain evidence only. No output of this assignment can issue framework acceptance.
