# Developer test-integrity review

The frozen diff contains five new test/support files, a Cargo registration for the new synthetic protocol peer, and one private `cfg(test)` module registration. Existing runtime function bodies, original tests/assertions, lockfile and specification bytes are unchanged. This is coverage remediation for already-implemented behavior; no functional repair or valid product Red is claimed.

- Effective-profile expected sets, limits, projection keys and denied states come from the selected preflight contract. Tests exercise public validators. Positive controls accompany malformed, active and unreviewed variants; synthetic objects do not qualify actual Codex effective state.
- Source-inventory tests use real Windows files/directories for byte/member/depth/type/grammar behavior. The accumulated-byte test reads eight real 1 MiB files before rejecting another byte. A direct private entry-count setup isolates the 2,048-entry limit; this unit test does not claim end-to-end native inventory qualification.
- Protocol edge tests launch a compiled synthetic peer through the actual owned-process/pipes/session code. Mandatory trace and journal reads fail on missing files. The no-work assertion requires an observed `initialize` trace. Exact cancel/unsupported responses, process termination, fixture bytes and sanitized evidence are asserted. Stderr accounting accepts real OS chunk boundaries and checks total bytes.
- Root runner cases exercise actual missing-cwd spawn, evidence filesystem failures, compiled CLI errors and public review-validation rejection. The constructed v2 request tests call review validation directly; they do not exercise or bypass native admission to launch a process. A valid-policy setup assertion prevents an unrelated earlier rejection from satisfying RT-05.
- No ignored or skipped test was introduced, and no original assertion was removed or relaxed. Focused attempts and later instrumented executions are preserved separately; only the enumerated full-suite functions determine the final developer pass rate.

Two focused setup errors were retained and corrected: the source-depth fixture accidentally crossed another documented bound, and the root containment comparison mixed normalized and ordinary Windows prefixes. Neither failure established a product defect. Formatting failures remain visible in original attempts. Some early source-test temporary trees were removed by their initial helper; the final helper and final runs retain them. All original raw console receipts remain retained.

This is a developer review. Fresh independent QA must audit the frozen bytes, oracles, raw records and full measurements before marking QA-F-COV-01 `VERIFIED_FIXED`. Successful peer tests and coverage do not establish native trial readiness or protected framework acceptance.
