# Root-owned characterization cases

Declared before execution. These cases address existing requirements; an initial pass is characterization evidence, not a fabricated Red. Source inventory/environment fixtures are synthetic and never launch Codex.

| Case | Contract / independent expected result | Level |
| --- | --- | --- |
| RT-01 fixture disappears after admission | Base sections 4/6/7 and WF-19: move the test-owned fixture after durable admission; spawn with missing cwd must not produce a server, final fixture comparison fails, exit 4/fixture_changed, no thread/turn and no successful oracle. Retain moved original bytes and outside sentinel. | Real runner/process API |
| RT-02 journal creation failure | Base sections 4/7: an evidence parent that is a regular file prevents directory creation; report evidence_write_failed, preserve that file, no successful journal. Existing write_new target must remain byte-identical and not be overwritten. | Filesystem integration |
| RT-03 source CLI diagnostics | Preflight source observation: relative paths are exit 2/invalid_path; a control source whose required file path is a directory is exit 3/profile_source_type. Separate stdout/stderr, unchanged synthetic configuration/task and no run directory. | Compiled CLI |
| RT-04 preflight review cannot authorize peer | Native review/preflight contract: calling public inspection-review validation on a peer with no native profile returns profile_unqualified and performs no spawn or journal creation. | Public library negative path |
| RT-05 untrusted review bytes | Native readiness review digest/bounds and preflight review intake: both public review entrypoints reject missing, oversized, digest-mismatched, or malformed v2 review bytes as profile_unqualified, without creating a run or launching work. Uses a constructed request only, not native admission. | Public library with real filesystem |

Runtime branches requiring an actual qualified Codex source/profile remain unexecuted. They stay in the full source coverage denominator; these tests do not replace native evidence or introduce a native-admission bypass.
