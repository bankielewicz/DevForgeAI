Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\protocol_edges.rs:4:
     journal::Journal, process_windows::OwnedProcess, protocol::Session, runner::Options,
 };
 use serde_json::{Value, json};
-use std::{fs, path::PathBuf, time::{Duration, Instant}};
+use std::{
+    fs,
+    path::PathBuf,
+    time::{Duration, Instant},
+};
 
 struct Observation {
     result: Result<(), String>,
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\protocol_edges.rs:48:
             &[fixture.request.checkout_root.join("peer-case.txt")],
         )
     };
-    assert!(process.stop(options.teardown), "{case}: process tree not stopped");
+    assert!(
+        process.stop(options.teardown),
+        "{case}: process tree not stopped"
+    );
     assert_eq!(process.active(), Ok(0), "{case}: active process remained");
     drop(journal);
 
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\protocol_edges.rs:70:
             evidence.push_str(&fs::read_to_string(path).unwrap());
         }
     }
-    Observation { result, trace, events, evidence }
+    Observation {
+        result,
+        trace,
+        events,
+        evidence,
+    }
 }
 
 fn assert_no_work(observation: &Observation, case: &str) {
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\protocol_edges.rs:86:
 #[test]
 fn server_requests_are_denied_with_the_captured_response_and_never_granted() {
     let approval = run("approval-request");
-    assert_eq!(approval.result, Err("approval_required".to_owned()), "PE-01");
     assert_eq!(
+        approval.result,
+        Err("approval_required".to_owned()),
+        "PE-01"
+    );
+    assert_eq!(
         approval.trace.last(),
         Some(&json!({"id":"server-approval","result":{"decision":"cancel"}})),
         "PE-01"
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\protocol_edges.rs:94:
     );
     assert_no_work(&approval, "PE-01");
-    assert!(!approval.evidence.contains("PRIVATE_APPROVAL_REQUEST"), "PE-01");
+    assert!(
+        !approval.evidence.contains("PRIVATE_APPROVAL_REQUEST"),
+        "PE-01"
+    );
 
     let unsupported = run("unsupported-request");
     assert_eq!(
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\protocol_edges.rs:107:
         "PE-02"
     );
     assert_no_work(&unsupported, "PE-02");
-    assert!(!unsupported.evidence.contains("PRIVATE_UNSUPPORTED_REQUEST"), "PE-02");
+    assert!(
+        !unsupported.evidence.contains("PRIVATE_UNSUPPORTED_REQUEST"),
+        "PE-02"
+    );
 
     let malformed = run("malformed-request-id");
     assert_eq!(malformed.result, Err("protocol_error".to_owned()), "PE-03");
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\protocol_edges.rs:114:
-    assert_eq!(malformed.trace.len(), 1, "PE-03: malformed ID received a response");
+    assert_eq!(
+        malformed.trace.len(),
+        1,
+        "PE-03: malformed ID received a response"
+    );
     assert_no_work(&malformed, "PE-03");
 }
 
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\protocol_edges.rs:127:
         .collect();
     assert_eq!(stderr_events.len(), 1, "PE-04");
     assert_eq!(stderr_events[0]["data"]["content"], "omitted", "PE-04");
-    assert!(stderr_events[0]["data"]["bytes"].as_u64().is_some_and(|n| n > 0), "PE-04");
-    assert!(stderr_events[0]["data"]["sha256"].as_str().is_some_and(|s| s.len() == 64), "PE-04");
-    assert!(!observation.evidence.contains("PRIVATE_STDERR_SENTINEL"), "PE-04");
+    assert!(
+        stderr_events[0]["data"]["bytes"]
+            .as_u64()
+            .is_some_and(|n| n > 0),
+        "PE-04"
+    );
+    assert!(
+        stderr_events[0]["data"]["sha256"]
+            .as_str()
+            .is_some_and(|s| s.len() == 64),
+        "PE-04"
+    );
+    assert!(
+        !observation.evidence.contains("PRIVATE_STDERR_SENTINEL"),
+        "PE-04"
+    );
 }
 
 #[test]
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\runtime_edges.rs:16:
     fs::write(&sentinel, b"unchanged outside fixture").unwrap();
     let resolved_root = request::resolve(fixture.root.path()).unwrap();
     assert!(fixture.request.checkout_root.starts_with(&resolved_root));
-    assert_eq!(request::resolve(moved.parent().unwrap()).unwrap(), resolved_root);
+    assert_eq!(
+        request::resolve(moved.parent().unwrap()).unwrap(),
+        resolved_root
+    );
     assert!(!moved.exists());
     let mut events = Vec::new();
     let (code, terminal) = runner::run(
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\runtime_edges.rs:44:
     assert_eq!(terminal["worker_exit_code"], Value::Null);
     assert_eq!(terminal["thread_id"], Value::Null);
     assert_eq!(terminal["turn_id"], Value::Null);
-    assert_eq!(events.iter().filter(|e| e["kind"] == "spawn_intent").count(), 1);
+    assert_eq!(
+        events
+            .iter()
+            .filter(|e| e["kind"] == "spawn_intent")
+            .count(),
+        1
+    );
     assert!(!events.iter().any(|e| e["kind"] == "server_started"));
     assert!(!fixture.root.path().join("peer-trace.jsonl").exists());
     assert_eq!(fs::read(moved.join("task.json")).unwrap(), request::TASK);
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\runtime_edges.rs:58:
     fs::write(&parent_file, b"preserve parent").unwrap();
     let mut request = fixture.request.clone();
     request.run_dir = parent_file.join("run");
-    assert_eq!(Journal::create(&request).err().as_deref(), Some("evidence_write_failed"));
+    assert_eq!(
+        Journal::create(&request).err().as_deref(),
+        Some("evidence_write_failed")
+    );
     assert_eq!(fs::read(&parent_file).unwrap(), b"preserve parent");
     assert!(!fixture.request.run_dir.exists());
     let existing = fixture.root.path().join("existing-evidence.json");
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\runtime_edges.rs:65:
     fs::write(&existing, b"original evidence\n").unwrap();
-    assert_eq!(journal::write_new(&existing, b"replacement").unwrap_err(), "evidence_write_failed");
+    assert_eq!(
+        journal::write_new(&existing, b"replacement").unwrap_err(),
+        "evidence_write_failed"
+    );
     assert_eq!(fs::read(existing).unwrap(), b"original evidence\n");
     assert!(!fixture.root.path().join("peer-trace.jsonl").exists());
 }
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\runtime_edges.rs:77:
         .unwrap();
     assert_eq!(relative.status.code(), Some(2));
     assert!(relative.stdout.is_empty());
-    assert_eq!(serde_json::from_slice::<Value>(&relative.stderr).unwrap(), json!({"error":"invalid_path"}));
+    assert_eq!(
+        serde_json::from_slice::<Value>(&relative.stderr).unwrap(),
+        json!({"error":"invalid_path"})
+    );
 
     let storage = support::Fixture::new("WF-17");
     let workspace = PathBuf::from(env!("CARGO_MANIFEST_DIR"))
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\runtime_edges.rs:84:
-        .ancestors().nth(3).unwrap().to_path_buf();
+        .ancestors()
+        .nth(3)
+        .unwrap()
+        .to_path_buf();
     let trial_parent = workspace.join("docs/plan/framework-worker-trials");
-    let trial = tempfile::Builder::new().prefix("RT-source-type-").tempdir_in(&trial_parent).unwrap();
+    let trial = tempfile::Builder::new()
+        .prefix("RT-source-type-")
+        .tempdir_in(&trial_parent)
+        .unwrap();
     let fixture = trial.path().join("fixture");
     fs::create_dir(&fixture).unwrap();
     fs::write(fixture.join("task.json"), request::TASK).unwrap();
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\runtime_edges.rs:97:
         .arg(&fixture)
         .env("USERPROFILE", &user)
         .env("PROGRAMDATA", &system)
-        .output().unwrap();
+        .output()
+        .unwrap();
     assert_eq!(output.status.code(), Some(3));
     assert!(output.stdout.is_empty());
-    assert_eq!(serde_json::from_slice::<Value>(&output.stderr).unwrap(), json!({"error":"profile_source_type"}));
+    assert_eq!(
+        serde_json::from_slice::<Value>(&output.stderr).unwrap(),
+        json!({"error":"profile_source_type"})
+    );
     assert!(wrong_type.is_dir());
     assert_eq!(fs::read(fixture.join("task.json")).unwrap(), request::TASK);
     assert!(!trial.path().join("run").exists());
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\runtime_edges.rs:107:
-    fs::write(storage.root.path().join("source-cli.stdout.bin"), output.stdout).unwrap();
-    fs::write(storage.root.path().join("source-cli.stderr.bin"), output.stderr).unwrap();
+    fs::write(
+        storage.root.path().join("source-cli.stdout.bin"),
+        output.stdout,
+    )
+    .unwrap();
+    fs::write(
+        storage.root.path().join("source-cli.stderr.bin"),
+        output.stderr,
+    )
+    .unwrap();
     if std::env::var_os("WF_TEST_EVIDENCE").is_some() {
         let _ = trial.keep();
     }
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\runtime_edges.rs:114:
 #[test]
 fn public_preflight_review_rejects_a_peer_without_native_profile() {
     let fixture = support::Fixture::new("WF-01");
-    assert_eq!(fixture.request.verify_preflight_review().unwrap_err(), "profile_unqualified");
+    assert_eq!(
+        fixture.request.verify_preflight_review().unwrap_err(),
+        "profile_unqualified"
+    );
     assert!(!fixture.request.run_dir.exists());
     assert!(!fixture.root.path().join("peer-trace.jsonl").exists());
-    assert_eq!(fs::read(fixture.request.checkout_root.join("task.json")).unwrap(), request::TASK);
+    assert_eq!(
+        fs::read(fixture.request.checkout_root.join("task.json")).unwrap(),
+        request::TASK
+    );
 }
 
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\support\protocol_edges_peer.rs:141:
             match case.as_str() {
                 "approval-request" => {
                     awaiting_server_response = Some("approval");
-                    send(json!({"id":"server-approval","method":"item/commandExecution/requestApproval","params":{"private":"PRIVATE_APPROVAL_REQUEST"}}));
+                    send(
+                        json!({"id":"server-approval","method":"item/commandExecution/requestApproval","params":{"private":"PRIVATE_APPROVAL_REQUEST"}}),
+                    );
                     continue;
                 }
                 "unsupported-request" => {
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\support\protocol_edges_peer.rs:148:
                     awaiting_server_response = Some("unsupported");
-                    send(json!({"id":77,"method":"account/login/start","params":{"private":"PRIVATE_UNSUPPORTED_REQUEST"}}));
+                    send(
+                        json!({"id":77,"method":"account/login/start","params":{"private":"PRIVATE_UNSUPPORTED_REQUEST"}}),
+                    );
                     continue;
                 }
                 "malformed-request-id" => {
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\support\protocol_edges_peer.rs:153:
-                    send(json!({"id":{"invalid":true},"method":"item/fileChange/requestApproval","params":{}}));
+                    send(
+                        json!({"id":{"invalid":true},"method":"item/fileChange/requestApproval","params":{}}),
+                    );
                     std::thread::sleep(Duration::from_secs(30));
                     continue;
                 }
Diff in \\?\C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe\tests\support\protocol_edges_peer.rs:263:
             "account/rateLimits/read" => {
                 assert_eq!(params, &json!({}));
                 match case.as_str() {
-                    "rate-limit-rpc-error" => send(json!({"id":id,"error":{"code":-32000,"message":"PRIVATE_RATE_LIMIT_ERROR"}})),
+                    "rate-limit-rpc-error" => send(
+                        json!({"id":id,"error":{"code":-32000,"message":"PRIVATE_RATE_LIMIT_ERROR"}}),
+                    ),
                     "rate-limit-negative" => result(
                         id,
                         json!({"rateLimits":{"primary":{"usedPercent":-1},"secondary":{"usedPercent":0}}}),
