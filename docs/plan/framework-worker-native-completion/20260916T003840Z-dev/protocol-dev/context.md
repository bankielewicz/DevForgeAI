# QA-F-COV-01 protocol development slice

Selected evidence root: `C:\Projects\DevForgeAI\docs\plan\framework-worker-native-completion\20260916T003840Z-dev\protocol-dev`, assigned by the root development workflow before any product/test change. The selected package is `C:\Projects\DevForgeAI\devforgeai\experiments\codex-worker-probe` on native Windows x64. There is no Git metadata; package bytes are identified by the root candidate manifests and per-command manifests.

This bounded slice owns only new test files `tests/protocol_edges.rs` and `tests/support/protocol_edges_peer.rs`. Root owns the required `Cargo.toml` binary registration and integrated candidate. No production source, existing test, specification, operational copy, native Codex process, configuration, credential, installation, coverage collection, or framework authority is in this slice.

The failed 48-file candidate manifest is SHA-256 `809a8beb50f8b57198a963725fd0d2ccd5e586a1243e177284bff3d6dfa471dd`. Selected contract hashes are feasibility `7cb5b0cb87e515bb4d59235b615922ab4ea07ef607b8111dde6cc73c8f101b23`, native readiness `c3c0673cbf95ae7aa056d9fd0009ce89947309b943013fd7838438d6ee66b06d`, and preflight `6e781f23f221d1033896716512e9162579653b529d107e0040d49c499574afd1`. Initial `src/protocol.rs` is `51a75924b10ae57ec208161d5fcb68732a9e371b8bbbfcdd49eacf187594889d`; the existing `profile_protocol.rs` and peer are unchanged comparison inputs.

The prior raw coverage report is diagnostic only: it measured `src/protocol.rs` at 594/627 lines. Coverage counts do not establish a behavioral defect. This slice adds requirements-derived, real compiled pipe/child tests for the captured JSONL subset and pre-thread preflight. Because the expected behavior already exists, initially passing characterization is valid; no artificial Red is required. Any unexpected failure is retained and classified before a source change.

Commands use `C:\Users\bryan\.cargo\bin\cargo.exe --locked --offline` in the package directory with an isolated `CARGO_TARGET_DIR` below this evidence root. `record.py` retains exact command, cwd, environment override, timestamps, exit status, raw stdout/stderr and before/after package manifests. Python is evidence capture only.
