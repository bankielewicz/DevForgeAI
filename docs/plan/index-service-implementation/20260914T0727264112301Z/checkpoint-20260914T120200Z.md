# Implementation checkpoint

Selected project and specification remain those bound in `context.md` and `inputs.json`. Original evidence selection remains `docs/plan/index-service-implementation/20260914T0727264112301Z`, selected under service specification section 10. No checkout was copied or switched. Product changes are under `devforgeai/`; operational skills and selected specifications remain unchanged.

Candidate manifest: `attempts/040-wsl-green/candidate.json`, SHA-256 `c4244c953440253cb40f6871cbcf20f787cc5ef09e35821bfa4a5e29c892eca0`. This is a development candidate, not qualified delivery.

Implemented foundation: strict management requests/framing/errors; SQLite staging/publication and two-generation retention; Tree-sitter adapters and exact captured bytes; cooperative scanner; two persistent parser workers with serial source admission below the byte bound; registry/jobs/retry state; native Windows DACL/named-pipe and Linux UID/socket adapters; management CLI; WSL relay; native Windows tray source and opt-in preferences.

Actual execution lineage is in `executions.jsonl`. Windows tests passed in attempt 033 (25 tests), then three process-level cases passed in 039. WSL Ubuntu 24.04.5 x64 with Rust 1.98.1 passed all 28 current tests in 040. Windows rustc is 1.97.1. These focused tests do not establish all 19 required acceptance scenarios.

Retained defects/failures: missing-contract red results; a storage integer compile error; sandbox DACL denials resolved by approved native runs; tray API feature/Send compile errors; already-active Resume incorrectly reconciled (fixed); Windows detached process inherited the CLI stdout pipe (reproduced in 038, fixed with non-inheriting CreateProcessW); Linux watcher access events caused extra scans (fixed); stop confirmation now checks local ownership-lock release. Attempt 035 timed out before the stdout inheritance defect was isolated; old attempts remain intact. The diagnostic process inventory found no lingering Windows test processes before retry.

User upgraded WSL Rust during this run. Initial `/usr/bin` Rust 1.75 was superseded by verified `/home/bryan/.cargo/bin` Rust/Cargo 1.98.1; user later removed the old apt packages. WSL commands explicitly resolve the user-local binaries. Sources are read from `/mnt/c/Projects/DevForgeAI/devforgeai`; Linux build outputs are separately owned at `/tmp/devforgeai-index-20260914T0727264112301Z`. WSL is not standalone-Linux qualification.

Pending: measured coverage, remaining negative/concurrency/path-race and watcher debounce checks, full acceptance accounting, schema/examples and grammar license manifest, reproducible benchmark, release builds and documentation, native Windows visual QA, WSL relay conformance and standalone Ubuntu host qualification. No coverage percentage or protected acceptance is claimed. Startup installation remains unperformed.

No known active build jobs at this checkpoint: attempts 039 and 040 completed. Next safe action: measure Windows coverage and continue the unresolved tests/delivery artifacts against fresh candidate manifests. Recheck source/input/tool drift before a later resume; never restore this candidate over current work.
