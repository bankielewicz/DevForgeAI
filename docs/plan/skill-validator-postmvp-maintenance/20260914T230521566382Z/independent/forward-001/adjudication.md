# Independent forward-trial adjudication

Both native actors completed before their 600-second ceilings: ledger-a in 141.797 seconds and ledger-b in 146.937 seconds. They ran as native Windows Codex processes. Windows Job cleanup was VERIFIED; all bound evaluator, fixture, and specification bytes remained unchanged. Neither wrote synthetic-project files.

The inherited Codex actor configuration was **read-only**, although the outer runner had permission to write its own evidence. Both actors correctly identified that restriction, continued permitted static/helper inspection, and explicitly disclosed every undelivered artifact. Their CLI exits were zero; the runner mechanically recorded FAIL because the requested review.md did not exist. This is a failed delivery fixture prerequisite, not evidence that validator instructions improperly stopped authorized writing. No timeouts occurred.

## Ledger-a

The original expectation was a nominally positive helper. Independent actor inspection revealed a real fixture defect: selecting the input file as output overwrites the input, violating R3. The actor confirmed it through clearly labeled substituted-I/O execution, reported FAIL with incomplete execution coverage, and did not misrepresent its in-memory probes as real filesystem/native trials. It reported 14 successful helper probes and one failing preservation probe. Therefore this is **not a qualified clean positive control**. The original oracle is preserved unchanged; a corrected fixture requires a fresh evaluation.

## Ledger-b

The actor found the seeded constant-zero R1 defect with nonzero-sum examples and independently identified the same R3 input/output-alias defect. It reported ten successful and four failing in-memory probes, distinguished actual CLI help/argument execution from substituted-I/O behavior, and reported both the FAIL assessment and incomplete reporting. This provides bounded adverse-detection evidence, not completed artifact-delivery qualification.

## Shared limitations and identities

- Required normal validation reports, review.md summaries, record bundles and revision proposals were undelivered. Real-file/native target workflow obligations remain NOT_RUN.
- These were fresh CLI sessions with inherited host configuration and memory, not fully isolated model contexts.
- Earlier attempt-001 startup failures and separately host-approved attempt-002 continuations remain retained. See continuation.md.
- Snapshot: 86 files, 767847 bytes. Snapshot manifest SHA256 is `4812bd979c0e1e262d960cc6dfe1f9746bec33e9cc3c79899d440d77832b44ea`.
- Source changed after capture; `snapshot-current-differences.json` lists differences. Observed native behavior applies to the captured evaluator bytes only. It does not qualify later development source wholesale.

No further actor launch or native retry is performed by this adjudication. Two new writable-host trials, including a corrected positive fixture, require the separately selected continuation discussed with the parent.
