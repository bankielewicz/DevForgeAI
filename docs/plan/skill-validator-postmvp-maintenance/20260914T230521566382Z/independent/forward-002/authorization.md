# Explicitly selected native trials

The user authorized this continuation with: **Authorize two fresh trials**. The parent relayed that authorization before either trial launched.

Selected work is precisely the prepared ledger-c and ledger-d attempt-001 plans: each uses the retained final evaluator snapshot, explicit Codex `--sandbox workspace-write`, its separate disposable project, and a 600-second limit. No completed-native retry or automatic additional attempt is selected.

The outer Codex startup limitation was already demonstrated in forward-001, so these launch commands use the normal host escalation mechanism directly. They do not use a sandbox bypass flag, alter authentication/model configuration, or broaden the actor's requested write scope. Actor prompts, expectations, seals and input digests were saved before this authorization and launch.
