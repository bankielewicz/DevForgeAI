# Final independent forward-trial adjudication

**Selected native validator delivery scenarios: 1 of 2 completed; overall INCOMPLETE.** User-authorized plans and frozen semantic oracles were retained. No completed-native retry or deadline extension was performed.

| Scenario | Validator workflow result | Target skill assessment/evidence |
|---|---|---|
| Ledger-c corrected positive fixture | **PASS** for the selected bounded assessment-and-delivery oracle | Delivered report correctly says **INCOMPLETE**, with no demonstrated target defect and nested target-native/discovery obligations explicitly NOT_RUN. |
| Ledger-d seeded wrong-total fixture | **INCOMPLETE** after the selected 600-second timeout | Wrong-total defect demonstrated, but complete FAIL report, revision proposal and delivery summary were not saved. |

## Ledger-c: completed assessment delivery

The outer native actor completed in **582.328 seconds**, exit zero, no timeout, VERIFIED Windows Job cleanup, all protected inputs unchanged. The original runner `check` independently revalidated the sealed receipt and actual directory/artifact bytes with PASS.

The [delivery summary](ledger-c/project/review.md) links to an existing [validation report](ledger-c/project/docs/plan/skill-validations/ledger-c/20260915T001901336490Z/validation-report.md). All 26 local report links resolve. Raw JSONL contains 24/24 successful real helper cases; sampled valid outputs, argument vectors, input/sentinel hashes and before/after receipts support those claims. The actor used the enhanced sealed utility runner and independently checked its receipt. Record-integrity output reports no errors and 201 reference checks.

The report makes no fabricated defect claim, preserves incomplete native/discovery obligations, differentiates utility controls from skill qualification, and does not propose unnecessary source repair. That satisfies the frozen bounded validator-workflow oracle even though the **target ledger-c assessment remains INCOMPLETE**. It does not establish a fully passing validation of the target skill.

Independent report hash: `d519079679535cbf960fc91f9edbc7edaa9bb681c9c0f3f238e19a60dbe99dbe`. Review hash: `b07a0f4c44a6bc41e2dececc44e7b752dc0144220be53a7b703eb76135fddc70`. Details and actual usage events are retained in [independent-grade.json](ledger-c/independent-grade.json), actor-final.md and actor-usage.json.

## Ledger-d: detection achieved, delivery unfinished

The outer actor exceeded its selected **600-second limit**, with **602.891 seconds** elapsed including cleanup, exit 124, timeout true, VERIFIED Windows Job cleanup and all protected inputs unchanged. Runner readback correctly returns NOT_RUN for incomplete execution; the mechanical missing-file observation is retained without promoting it to a target defect.

Raw helper evidence records **19 executed cases: 15 pass, 4 fail**. Positive, negative, mixed and large nonzero sums incorrectly produced zero. The remaining invalid-input and preservation observations passed. Sample evidence binds the actual exit-zero/wrong-content receipt, independently selected expected total and stream hashes. The actor's last progress also describes fresh target workflow observations; retained nested fixture outputs support wrong-total production and Boolean-input rejection, but unfinished campaign reporting limits their qualification.

At timeout, **review.md, validation-report.md and revision-spec.md were absent**. The last progress message saying these were being written is not delivery evidence. Frozen negative-oracle requirements for an actual report and actionable revision remain unfulfilled. This scenario is INCOMPLETE, not a qualified complete adverse forward-test, and timeout alone does not establish a new validator source defect. See [independent-grade.json](ledger-d/independent-grade.json) and actor-last-progress.md. No completed-turn usage event exists; usage was not estimated.

## Provenance and scope

The initially delegated approval request was canceled before execution. Root subsequently obtained direct host approvals and started the same sealed attempt-001 directories; [launch-transfer.md](launch-transfer.md) preserves this sequence. Each actor used the explicitly selected workspace-write sandbox and separate synthetic project. No sandbox bypass flag, authentication/model override, installation or automatic extra attempt was selected.

The final evaluator snapshot contains **89 files / 847974 bytes**. Manifest SHA256 is `61CD1D0A024B0DEA100B2DA7751E1C2DAC76EF51F713CDDC3F54268433B94EE9`. All 14 executable hashes matched the parent's final coverage source capture. Final [source readback](final-source-readback.json) found no differences between any captured evaluator file and the development source at adjudication. Both outer receipts also verify their own bound snapshot and fixture identities.

These are fresh native Codex actor observations with inherited host configuration/memory; no claim of full model or OS isolation is made. Results are independent development evidence, not protected framework acceptance. Native qualification for the selected two-case delivery scope remains incomplete until a separately authorized fresh adverse delivery trial completes; no retry is initiated here.
