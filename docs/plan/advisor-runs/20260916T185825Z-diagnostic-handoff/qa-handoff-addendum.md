# Assessed clarifications for independent offline QA

Use this addendum alongside the preserved [original handoff](../../framework-worker-diagnostics/20260916T181820Z-dev/qa-handoff.md). It clarifies future QA planning only; no QA or native execution is recorded here. All original prohibitions remain applicable. Candidate manifest SHA256: `419c98b437a44ce479554170bda36b40f4b8a05b3f2307361680d44fab039540`.

1. Explicitly inventory the OS accounting-query failure case. Development did not exercise `protocol.rs:258`, which projects `query_failed` with null total/active counts. Retained LLVM coverage records zero execution for that expression. Runtime verification is NOT_RUN; source/serialization inspection is separate evidence and cannot supply a runtime PASS. No injectable accounting seam was found. If independent QA cannot identify a feasible authorized technique against the frozen production bytes, record the case NOT_RUN and its prerequisite/readiness BLOCKED with the concrete reason. Do not add a production seam, silently waive the case, or claim complete diagnostic-branch verification. Ready independent checks may proceed, but unresolved required checks prevent overall QA PASS. Absence of a seam alone does not prove all non-mutating techniques impossible.

2. Author independent tests in a fresh QA-owned evidence/harness location. Prefer external stimuli and a separate Rust harness using unchanged public interfaces where feasible. Preserve the original package and snapshot. If a QA-owned package copy is necessary for private test access, declare its absolute path and test-only delta before execution, verify every production file against the frozen manifest, and account for path-sensitive fixture/source identities. Do not assume relocation is behaviorally equivalent. Copy-based evidence is supplemental until its mapping to the selected candidate is established; run the inherited regression/coverage campaign against the frozen original with QA-owned output directories. No production code change is authorized by this addendum.

3. Declare the complete required case inventory before execution. The 133 inherited cases are a baseline, not a cap; include every new required QA case and count each logical case once. Failed, errored, skipped, blocked and unexecuted required cases are not passes. Repeated normal/instrumented executions and retries do not create additional cases or erase failed attempts. Report the required unit denominator separately (40 inherited units plus new required units); do not mix integration cases into the unit pass rate. Also report the overall required suite rate and every mandatory invariant.

4. Run the full `--all-targets` campaign and all required subfixtures using the original handoff's command/setup guidance. Do not omit slow process tests to improve time or metrics. Declare all first-party executable source files before collection; retain the 13-file inventory with no first-party exclusions. Recompute executed-line coverage and compare per-file counts with the retained developer analysis, explaining differences without inventing a per-file 95% floor. At the fixed 3497-line denominator, 3323 covered lines are needed: the historical 3335 result can lose 12 covered lines and still meet 95%, but not 13. Aggregate evidence does not establish which tests contribute that margin.

5. Report branch coverage separately. If it was not collected, report NOT_RUN and the reason; claim unsupported tooling only with version/capability evidence. Do not fabricate a percentage or a new branch threshold. The executed-line floor remains mandatory independently of test pass rates and mandatory scenarios.

Read back candidate, snapshot and selected input hashes before the final scoped QA result. Preserve all previous evidence. The reviewer response and this addendum do not supply test results, permit native Codex launch, advance phases, or establish compiled-Rust framework acceptance.
