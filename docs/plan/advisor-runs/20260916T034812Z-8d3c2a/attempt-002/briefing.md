## REQUEST TYPE and ONE-LINE ASK
Type: stuck; execution retry after retained connection refusal, not a new review. Determine the smallest contract-compliant next step after the worker's installed-source inventory succeeded but the current parent environment contains ANTHROPIC_API_KEY. Assess whether selecting a child-only clean environment is already authorized ordinary preparation or requires an explicit new environment-policy selection; do not authorize it yourself.

## REPO ROOT
C:/Projects/DevForgeAI. Verify claims against the repository; treat this briefing as claims. Native Windows and PowerShell are selected, not WSL.

## TASK AS GIVEN
The user approved the concrete source-identity proposal by saying: "ok - i approve then". Its scope is docs/plan/framework-worker-native-continuation/20260916T013303Z/source-inventory-proposal.md. This approves implementation, independent QA, and an installed-profile source collection retry after QA PASS; native preflight/operator findings/trials retain their prerequisites. Latest user instruction: "if you get stuck, feel free to use $advisor tool and ask claude for guidance.  its new & just implemented to be part of the DevForgeAI framework."
Earlier history was compacted. The selected artifacts and current results were read back; do not assume missing original chat messages authorize additional effects. Native WN-01/WN-02 are previously selected but remain unexecuted.

## SCOPE
Read-only advice about the environment prerequisite and the next permitted action. Preserve the frozen worker, installed profile, credentials, operational skills, old evidence and exact one-shot native selections. No implementation, login, deployment, framework acceptance, or native execution is requested from Claude. This advisor invocation itself uses the advisor skill's subscription mode; that Claude-specific policy is not automatically a Codex worker policy.

## BINDING CONSTRAINTS
- AGENTS.md:49 -> "- Compiled Rust is mandatory for DevForgeAI framework implementation:" Read this and the ownership/QA rules.
- AGENTS.md:123 -> "- Follow the user's selected scope." Preserve previous evidence and operational state.
- .agents/skills/dev/SKILL.md:14 -> "An implementation request authorizes ordinary reversible product source, test, and evidence work within scope." Avoid demanding a redundant confirmation when existing authorization suffices.
- docs/specs/framework/runtime/codex-worker-feasibility-v1.md:120 -> "Native execution requires a separate user-selected trial request" This requires rejection of API credentials in the child environment and prohibits changing operational config to make the profile pass.
- docs/specs/framework/runtime/codex-worker-preflight-v1.md:14 -> "Compile one closed" Read the fixed launch/no arbitrary settings clause.
- docs/specs/framework/runtime/codex-worker-preflight-v1.md:20 -> "To remove the circular dependency" Preflight permits false findings; work still requires all four supported findings.
- docs/specs/framework/runtime/codex-worker-preflight-v1.md:59 -> "If the installed profile cannot meet these predicates" Preserve the concrete failed prerequisite and stop dependent native work; no policy relaxation implied.
- docs/specs/framework/runtime/codex-worker-source-identity-v1.md:61 -> "Only after independent QA PASS" [MODIFIED THIS SESSION] This selected companion was implemented and independently tested, but its existence alone does not prove correctness or expand authorization.

## FACTS
- Frozen candidate is devforgeai/experiments/codex-worker-probe, 56 files, manifest SHA256 3108f976f7d51b734ab995ebf729302ef74a4386ddaafb5561cddbd6f588d65c. The identity manifests and final readback are under docs/plan/framework-worker-source-identity-qa/20260916T021843Z-retest.
- Independent offline QA report is docs/plan/framework-worker-source-identity-qa/20260916T021843Z-retest/qa-report.md, SHA256 b2e8304c07a89a7c155669e1d0a9e9b93c8f5a0ac966b567fca1fee461b7f2d0. It reports 123/123 original tests, 37/37 unit subset, 3191/3339 executable lines, and 42/42 overlapping traceability groups. Check underlying records as needed; this briefing does not establish acceptance.
- Read-only installed collection receipt is docs/plan/framework-worker-native-continuation/20260916T034357Z-source-identity/source-observation-001/receipt.json. Native exit0, 0.078seconds, candidate unchanged. Raw stdout is a schema2 inventory:137 entries,36 physical file bindings,56 directory indices,45 explicit absences,1 approved junction. It started no Codex process.
- devforgeai/experiments/codex-worker-probe/src/runner.rs:96 -> "        if std::env::vars_os().any(|(key, _)| {" [MODIFIED THIS SESSION elsewhere for final-check placement; this credential guard predates the change]. At line98, the guard matches any name containing API_KEY or ACCESS_TOKEN, or exactly OPENAI_BASE_URL. It runs before verify_preflight_review and spawn.
- The exact output of a PowerShell environment-name-only query in this session was one line: ANTHROPIC_API_KEY. No credential value was read into model output or retained. This is ephemeral observed state, not independently verifiable by Claude's restricted Read/Grep/Glob tools. No variable was removed from the parent.
- .agents/skills/advisor/SKILL.md:17 -> "- `auth`: `subscription`" The newly installed advisor explicitly permits removing only ANTHROPIC_API_KEY in Claude's child environment. That is evidence of the advisor's own selected interface, not evidence of authorization for worker changes.

## INFERENCES
High confidence from source and environment-name observation: the current inherited environment will cause profile_unqualified before Codex spawn. This has not yet been reproduced through compiled preflight; no runtime denial is claimed. Uncertain interpretation: filtering just this unrelated-provider credential in an isolated worker-harness child may be ordinary restrictive preparation, or may constitute an unselected environment policy. Resolve that using actual contracts, not generic preference.

## STATE
Since the initial briefing was written, AGENTS.md changed externally to SHA256 3b8e2a112c438b536ab59db2abbcfd6e08ebdd7f41caa13cdf6d5c49866fab86, adding advisor guidance. The first briefing's line88 anchor was stale; this briefing corrects the scope clause anchor. The candidate and all four governing specifications remain unchanged. The original inventory is historical, and a fresh compiled source collection at docs/plan/framework-worker-native-continuation/20260916T034357Z-source-identity/source-observation-002/receipt.json passed (same counts, stdout SHA256 2029d7889cd4054d2791fd0e55684dae48ac0c37aac12710e068aad858a1fbad). State observation at state-observation-001.json in that continuation root records only AGENTS.md changed among prior inventory files and the same single prohibited environment name. No native preflight has run.
Worker source and four selected contracts are frozen and independently QA-qualified for offline scope. Changes in this session: bounded plugin junction mapping, inventory schema2, source rechecks, test additions, companion contract, and evidence. See docs/plan/framework-worker-source-identity/20260916T021843Z-dev/changed-files.json. Subsequent work only created evidence. Git metadata is absent; branch/HEAD and tracked/untracked distinctions are unavailable. Unsaved editor state is UNKNOWN. Do not modify anything. Authority implementation/provisioning remains deferred.

## ATTEMPTS
Installed source command: QA-built target-original/debug/devforgeai-codex-worker-probe.exe profile-sources --checkout-root C:/Projects/DevForgeAI/docs/plan/framework-worker-trials/20260916T034357Z-source-observation/fixture. Exact argv and captured bytes are in the receipt above; exit0. Inventory contents are on disk, so not pasted here. Environment query inspected names only; exact output ANTHROPIC_API_KEY. Compiled effective-profile preflight and native trials have not run. Advisor standalone version/help preflight passed for Claude2.1.273 with auth_mode=subscription; receipt at docs/plan/framework-worker-native-continuation/20260916T034357Z-source-identity/advisor-preflight/receipt.json. Attempt 1 completed with process exit 1 after 181.875 seconds. Its CLI JSON reports terminal_reason=api_error, API Error: Connection refused (ConnectionRefused), zero API duration/tokens/cost, empty stderr and no reviewer response. Receipt: docs/plan/advisor-runs/20260916T034812Z-8d3c2a/attempt-001/execution.json; raw stdout at the same attempt. No advice was produced. This is the final permitted attempt, using the identical immutable request, auth mode, restrictions and USD1 cap; normal host escalation is requested for the likely sandbox/network restriction. No third attempt is permitted.

## CURRENT PLAN
1. Preserve the successful installed inventory and unchanged candidate.
2. Keep the compiled credential guard and all stored credentials/configuration unchanged.
3. Decide whether to retain an expected compiled preflight denial with the current environment and stop, or whether an explicitly recorded, child-only clean environment already fits authorized preparation. Do not guess or conceal the input change.
4. Never generate all-true operator findings. Even a later positive preflight does not establish native trial success or framework acceptance.

## OPTIONS CONSIDERED
Keep current environment: expected guard denial; least interpretation risk but may stop unnecessarily. Remove only ANTHROPIC_API_KEY from a one-use worker-harness child environment: preserves parent/stored credentials and the compiled guard, but authorization and framework-policy ownership need assessment. Relax the Rust guard, add generic filtering to framework behavior, delete stored credentials, change global environment or saved config: excluded from this selected amendment and not proposed.

## ASSUMPTIONS
The observed variable still exists in the parent environment; this must be rechecked before execution. Existing native selections have not been cancelled. Successful source inventory plus offline QA does not prove active profile isolation. A reviewer recommendation cannot create authority or expand user scope. Challenge any assumption not supported by the artifacts.

## OPEN QUESTIONS
Is a one-use child-only environment selection contract-compliant without a new product change or new user selection? If not, specify the concrete minimal selection or prerequisite needed. Is even running a predictably denied preflight useful here, or should the static prerequisite evidence suffice? Identify any missed source/profile prerequisite. Do not read credential stores or request credential values.

## WHAT WOULD CHANGE MY MIND
An explicit contract allowance or prohibition governing child-environment selection; evidence that the guard targets a different environment or does not apply to preflight; a current environment-name observation with no prohibited names; or a demonstrated mismatch between this briefing and the retained candidate/QA/source receipts.

## EXCERPTS
The credential-name predicate is exactly: key.contains("API_KEY") || key.contains("ACCESS_TOKEN") || key == "OPENAI_BASE_URL". All other evidence is cited rather than shortened into purported complete output. No secret values, authentication files or raw configuration content are included.
