# Selected native diagnostic

The user selected one bounded no-work native diagnostic on 2026-09-16 in
`C:\Projects\DevForgeAI`, on native Windows, following
`../../framework-worker-diagnostics-qa/20260916T191731Z/native-diagnostic-handoff.md`.
The externally supplied artifact-index SHA256 is
`552912696c5260091a8cb2305b13d1f1987e53630b610dd66afec55f84d132d9`.

This directory owns fresh preparation, execution and readback evidence only.
The fixture is the exact frozen `task.json` in the required sibling trial layout.
The selected original-package binary is the QA-built CLI, not the QA wrapper or
protocol peer. The operation is `preflight --request`, never `run`.
The fixed worker identity, launch policy, model `gpt-6-astra`, effort `high`,
120-second dispatch bound, 10-second RPC bound and 5+5-second cleanup bounds
remain unchanged. The outer recorder bound is 145 seconds.

One native preflight invocation is permitted, with zero retries, no thread/turn
or model-work dispatch, and all four review findings false. Source observations
and terminal inspection are read-only operations that do not launch Codex.
After a host approval/permission-context change, collect a new source inventory
and bind a new immutable request/review version before any native invocation.
Preserve every previous observation/version. Stop on a failed prerequisite.

The parent environment is unchanged. Only `ANTHROPIC_API_KEY`, observed present
by name, is omitted from a copy of the child's environment; no credential value
is logged. Other credential/provider variables cause a preparation stop. No
alternate CODEX_HOME, installed-state edit, login, installation, junction change,
policy/predicate relaxation, product repair, or true operator finding is selected.

Python/PowerShell in this folder records evidence and prepares inputs only.
The unchanged compiled Rust CLI retains all admission, policy, process and
diagnostic decisions. No new product implementation or regression suite is
selected. No WN-01/WN-02 credit. Framework acceptance remains NOT_EVALUATED.

The official OpenAI App Server initialization documentation was checked for
context: <https://learn.chatgpt.com/docs/app-server#initialization>. The pinned
local contracts and unchanged implementation govern this experiment; current
documentation is not native qualification of the pinned executable.
