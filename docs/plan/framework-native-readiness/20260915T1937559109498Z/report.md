# Native readiness and authority preparation — delivered

The requested preparation is complete: executable identity was re-discovered, the current profile was inspected without activation, two bounded trial drafts were prepared, and the first operator-attested acceptance authority was specified. Native execution remains blocked by concrete implementation/profile prerequisites. No protected authority is installed or qualified.

## Deliverables

| Artifact | Outcome |
| --- | --- |
| [Native readiness amendment](../../../specs/framework/runtime/codex-worker-native-readiness-v1.md) | Specifies exact physical executable/two-junction identity, narrow reparse exception, versioned restrictive launch policy and12 required readiness cases. Identity-only offline slice is ready for development; launch-policy keys/effective behavior need pinned-version verification. |
| [Static profile review](profile-review.md) | BLOCKED for native execution; sanitized observed configuration and explicit unknowns. |
| [Trial selection proposal](trial-selection.proposed.md) | WN-01/WN-02 exact requests, fixture/oracle bytes, proposed model/effort and limits; both NOT_RUN. |
| [Authority contract](../../../specs/framework/runtime/acceptance-authority-v1.md) | Operator-attested import selected by user; concrete Windows identity/ACL/IPC, byte upload, policy evaluation, persistence/receipts and20 qualification groups. Specified, not implemented. |
| [Worker evidence adapter](../../../specs/framework/runtime/acceptance-worker-evidence-v1.md) | Exact first-policy scope and deterministic observations, with authenticated operator judgments for provenance/independence/FFI review. |
| [Development handoff](development-handoff.md) | Bounded worker and authority implementation sequence, independent QA and later provisioning/native selection. |

## Main findings

1. **Launcher resolution is concrete.** Two junctions lead to the physical0.154.0 executable; alias/physical SHA-256 both `be96b992178b1e467c225800da0d65f2c86d5eba1ef0b14632f65db381cbdfde`. Fresh physical executable version/help probes succeeded. Current request.rs still resolves and rejects the captured reparse path; production code was not changed.
2. **The current profile cannot be approved for the specified trial.** Two enabled-by-default MCP entries, twelve enabled plugins and five cached version candidates with hook definitions were found. Effective activation/trust is not established. User config also enables cached search/memory and configures the unelevated Windows sandbox. None of those settings alone proves actual effects, but they prevent an honest no-external-effects approval.
3. **A restrictive invocation requires an explicit contract amendment.** The proposed20 override arguments disable observed integrations for these invocations without persistent configuration writes. The base contract currently forbids such overrides. CLI0.154.0 help supports `-c` and `--strict-config`, but per-key compatibility/effective disablement still needs verification. No silently weakened guardrail or trusted=true record was produced.
4. **First authority trust mode is selected.** User explicitly chose operator-attested evidence first. The service will authenticate a separate operator, then independently check bytes, completeness, numeric floors and mandatory predicates in Rust. A trusted operator cannot force ACCEPTED over failed conditions. This explicitly trusts human evidence provenance; it does not claim an isolated automatic QA runner already exists.
5. **Initial policy is concrete and narrow.** The proposed inventory binds32 candidate files,8 source files,50 test names,28 mandatory offline groups and2592 reference evidence artifacts (6,390,619 bytes). These are existing inputs, not fresh executions or installed policy. A receipt would identify offline-only scope; native/full-framework acceptance needs its own satisfied policy.

## Checks actually performed

- Native Windows read-only source/configuration/extension inventory, with credentials and arbitrary environment values omitted. No auth store was read.
- Three bounded Codex discovery commands: physical executable `--version`, `--help`, `app-server --help`; exact argv/cwd/duration/exit and output hashes retained.
- Verified prior independent QA manifest and all2592 referenced files before policy inventory generation; preserved the original corpus.
- Checked candidate/source/contract readbacks, draft JSON shapes/digests, original fixture equality,12 NI and20 AU case IDs, authority inventory cardinalities, local document links and JSON readability. Actual outcomes are in verification.json; it is documentation verification, not runtime QA.
- Original worker source and base contracts are unchanged. New artifacts are confined to this evidence root and three additive documents under docs/specs/framework/runtime. No index manifest, operational skill, installed configuration or startup setting was edited.

Discovery limitations: web retrieval of three `.md` documentation URLs returned unsupported content type and one Windows URL returned404; normal official HTML pages were successfully read instead. Initial scoped search also reported absent workspace `.codex/`; this is an observed absence, not an installed profile. One document patch failed context matching and was corrected before publication; no product test failed/retried. The retained helper sources show exact preparation operations. Config/extension observations are time-bound and must be refreshed before launch; a local file inventory does not prove active cloud-managed state.

## Status and next action

| Area | Status |
| --- | --- |
| Readiness/authority documentation preparation | COMPLETE, subject to recorded factual/link readback |
| Worker executable-identity implementation | NOT_RUN; specified next offline development slice |
| Restrictive launch policy implementation/qualification | NOT_RUN; version-specific key/effect verification required |
| Effective native profile approval | BLOCKED; current integrations/unknown effective state |
| WN-01 / WN-02 | NOT_RUN; intentionally unqualified draft requests |
| Authority Rust implementation | NOT_RUN; bounded contract and first adapter delivered |
| Operator account/service/ACL provisioning | NOT_RUN; distinct operator identity and concrete effects must be reviewed after builds exist |
| Framework acceptance | NOT_EVALUATED; no qualified service decision observed |

Next work is development of the identity slice and the isolated authority core against the delivered contracts. Independent QA follows each changed candidate. Before real Codex execution, resolve policy compatibility and issue fresh qualified profile/request records; present those exact digests for native selection. Before authority installation, prepare actual SID/binary/ACL effects and obtain the separate provisioning authorization required by the parent design. This report is not asking for approval of unknown future effects.

Current host provides `$dev` and `$qa`. The preparation used OpenAI Docs for current configuration/support references; neither runtime skill was used to claim nonexistent implementation or QA. No install/deploy/remote message was sent.

## Evidence and documentation

All artifacts and the three new specification files are bound by delivery-manifest.json; delivery-readback.json records actual readback. candidate-before/after.json preserve the32-file candidate identity; input-identities/input-after.json preserve original contracts; profile-source-inventory/profile-source-after.json expose profile drift. Raw Codex help/version receipts remain separate from trial evidence.

Current official [configuration precedence](https://learn.chatgpt.com/docs/config-file/config-basic), [integration controls](https://learn.chatgpt.com/docs/config-file/config-reference), [hooks](https://learn.chatgpt.com/docs/hooks), [Windows sandbox](https://learn.chatgpt.com/docs/windows/windows-sandbox), and [app-server documentation](https://learn.chatgpt.com/docs/app-server) were read for this review. App-server remains a nonproduction investigation under both the pinned local help and selected contract. These sources do not establish this account's model entitlement or the effective runtime profile.
