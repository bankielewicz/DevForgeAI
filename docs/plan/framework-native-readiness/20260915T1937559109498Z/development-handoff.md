# Development handoff — native readiness and first acceptance authority

Open `C:\Projects\DevForgeAI` in Codex on native Windows. Read AGENTS.md and verify the document/input/candidate bindings in this directory's delivery-manifest.json. No Git metadata exists at workspace root. User selected preparation and explicitly selected operator-attested evidence first; runtime changes and provisioning have not been performed here.

## 1. Next ready worker slice

Use `$dev` with `docs/specs/framework/runtime/codex-worker-native-readiness-v1.md` NI-01/NI-T01..06 plus the unchanged base `codex-worker-feasibility-v1.md`. Existing candidate: `devforgeai/experiments/codex-worker-probe`, corrected manifest `docs/plan/framework-worker-qa/20260915T1901023951559Z-retest/selected-manifest.json`, SHA-256 `3f65ade36cf8186fe711da73f1a2794a17d7fb51e02bf66261e9bba0ce158e39`.

Implement only pinned physical identity and the two exact launcher-junction observations through red -> green -> refactor -> QA. Keep original schema-command capture, all fixture/run/profile reparse restrictions, no-retry semantics and F-01/F-02 repairs. Add meaningful positive/negative tests with synthetic aliases; do not mutate installed Codex junctions. Verify code/test drift before editing; no restoration of old bytes. Return changed-file/build manifests and fresh exact test/coverage evidence. Independent `$qa` retest is required for the changed candidate. Do not transfer old candidate PASS to changed bytes.

## 2. Worker profile slice before native trials

NI-02 specifies a closed schema2 native request/review and one compiled-in restrictive argv, with legacy peer semantics preserved. The exact20 proposed overrides are in restrictive-launch-policy.proposed.json. First resolve pinned0.154.0 support/effective behavior from version-matched definitions; documented general override syntax alone is insufficient. Unknown or unenforceable disablement is a blocker. No user config, trust store, installed plugin, account or CODEX_HOME mutation is selected.

After policy implementation and offline QA, conduct a fresh complete profile review for each exact fixture cwd. Prepare qualified review/request bytes only if all required findings are supported. Current unqualified records remain retained. Present the actual version2 request/review/policy digests and model/effort for explicit native selection. Native account/model preflight is part of the bounded authorized trial; do not perform an unreviewed standalone startup with enabled integrations.

WN-01 and WN-02 remain NOT_RUN. Two v1 draft requests and exact fixture/oracle copies exist under trials/ for review; they are intentionally not launchable. Model/effort proposal is gpt-6-astra/high from current defaults, not a guaranteed available or user-selected native model. Do not blindly execute the drafts or change false review findings to true.

## 3. First authority implementation

Use `$dev` with both `docs/specs/framework/runtime/acceptance-authority-v1.md` and `acceptance-worker-evidence-v1.md`. User chose operator-attested import. Implement a separate Cargo workspace at `devforgeai/authority`; do not alter the index package or use the worker as acceptance authority. The frozen first-policy inventory is authority-worker-inventory.proposed.json and its reference artifacts are preserved in the existing retest root.

Implement bounded authenticated IPC, byte-only upload and sealed storage, exact policy/adapter checks, operator attestation, revision/idempotency, transactional decisions and verified readback. No arbitrary candidate code execution under service privileges. Generate a concrete provisioning proposal after builds exist, resolving intended service/operator/evaluated SIDs and ACL effects without applying them. Use real negative fixtures and raw receipts; meet both95% floors and all mandatory groups. No mocked IPC/ACLs may qualify the protected native groups. Service installation, account creation, startup changes and protected-state initialization require the later explicit reviewed provisioning selection.

Independent QA first assesses code and ordinary regressions. Actual AU-02/03/04/19 protection cases then run against explicitly authorized disposable installed state and real identities. Qualified bootstrap manifest installation is a trusted-operator/admin action after that evidence, never self-certification by the evaluated agent. No acceptance receipt may be claimed before those steps succeed.

## Outputs and ownership

Each implementation/QA assignment writes a fresh timestamped evidence root; preserve this preparation, original failures, corrected candidate and final independent retest. Report exactly implemented versus proposed versus BLOCKED/NOT_RUN. Development cannot close independent QA defects or manufacture framework acceptance. No source implementation, native execution or provisioning was done by this preparation assignment. Current host catalog exposes `$dev` and `$qa`; these are conversation skills, not shell commands.
