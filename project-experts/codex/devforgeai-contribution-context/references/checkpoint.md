# Checkpoint and transfer

Read this only when checkpoint mode applies. Resolve the installed skill root from the loaded SKILL.md, independently of the shell working directory and consuming project's selected roots. Use the package-local [shared handoff template](../assets/handoff.md); it contains intentional fill-at-invocation slots. Its provenance is in [derivation metadata](derivation.json). No author checkout is required at runtime.

Confirm the selected workflow permits the delivery location. Missing or conflicting target ownership does not revoke independently valid report-only authority. If no writable authorized delivery path exists, report that exact limitation in the terminal and stop dependent persistence; do not choose a substitute. Ordinary orientation requires no such path.

Fill the shared content model with actual facts:

- Separate the recovery task/state from the underlying contribution and phase. Identify role/provider/owner, assignment reference, worktree/base/fence and carried authorization, or explicit unknowns.
- Use the input/output table as the bounded recovered inventory: exact selected identities, resolvable store/path, full digests, sections, draft/adopted state, verification performed and missing facts. Include relevant package/candidate/run/finding identities and competing observations. Hash completed outputs after finalization. Exclude the handoff itself from its digest-bearing inventory.
- Describe actual changes, proposals versus adopted decisions, open findings, blocked prerequisites and unchanged historical omissions. Attribute reported checks separately from checks you performed. Use canonical outcome labels with cause/scope; unknown required observations remain unresolved.
- Give one next task with owner or ownership gap, prerequisites, permitted destination and completion evidence. The copyable prompt carries those exact references and boundaries. Use only an actually available skill/command or plain-language task; recommendations supply no new authority.
- Record actual ownership disposition, gate evidence and invalidation conditions: changed selected authority/rules, candidate, runtime, base, assignment or relevant evidence. Do not release ownership on the strength of your own handoff.

Retain the artifact envelope: truthful producer, actual execution reference, source references, evidence and explicit missing inputs. Use null when an ID/decision is genuinely absent; do not fabricate acceptance. Preserve causal upstream direction; do not create a digest cycle. Add stable section IDs to newly authored records while keeping references to older source headings accurate. Remove template drafting instructions and fill required fields; genuine unknowns are explicit facts, not unresolved filler.

Finalize the entire handoff, save it, read back its bytes, and compute SHA-256 over those final bytes. Then deliver its **absolute saved path and full 64-character SHA-256 outside the handoff**, in the permitted terminal response or external receipt. A hash seen only in an internal tool trace is not completed delivery. The handoff must never contain its own complete-byte digest. Preserve it unchanged after delivery; a correction needs a distinct retained revision and new receipt. Reuse a current checkpoint when facts are unchanged, unless the user requests a new checkpoint.
