---
description: Orchestrator for the spec-driven-research skill that captures and persists research findings
argument-hint: "[topic] | [--resume ID] | [--search query] | [--list ] [--category type]"
model: opus
---

# Research Command

Invoke the spec-driven-research skill to capture and persist research findings.

---

## Invocation

This command invokes:
```
Skill(command="spec-driven-research", args="{user_args}")
```
